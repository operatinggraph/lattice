# Backlog — Lattice (Stream 2): features + component maintenance

The **Lattice** lane: platform **features** + **component maintenance**. Advanced by the **Lattice Steward**
(`steward-autonomous`) **round-robin across components** (Core / Weaver / Loom / Refractor / Loupe +
cross-cutting features); hydrated by the **Surveyor** (`platform-surveyor`) **+ PO-routed platform gaps**
(demand the verticals surface). Scales, status vocabulary, cross-lane rules: see [../backlog.md](../backlog.md).
Design: `implementation-artifacts/agentic-ops-swimlanes-design.md`.

Status: 📋 ready · 🏗️ in-flight (worktree) · ✅ done (commit) · 📐 design-proposed · 🚧 blocked.

---

## Active initiative — Loupe: the View & Control app *(first Edge Lattice prototype)*

> **Name:** *Loupe* (tentative). A jeweler's loupe is the tool you inspect a crystal through — apt for
> a window onto the lattice.

**What it is.** An internal **view-and-control client** for a running Lattice deployment: browse Core
KV (vertices / aspects / links), submit operations, install / uninstall capability packages, drive each
component's control plane (Refractor / Weaver / Loom), and observe Health KV. The first concrete UI on
top of the platform.

**Framing (the "a-ha").** Loupe is an **internal, trusted-operator tool**, but built *around the
Edge-node local-first machinery* — the same substrate + VAL mirror + reconcile-by-revision a real Edge
Lattice node would use — so it doubles as the **first prototype of Edge Lattice** without taking on the
Edge security layer. It is a stepping stone: prove the local-first view/control loop now; grow into the
per-user sovereign node later, once the deferred security pieces land.

**Non-goals (explicitly OUT — these stay Phase 3+).** Loupe runs as a **single trusted / privileged
identity** (like the CLI / admin), so this initiative does **not** build:

- per-user **authN / authZ**,
- the **Gateway**,
- **read-path authorization** (D1),
- **Personal Lens** / per-user filtering.

Loupe reads the **full** graph directly as a trusted client; per-user scoping is a later Edge evolution.

**Capabilities (v1).** Read Core KV + lens projections · submit ops (forms driven by DDL
self-description: `inputSchema` / `fieldDescription` / `examples`) · install / uninstall packages ·
Refractor / Weaver / Loom control ops · Health KV dashboard · view + upload large binaries (photos,
lease PDFs).

**Enabling work (the picked "Now" set) — ✅ all shipped (see the Progress board).**

| Enabling item | Why Loupe needs it | Imp | Size |
|---|---|---|---|
| **Loom control plane** | *Hard blocker.* Refractor + Weaver expose `lattice.ctrl.*` responders; Loom has none. Build `internal/loom/control` + `cmd/lattice/loom` + a `lattice.ctrl.loom.*` responder (list running instances, pause/resume consumers, inspect/fail an instance), mirroring `internal/weaver/control`. | ★★★ | M |
| **Large-file / binary handling** | Loupe shows + uploads profile photos and lease PDFs. NATS Object Store (chunked, content-addressed); the graph holds a pointer-aspect, the store holds the bytes; blobs never flow through the Refractor. *Authorization simplifies under the trusted-tool model* (binds to the trusted identity, not per-user). | ★★ | M–L |
| **Refractor substrate inner-package migration** | Hygiene + directly supports "around Edge machinery": ~30 `internal/refractor` files still hold raw `nats.go` / `jetstream` handles; a clean substrate boundary is what makes a local / embeddable node tractable. Needs substrate Watch / UpdatesOnly / NumPending / durable-consumer helpers first. | ★★ | M |

**Supporting / not blocking.** `UI Form Schema aspect` (brainstorming #52) would standardize form
rendering (DDL self-description already suffices for v1) · NATS **WebSocket** transport if Loupe is
browser-based (desktop / TUI / Electron use the native client) · Processor + Bridge have **no** control
plane — Loupe reads their Health instead (a minimal admin endpoint is optional, later).

**Open design questions for the epic.** Transport + host (desktop / TUI / Electron / browser-WS) ·
does Loupe embed a **local VAL mirror** via reconcile-by-revision (the Edge machinery) or read live
only · whether to add a thin read/query convenience surface (direct KV + lens reads work for v1).

---


## Component maintenance

Grounded, definition-of-ready maintenance/observability items surfaced by the **Surveyor** from the
component code + docs + Health/CI signals. Tagged by component; the Steward round-robins these alongside
features. None below needs a frozen-contract change or is an architectural fork.

| Item | What + why (grounding refs) | Imp | Size | Status |
|---|---|---|---|---|
| **[Refractor] Capability-Lens liveness/lag alert** | ✅ **Done** — the Refractor instance heartbeat now implements the Contract #5 §5.5 `issues[]` + §5.4 `status` anomaly channel (previously hard-coded healthy/empty) driven by auth-plane (`projection.IsAuthPlane`, `capability-kv`) lens liveness: a **paused** capability lens → `CapabilityLensPaused` (error ⇒ `status:unhealthy`), an **active** lens with `consumerLag` over threshold (default 100, overridable) → `CapabilityLensLagging` (warning ⇒ `status:degraded`); `metrics.capabilityLens.<name>` `{status,consumerLag,alert}` emitted every cycle; `since` persists across heartbeats, drops on resolve. Read-only (no authz path / Core KV / projection touched). **No contract change** — the §5.5 schema already existed. The **Lamplighter** classifies it now. Design + thorough-lead-review: `implementation-artifacts/refractor-capability-lens-liveness-alert.md`. | ★★ | S–M | ✅ done |
| **[Weaver] Heartbeat status/issue-severity inconsistency** | ✅ **Done** (4de7677) — the heartbeater hard-coded `status` to the lifecycle string, so a heartbeat carrying issues still reported `status:"healthy"` (self-inconsistent with Contract #5 §5.2/§5.3: issues empty iff healthy; warning⇒degraded, error⇒unhealthy). `aggregateStatus(lifecycle, issues)` now escalates the steady-state status to the worst open issue severity (error wins over warning); `"starting"`/`"shutdown"` are reported verbatim (lifecycle phase, not a health grade). Both halves of the original decision now closed: per-target data gaps were already downgraded to `severity:"warning"` (1bcdce4), and the source `status` emission is now consistent. Read-only Health-KV self-report; no Core KV / contract change. Tests: `TestAggregateStatus` table + e2e now asserts `status:"unhealthy"` alongside the error issues it checks. Mirrors Refractor's `aggregateStatus` (with `"starting"` also protected — §5.3-defensible, transitions are author's discretion). | ★★ | S | ✅ done |
| **[Loom] Heartbeat status hard-coded to lifecycle string** | ✅ **Done** (c59a39f) — the Loom heartbeater set the Contract #5 `status` field to the raw lifecycle string, so a heartbeat that appended a `ConsumerPaused` warning issue still reported `status:"healthy"` (violates §5.2/§5.3 — issues empty iff healthy; warning⇒degraded, error⇒unhealthy). New `aggregateStatus(lifecycle, issues)` escalates the steady-state status to the worst open issue severity (error⇒unhealthy wins over warning⇒degraded), leaving the `"starting"`/`"shutdown"` lifecycle phases verbatim; issues now sorted by message for deterministic output. **Completes the health-honesty sweep across all four self-reporters** (Refractor caplens-liveness → Weaver `aggregateStatus` → Processor lane_lag → Loom). Read-only Health-KV self-report; no Core KV / contract change (§5.2/§5.3 already mandate the mapping). 6-case `TestAggregateStatus` unit table (the structural-pause issue path isn't trivially e2e-triggerable; manual pause raises no issue). Loom was the round-robin stalest component (12h). | ★★ | S | ✅ done |
| **[Refractor] Postgres adapter `Truncater`** | ✅ **Done** (f537f6b) — `PostgresAdapter.Truncate` now issues `TRUNCATE TABLE "<table>"` (quoted via `quoteIdent`; the constructor already rejects an embedded double-quote), so a `Pipeline.Rebuild(truncate=true)` against a SQL-target lens clears the table in one statement instead of silently skipping (the old warn at `pipeline.go:400`). Mirrors the NATS-KV adapter's purge-every-key `Truncate`; Postgres targets carry no projection-write guard (Contract #6 §6.2 — unconditional last-writer-wins), so there is no watermark to reset and the replay re-inserts from a clean table. `var _ Truncater` compile-time assertion added; `withTimeout` applied like Upsert/Delete. **No live impact yet** (no Postgres-target lens ships), but the prerequisite for the deferred **Postgres read-model** is now in place. No DDL/keys/contract change. Unit tests (SQL construction + interface satisfaction) + a `POSTGRES_TEST_DSN`-gated integration test. | ★ (★★ once a Postgres-target lens ships) | S | ✅ done |
| **[Refractor] Doc drift — `consumer.Manager` retired** | ✅ **Done** — `docs/components/refractor.md` sub-package table + lens-lifecycle step 5 now describe the live design: per-lens durable consumers are owned by each `pipeline.Pipeline` via `substrate.ConsumerSupervisor` (durable `refractor-<ruleID>`); the `consumer/` package holds only the adjacency `Bootstrapper`. Pure doc fix. | ★ | XS | ✅ done |
| **[Core] Processor health: fabricated `lane_lag` + no status/issues anomaly channel** | ✅ **Done** (f16e625) — the Processor heartbeat surfaced fabricated `lane_lag` zeros + always-healthy status (a backed-up write lane was invisible to the Lamplighter on the sole Core-KV writer). Now reports the **real backlog** from the durable consumer (`processor-main` `NumPending`), threaded via `AttachConsumer` before `Run` starts (race-free, consumer created first). Since one consumer spans all `ops.*` lanes, per-lane keys are `null` ("not measured per-lane") + the genuine aggregate is **`lane_lag_total`**; a `null` total means unreadable this tick — never a fabricated `0`. Backlog over a configurable threshold (default 100, `SetLagThreshold`) raises **`ProcessorLaneLagging`** (warning) and `aggregateStatus` escalates the steady-state status to **degraded** (`starting`/`shuttingDown` protected), with `since`-persistence + resolve-drop. **Completes the health-honesty sweep** (Refractor caplens-liveness + Weaver `aggregateStatus`). Read-only Health-KV self-report; no Core KV / contract **commit**. `buildHealthDoc` extracted KV-free; fake-consumer table covers null/below/above threshold, Info error, lifecycle protection, custom threshold, `[]` marshalling (race-clean). **✅ Contract #5 §5.4 clarification ratified + committed (2026-06-27)** (`docs/contracts/05-health-kv.md` — documents the Phase-1 single-consumer `null` per-lane + `lane_lag_total` reality, with the per-lane keys **reserved** for the architecture's planned per-lane consumers — see the new [Core] per-lane-consumers item below). | ★★★ | S–M | ✅ done |
| **[Core] Bootstrap key construction → substrate key helpers** | ✅ **Done** (6998a39) — the primordial seeding path now builds all vertex/aspect/link keys via the canonical `substrate.VertexKey` / `AspectKey` / `LinkKey` helpers (`internal/substrate/keys.go`) instead of raw string concatenation. Covers `nanoid.go`'s key-constant derivations (vertex + holdsRole link keys, placed after the `IsValidNanoID` validation loop so the panic-on-invalid helpers are safe) and every concat in `primordial.go` (MetaRoot DDL, operator role, grantedBy links, package-install DDLs, aspect-type metas, lens aspects); the aspect-type loop also stops recomputing each key twice. Output is **byte-identical** — verified by the bootstrap e2e seeders (full-graph build) + `go test ./internal/bootstrap/...`, golangci-lint, and STRICT lint-conventions all green. Starlark key construction inside DDL scripts (`meta_ddl.go`) is correctly untouched (it's Starlark, not Go); `install_ddl.go`/`system_actors.go` had no Go concat sites. Centralizes the Contract #1 §1.1 key shapes so an off-by-a-dot can no longer silently mis-seed the primordial graph. No contract change. | ★ | XS–S | ✅ done |
| **[Loom] Guardless-step recovery check-before-act probe** | On **total `loom-state` loss** followed by a re-triggered `StartLoomPattern`, a fresh instance replays guards from cursor 0 — a guarded step whose precondition now holds is correctly re-skipped, but a **guardless step has no guard-replay signal**, so replay always re-runs it; and because each step's `requestId` derives from `(instanceId, cursor)`, the gen-2 `requestId` differs from gen-1's committed one, so the Contract #4 `vtx.op.<requestId>` dedup tracker cannot collapse it → the op commits a **second time** (`internal/loom/engine.go` step-replay; `docs/components/loom.md` "Disaster recovery — guardless steps", ~line 139). This is the Contract #10 documented-bound doctrine (bounded + operator-visible — one extra commit per guardless step in the recovery window, never an unbounded loop), and loom.md explicitly flags "**A robust check-before-act variant is Phase-3 hardening.**" A bounded **check-before-act probe** (before re-running a guardless step in a recovery generation, probe Core KV for the step's already-landed effect) would collapse the double for non-idempotent guardless ops while leaving the guard-replay idempotency intact. **Low practical urgency** — fires only on disaster recovery (not a normal restart), and the authoring guidance ("give a guard to any costly step") already mitigates it; the **Weaver** reclaim check-before-act probe is the higher-stakes sibling (per-sweep, already filed below). **No frozen-contract change** (probe reads, no new shape); **needs design** — what effect-evidence each step kind probes, the per-recovery read cost, and how the probe distinguishes "not yet run" from "already ran" for a guardless step (the §10.6 invariant-2 gap it must solve via Core-KV effect-presence). | ★ | S–M | 📐 **awaiting-Andrew (ratification) — 2026-06-29** — design: [`loom-guardless-step-recovery-effect-guard-design.md`](../../implementation-artifacts/loom-guardless-step-recovery-effect-guard-design.md). **Mechanism = the `effectGuard`** — the inverted-skip twin of a guard (same §10.5 grammar; engine **skips a guardless step whose effect-guard is TRUE** = its effect already landed in Core KV → recovery-idempotent, advance + alert). **Grounding overturned the row's "no frozen-contract change" note:** a clean check-before-act needs an author-declared per-step field → **Contract #10 §10.5 (additive optional `effectGuard?`) + a §10.6 invariant-2 refinement — staged UNCOMMITTED in `main`** (the diff is the proposal; backward-compatible, absent ⇒ today's behavior). **One mechanism fork, designed through + REJECTED:** a generation-independent step token (key on `(patternRef,subjectKey,cursor)` so the §4 tracker collapses the re-run) is **unsound** — it conflates a *legitimate* second episode of the same pattern/subject with a recovery generation (silent drop of real work); the fresh `instanceId` per generation is load-bearing, so the effect-guard (not the token) is the fix. **No architectural fork.** **Honest value split:** Fire 1 (declarative aspect effect-guard) is **buildable now** + lands the contract/engine seam (systemOp/aspect case); **Fire 2 (the prize — userTask/externalTask) needs a `linkPresent` atom over the already-ratified `kv.Links`/`KVListKeysFilter` seam** (op-time bounded link-enumeration, Contract #2 §2.5.1) — composes, no new fork. Self-resolved every open question (design §8). **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds Fire 1 now; Fire 2 once the `kv.Links` substrate half ships).** |
| **[Loom] Cover the redelivery-dedup + op-meta-deregister engine paths** | ✅ **Done** (d8bfa34) — pinned the two live correctness paths that sat at 0% coverage: `stateStore.deleteToken` (`internal/loom/state.go:470`, the **idempotency-on-redelivery** guard) and `patternSource.removeOpMeta` (`internal/loom/source.go:294`, the **live op-meta deregister**). New `redelivery_dedup_internal_test.go`: deleteToken clears an advanced pointer, is idempotent on a double-delete (the redelivery shape), no-ops on a never-written token, and propagates a genuine substrate failure rather than swallowing it; removeOpMeta deregisters the tombstoned op's operationType, drops every type pointing at the same id, leaves unrelated mappings intact, and no-ops on an unknown id. `removeOpMeta` → 100%, `deleteToken` → 80% (the residual line is the `ErrKeyNotFound` swallow, unreachable via NATS — KV `Delete` is itself idempotent, which is exactly why the double-delete is a no-op). Test-only; no behavior / key / contract change. Mirrors the recent substrate / Go↔Starlark boundary pins. Loom round-robin pick (2nd-stalest component, ~12h). | ★ | XS–S | ✅ done |
| **[Weaver] Reclaim check-before-act probe (close the documented rare-double)** | On an **expired-lease reclaim**, the sweeper re-dispatches the gap's action as a fresh episode (`reclaim`, `internal/weaver/reconciler.go:218`) **without probing whether the prior episode's effect already landed** — the §10.3 "documented rare-double," visible only via the sweep's Warn log + `sweepReclaims` counter (`reconciler.go:230`, `:422`; weaver.md "Reconciler sweep" / "Actuator crash mid-flight"). For external-I/O gaps this is harmless (the **bridge** de-dups on `idempotencyKey`, FR58), but `assignTask` can mint a **second task** and `triggerLoom` a second pattern run before the first completes. A bounded **check-before-act probe** (read the entity's current convergence state / the gap's existing in-flight evidence before re-firing — the same evidence the `inflight_<g>` lens column already exposes for lane-1) would collapse the double for the non-idempotent actions while leaving the lease/level-reconcile recovery intact. Grounding: `reconciler.go` reclaim path + the `inflight_<g>` suppression machinery (weaver.md "Dispatch suppression"). No frozen-contract change (probe reads, no new shape); **needs design** — what evidence each action type probes, and the read cost per sweep. | ★★ | S–M | ✅ **Andrew-ratified (2026-06-27) — Option D (Weaver-state backoff)** — design: [`weaver-reclaim-check-before-act-probe-design.md`](../../implementation-artifacts/weaver-reclaim-check-before-act-probe-design.md). Grounding flipped it: the *correctness* double is already closed by ratified §10.3 (claimId idempotency); the residual is **phantom-reclaim churn** (up to ~1,440 redundant no-op ops/trackers/Warns per open human task over the 30-day TTL). Ratified fix = an **exponential backoff** on the reclaim of the collapse-only userTask actions (`assignTask`/`triggerLoom`), keyed on the mark's existing `ClaimedAt` + `dispatchCount` — **no Core-KV probe, no new state, no frozen-contract change** (the Option-A §10.3 edit is **withdrawn**; the first reclaim still fires at lease-expiry, so lost-dispatch recovery is unchanged). `directOp`/external untouched. **✅ SHIPPED (04c7689)** — built per Option D (see Done log); one grounded **deviation from the design, flagged for Andrew**: the design's "the mark TTL backstop already covers survival" premise is **false at the default constants** (`markTTLBackstopFactor`=2 ⇒ mark TTL = 2×lease = 60m ≪ the 24h backoff cap), so a backed-off mark would TTL-expire mid-backoff into a **markless open gap** → a CDC redelivery would mint a fresh `claimId` → a **duplicate task** (`fireEpisode` CAS-creates on mark absence). Fix (within Option D's mechanism — no extra writes, no new field): a userTask reclaim **sizes the re-armed mark's TTL to outlast the next backoff window**, so the mark is always reclaimed before it can die. This is the only behavior beyond the literal design; `directOp` marks keep the byte-identical default TTL. Not a contract change. |
| **[Core] Processor per-lane consumers (ConsumerSupervisor adoption)** | The Processor runs a **single** `processor-main` durable consumer over all `ops.*` lanes (Phase-1 simplification), but the architecture's **design-of-record is per-lane consumers** (the 3-lane-consumer model — `lattice-architecture.md:741`; the `LATTICE_PROCESSOR_LANES_*_CONSUMERS` override at :566; Contract #2 §2.3 lanes default/meta/urgent/system). Adopting **one `substrate.ConsumerSupervisor` per lane** (like every other component) would (a) make the Contract #5 §5.4 per-lane `lane_lag` keys **real** (today `null` — see the Processor-health item above), (b) let lanes be **paused/resumed + scaled independently** (priority isolation — an urgent-lane backlog no longer hidden behind `default`), and (c) bring the sole Core-KV writer in line with the substrate consumer pattern used everywhere else. **No frozen-contract change** (§2.3 lanes + §5.4 per-lane keys already specified; this *populates* them). **Needs design** — consumer-per-lane wiring, per-lane ack/ordering, the fan-in to the single commit path, and migration from the single consumer. Surfaced by Andrew during the §5.4 ratification (2026-06-27). | ★★ | M | 📐 **awaiting-Andrew (ratification)** — design: [`processor-per-lane-consumers-design.md`](../../implementation-artifacts/processor-per-lane-consumers-design.md). **No frozen-contract change** (Contract #2 §2.3/§3.7 + #5 §5.4 already specify per-lane consumers + reserve the per-lane keys — this *builds to* them). **No architectural fork.** Shape: one `substrate.ConsumerSupervisor` with four lane specs (`processor-{default,urgent,system,meta}`), `meta` pinned serial (§2.3); the commit path becomes a `SupervisedHandler` + `Classify` (mirrors Loom/Weaver); real per-lane `lane_lag` via `PendingForConsumer`. **One grounded finding:** the Processor is the **first request-reply consumer** of the supervisor, so Fire 1 adds a small `substrate.Message` reply-inbox seam (`ReplySubject`+`Header` — internal Go API, *not* a contract). 3 fires (Fire 1 disposition refactor at parity → Fire 2 lane split → Fire 3 per-lane concurrency from config); Fire 4 Processor control plane recommended as its own row. Recommended pre-build §5.2 adversarial pass on the meta-lane/DDL-cache concurrency boundary. **Publish-side verified (2026-06-28, Andrew-prompted): lane routing already works — meta DDL ops correctly publish to `ops.meta` (installer/lens/bootstrap), engines→`ops.system`, clients→`ops.default`, `urgent` unused; the gap is consume-side (one concurrent consumer, no meta-serial pinning) → so the §2.3 meta-serial guarantee is *unenforced today* (latent DDL-cache race) and Fire 2/3's meta-pin is a correctness fix, not just isolation.** **✅ Andrew-ratified (2026-06-28).** **🏗️ Fire 1 SHIPPED (c16f739, CI green run 28345910504)** — the Processor commit path now runs on a `substrate.ConsumerSupervisor` (single all-lanes `processor-main` spec, byte-identical durable config: same four-lane `FilterSubjects`, 30s AckWait, DeliverAll — **no lane split, no migration**). `HandleMessage(jetstream.Msg)` → `dispatch(substrate.Message) (MessageOutcome, substrate.Decision)`, separating the client reply from ack disposition; `SupervisedHandler()` is the production path, `HandleMessage` retained as the in-process test/NFR-R1 adapter. **Substrate seam:** `Message.ReplySubject`+`Header` (first request-reply consumer) and `ConsumerSpec.FilterSubjects` (multi-filter set) — internal Go API, no contract change. **Health:** `AttachConsumer`→`AttachBacklogReader(supervisor)` via a `LaneBacklogReader`; `lane_lag` stays null-honest (per-lane null, `lane_lag_total` from the single backlog). **Honest deltas:** the two former blind `Nak`s (dedup-lookup / commit failure) now `NakWithDelay` (5s floor, kills the hot-loop); and the **consumption-concurrency model changed** — the old `cons.Consume` dispatched callbacks concurrently, the supervised pump drains one op at a time (design-of-record per §6, sufficient at single-cell MVP; **Fire 2's four parallel per-lane pumps + Fire 3's per-lane fan-out restore/exceed concurrency** — should follow reasonably soon). **Scoping deviation (sound, 3-layer-reviewed):** no `Classify`/`Probe`/infra-pause in Fire 1 (mirrors Loom/Weaver; infra-pause without a Probe would be a permanent-stall regression) — per-lane infra classification lands with the lane split. Tests added: `dispatch` decision-mapping table (accepted/duplicate→Ack, malformed/empty/auth-denied→Term, commit-failure→NakWithDelay), supervised-path commit (`PendingForConsumer` drains), substrate `FilterSubjects`+`Header`. Gates + `-race` + object-gc convergence green; Acceptance Auditor clean. **🏗️ Fire 2 SHIPPED (c5e1fc2 + docs d9653a7, pushed; CI watch in progress)** — the single `processor-main` spec is replaced by **four per-lane durables** (`processor-{default,urgent,system,meta}`, `FilterSubject=ops.<lane>`, built by `processor.LaneSpecs`); lanes drain on independent pumps (priority isolation — urgent no longer behind default). **Real per-lane `lane_lag`:** the heartbeater reads each lane's `PendingForConsumer` → `lane_lag.{default,urgent,system,meta}` real, `lane_lag_total` = sum of readable lanes, per-lane null on a transient read error (never a fabricated 0); `ProcessorLaneLagging` now raised **per lane** (lane named, `since` tracked independently per lane). **`meta` serialized:** single pump **+ `MaxAckPending=1`** (Contract #2 §3.7 — new `ConsumerSpec.MaxAckPending` substrate field), so a DDL commit + its synchronous DDL-cache invalidation never races a 2nd concurrent DDL; non-meta lanes run concurrently with meta and stay safe via the RWMutex value-copy DDL-cache snapshot + step-8 OCC. **§5.2 pre-build adversarial pass run + CONFIRMED SOUND** (value-by-copy `Lookup` + write-lock-held `Invalidate` atomic swap + fresh-ref-per-load + step-8 OCC; Edge-Case Hunter independently verified). **R4 migration:** `conn.DeleteStreamConsumer` (new idempotent substrate seam) retires the legacy `processor-main` durable **best-effort** on startup (a failed cleanup only parks a harmless pumpless durable — never blocks serving; the MAJOR 3-layer finding "fatal-on-transient-delete" fixed to best-effort); the retained stream re-delivers to the new lane durables, idempotent via the step-2 dedup tracker (durable — pruner retired). **No frozen-contract change** (built to §2.3/§3.7 + §5.4; §5.4's "Phase-1 today / single-consumer" *descriptive* framing is now superseded by this landed adoption it reserved the keys for — **flagged for Andrew**, not edited). Full 3-layer adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor — all ACs MET, no contract touched). Tests: `LaneSpecs` table, per-lane isolation + meta-serial integration (embedded NATS, `jsstore.Dir`), substrate `MaxAckPending` threading + `DeleteStreamConsumer` idempotency, per-lane health lane_lag (all-null/partial/two-lanes/threshold/lifecycle). **🏗️ Fire 3 SHIPPED (fb8fa5a, CI watch in progress)** — per-lane pump concurrency from config. New `substrate.ConsumerSpec.Workers` spins up N pump goroutines per lane, all binding the **same** pull durable; JetStream load-balances the consumer across them (each message delivered exactly once, `MaxAckPending` caps total in-flight). Sized per lane by `processor.LaneConsumers(os.Getenv)` from the `LATTICE_PROCESSOR_LANES_<LANE>_CONSUMERS` convention with the ratified defaults **default=2 / urgent=4 / system=2 / meta=1**; a malformed/sub-1 override keeps the lane default, and **`meta` is fail-closed clamped to 1** (LaneConsumers clamp **+** LaneSpecs pins Workers=1 + MaxAckPending=1 — double-guarded; a meta fan-out would break §2.3 serialization). Each worker is an **independent pump with its own pause state machine** sharing only the durable (no new shared mutable state); operator Pause/Resume is lane-wide (fans out to every worker), infra/structural pauses stay per-worker (self-healing). **Workers≤1 is byte-identical to Fire 2** — every Loom/Weaver/Refractor consumer is one pump with the untouched JetStream default prefetch. **Mechanism deviation from the ratified design — flagged for Andrew:** the design prescribed `ConsumerSpec.DeliverGroup` (a JetStream **queue group**), but DeliverGroup is a **push**-consumer concept, incompatible with the supervisor's **pull** `Messages()` iterator. The correct pull equivalent is **N iterators on one durable + bounded prefetch** (`fanOutPullMaxMessages`=8 per fan-out worker — the JetStream default of 500 would let the first iterator hoard a finite burst, starving the others and reducing the lane to serial). Same load-balanced/each-once semantics, no queue group; **no contract change** (internal substrate Go API; the existing `DeliverGroup` field is left in place for a future push use case). **Within-lane ordering relaxes under Workers>1** — two same-lane ops can commit concurrently; causal dependencies stay safe via the **step-8 OCC + the op's `reads`**, NOT lane FIFO (design §3.3/§6); `meta` remains strictly serial. **No frozen-contract change** (built to §2.3/§3.7 + §5.4). Thorough-lead-review + an adversarial diff pass (concurrency/blast-radius); the full 3-layer was scoped down per the design's Fire-3 guidance (additive config, no new disposition logic) — the high-risk concurrency boundary (DDL-cache/meta-serial) was the §5.2 pass already discharged in Fire 2, and meta stays pinned. Tests: substrate fan-out concurrency (N workers concurrent + each-message-once, **race-clean**) + single-worker prefetch parity; `LaneConsumers` env parsing (defaults/override/meta-clamp/malformed/padded) + `LaneSpecs` per-lane Workers + meta pin. Gates: go build, make vet, golangci-lint (0), STRICT lint-conventions (0), go test substrate/processor/loom/weaver/refractor (race on substrate+processor), lease-convergence e2e — all green (Gate 2/3 + verify-kernel deferred to CI: shared-stack collision locally + my change doesn't touch the auth plane they exercise). **Remaining: Fire 4** (Processor control plane — `lattice.ctrl.processor.*` responder, list lanes+lag, pause/resume a lane, consumed by Loupe — its own backlog row). |
| **[Loupe] No component doc** *(doc gap)* | ✅ **Done** — `docs/components/loupe.md` created (present-tense, matching the established `docs/components/*` shape): mandate + the "first Edge Lattice prototype / built around the Edge machinery" framing · the v1 capability table (Core-KV browse / Health+system-map / tasks / control-proxy / packages / DDL-driven op submission / object upload), keyed to the `cmd/loupe/*.go` files + `/api/*` routes · the **read/write paths** with the **P5 inspector exception** spelled out (the only `cmd/<app>` allowed to read Core KV directly, and *why* — it's the admin/console inspector; writes still go through the Processor per P2) · the **security posture** (trusted single-identity, no authN/authZ, the load-bearing **127.0.0.1 loopback-bind** rule + the `warnIfNonLoopback` startup warning + the `objectDisposition` anti-XSS guard) · Implementation status (built Phase 3 / Edge-evolution deferred). Wired into `docs/components/README.md` (new **Experience layer** index entry + Implementation-status row). Closes the last component without a doc — all seven now covered. Pure doc; no frozen-contract change. | ★★ | S | ✅ done |
| **[Loupe] Pin the loopback-bind security guard** *(test-only)* | ✅ **Done** (75db1ae) — `cmd/loupe/main_test.go` now table-tests both pure functions: `isLoopbackHost` (loopback IPv4 incl. the whole 127/8, `::1`, case-insensitive `localhost` ⇒ safe; empty-host bare-port, `0.0.0.0`, `::`, LAN/public IPs, and a non-`localhost` hostname incl. a `localhost.evil.com` spoof ⇒ NOT loopback) and `warnIfNonLoopback` (captures the slog output and asserts a loopback/localhost bind stays quiet while a bare port / all-interfaces / LAN / public / unparseable addr emits a WARN). Pins the load-bearing exposure check of the auth-less admin tool so a mis-classification can't silently widen the bind. Mirrors the `objectDisposition` anti-XSS pin (fd0cacd). Test-only; no behavior / contract change; all gates green. | ★★ | XS | ✅ done |
| **[Refractor] Doc drift — stale "Loupe doesn't read `status`/`issues[]`" residual** *(doc gap)* | `docs/components/refractor.md` "Capability-Lens health" → **Residual follow-ups** (L317-323) still says the **Loupe** health dashboard + system-map nodes "*do not yet read the Contract #5 `status` / `issues[]` fields … not yet on the Loupe component cards*" — but that **shipped 2026-06-26** (`2877a1c`): `cmd/loupe/health.go` `componentLiveness` now fuses freshness + §5.4 `status` + worst §5.5 `issues[]` severity (error→red, warning→yellow), shared by `computeHealth`/`computeSystemMap`. So the note describes a gap that no longer exists (it reads as open follow-up work). Fix: rewrite the residual to reflect that Loupe consumes the anomaly channel today, leaving only the genuinely-open residuals (lag hysteresis + the Gateway token-revocation hard control — see the next row + the D1/Gateway feature rows). Pure doc; no contract change. Grounding: `docs/components/refractor.md:317`, `cmd/loupe/health.go:153` (`componentLiveness`), commit `2877a1c`. | ★ | XS | ✅ **Done** — `refractor.md` "Residual follow-ups" rewritten: the stale Loupe-reads-freshness-only claim **and** the lag-hysteresis residual both marked **shipped** (Loupe `componentLiveness` fuses §5.4 `status` + §5.5 `issues[]`; the lag alert now debounces — see the row below), leaving only the genuinely-open Gateway token-revocation hard control (lands with D1). Pure doc, no contract change. |
| **[Refractor] Capability-Lens lag alert hysteresis** | The Capability-Lens liveness alert raises `CapabilityLensLagging` (warning ⇒ `status:degraded`) on a **single** heartbeat where `s.ConsumerLag > threshold` (`internal/refractor/health/lattice_heartbeater.go:275`, `evalCapabilityLenses`), and the issue is **dropped the next cycle** when lag dips back under (`reconcileCapIssues`) — so a **one-cycle backlog spike flaps** the Refractor heartbeat degraded→healthy, and the Lamplighter classifies a transient blip as a real anomaly. The constant's own doc comment flags it ("*A warning … it self-resolves on*" the next heartbeat — `lattice_heartbeater.go:22`) and `refractor.md:321` lists **"Lag-threshold hysteresis (a one-cycle spike self-clears on the next heartbeat) … remain future work."** Fix (needs light design): damp the flap — either **raise-after-N-consecutive** over-threshold cycles, or a **dual-threshold** (raise at `threshold`, clear only below a lower `clearThreshold`); keep the paused-lens path (a hard `error`) un-hysteresised (no spike there). Read-only Health-KV self-report; **no Core KV / contract change** (§5.4/§5.5 author-discretion). Grounding: `internal/refractor/health/lattice_heartbeater.go:253-302`, `docs/components/refractor.md:321`. Surveyor 2026-06-28; un-filed before. | ★ | S | ✅ **Done** (3d40b9d) — per-lens lag **hysteresis** added to `evalCapabilityLenses`: a lens must stay strictly over the raise threshold for `raiseCycles` consecutive heartbeats (default **3** ≈ 30s sustained at the NFR-O1 floor) before `CapabilityLensLagging` raises, and once raised it holds until lag falls to/below a lower **clear-threshold** band (default = the raise threshold). The paused-lens path stays a hard `error`, un-debounced. Per-lens state (`lagHysteresis{overStreak,raised}`) pruned to live lenses each cycle, reset when a lens leaves `active`, guarded by a dedicated mutex; defaults apply in production (the threshold field is likewise unwired). Read-only Health-KV self-report, **no Core KV / contract change** (§5.4/§5.5 author-discretion — my impl-level design ratification as lead). Tests: spike-doesn't-flap, raise-after-sustained (`since` at the raise cycle), clear-band-holds-then-clears, reset-on-pause, prune-absent-lens. Thorough-lead-reviewed (XS/S health-self-report, no security/capability-plane touch). |
| **[Refractor] `LatencyRingBuffer` (NFR-P3 instrument) percentile pin** *(test-only)* | The per-lens **projection-latency ring buffer** — the doc's *"primary instrument"* for the NFR-P3 ≤500ms end-to-end latency principle (`refractor.md:330`, surfaced on the heartbeat as `lens.<name>` p95/p99/mean/count, `refractor.md:307`) — is **entirely untested**: `NewLatencyRingBuffer` / `Record` / `Snapshot` / `percentile` are all **0%** (`internal/refractor/pipeline/latency.go`; the `pipeline` package is the lowest-covered in Refractor at **44.5%**, vs 65-100% elsewhere). The `percentile`/p95/p99 math is pure + deterministic — a table test (empty buffer, single sample, wrap-around overwrite at capacity, known p50/p95/p99 over a fixed series, count saturation) pins the instrument the latency NFR rests on. Test-only; no behavior/contract change. Grounding: `internal/refractor/pipeline/latency.go:31-110`, `docs/components/refractor.md:307,330`. Surveyor 2026-06-28. | ★ | XS–S | ✅ **Done** (af5d43d) — `internal/refractor/pipeline/latency_test.go` added: deterministic table over capacity fallback (≤0 ⇒ default), empty/single-sample snapshots, the mean + nearest-rank p50/p95/p99 math on a fixed 1..100ms series, wrap-around (newest-window-only) semantics, non-draining reads, and the `percentile` clamp edges (q=0/0.5/1). `latency.go` now ~100% (`NewLatencyRingBuffer`/`Record`/`Snapshot` 100%, `percentile` 90% — the residual is a defensive unreachable clamp); `pipeline` package coverage **44.5% → 49.8%**. Test-only, all gates green. |
| **[Loupe] HTTP handler-layer coverage (`server.go` 0%)** *(test-only)* | ✅ **Done** (c318ee5) — added `cmd/loupe/server_test.go`: an `httptest`-against-`registerRoutes`-mux layer with a **nil-conn** server (the package's no-embedded-NATS style) pinning the previously-0% HTTP wiring. **`requireConn` guard:** every data handler answers a JSON **502** (not a nil-`*substrate.Conn` panic) when NATS is unreachable — table over all 15 conn-dependent routes asserting status + `application/json` + an `error` field. **Routers → 100%:** `handleObjects` (`objects.go:73`) and `handleControl` both fully exercised, incl. the bad-shape **400 defaults** that decide *before* the conn check (PUT/GET-bare/POST-with-oid/DELETE-bare; control wrong-part-count/method); plus `handleOp`'s POST gate. **Writers:** `writeJSON`/`writeError`/`requireConn`/`mustJSON` status/shape/Content-Type pinned directly. Asserts the **actual** 502 (`StatusBadGateway` — NATS-down = bad gateway; the "503" in the original filing was loose). Package coverage **42% → 52%**; `handleObjects` + `handleControl` + `writeError` + `handleControl` at 100%. Deterministic (no NATS / goroutines / time). Test-only; no behavior / contract change; all gates green. Closes the last Loupe survey gap (the loopback-guard pin shipped 75db1ae). | ★ | M | ✅ done |
| **[Core] Processor Health-KV emission paths 0%-covered** *(test-only)* | The Processor's live, security/operationally-significant **Health-KV emitters are entirely untested (0%)** — the channel the **Lamplighter** reads off the *sole Core-KV writer*. `internal/processor/health_alerts.go` is wholly uncovered: `HealthAlertEmitter.EmitAlert` (writes `health.alerts.security.<code>` per Contract #5 alert convention — today the `stub-auth-active` warning, emitted at startup + every Nth `Authorize` in stub mode, i.e. *production running auth-less*), `RecordClaimAttempt` (the read-modify-write `health.processor.<inst>.claim-attempts.<outcome>` counters, fired on **every** commit-path claim — success at `commit_path.go:352`, every failure outcome at `:551`), the `New{HealthAlert,ClaimAttempt}Emitter` nil-conn→noop guards (`commit_path.go:687,735` wire them live), and pure `alertSeverity`; plus `health.go:355 emitCapabilityAuthSignals` (the per-heartbeat `step3-latency` signal, `health.go:213`). So a regression that mis-shapes the security-alert body, drops a claim-outcome counter, or silently swaps a severity ships **unnoticed** — and the Lamplighter consumes exactly these keys. The pure parts (`alertSeverity`, the nil-conn/empty-bucket noop branches, key/body construction) are unit-testable directly; the full KVPut path is exercisable with the existing embedded-NATS substrate fixture (`jsstore.Dir`, the package's per-lane-health test style). Mirrors the recent Refractor `LatencyRingBuffer` + Loupe `server.go` health-channel pins. Test-only; no behavior / key / contract change. Grounding: `internal/processor/health_alerts.go`, `internal/processor/health.go:355`, `internal/processor/commit_path.go:352,551,687,735`; Contract #5 alert convention. Surveyor 2026-06-28. | ★★ | S | ✅ **Done** (db3cb23) — `internal/processor/health_alerts_test.go` pins the previously-0% Health-KV emitters (the Lamplighter's input channel off the sole Core-KV writer): `alertSeverity` known+unknown→`warning` (100%); `New{HealthAlert,ClaimAttempt}Emitter` nil-conn/empty-bucket/empty-instance → inert noop (no nil-conn panic on first write); `EmitAlert` (live embedded-NATS KV) writes a well-formed `health.alerts.security.<code>` body (key/alertCode/severity/observedAt/verbatim details) + a re-emit **overwrites** ("currently happening", not an audit log); `RecordClaimAttempt` (live KV) is a read-modify-write counter starting at 1, incrementing per outcome, **distinct outcomes track independent counters**; `emitCapabilityAuthSignals` (live KV) — nil authorizer emits nothing, an attached authorizer emits the zero-sample `step3-latency` liveness doc. Coverage: `alertSeverity` 100%, `EmitAlert` 62.5%, `RecordClaimAttempt` 76.5%, `emitCapabilityAuthSignals` 87.5% (residuals are fault-injection-only marshal/KV-write error branches). Reuses the established `startEmbeddedNATS`/`provisionHarness`/`jsstore.Dir` fixtures (CI-parallel-safe). Test-only; no behavior / key / contract change. All gates green (build/vet/golangci 0/STRICT-conventions 0/`-race`). Thorough-lead-reviewed (test-only S, no security/capability-plane *change*). |
| **[Weaver] HealthSink pause-restore round-trip uncovered** *(test-only)* | The per-consumer `consumerHealthSink.Load` **paused-restore branch** (`internal/weaver/health_sink.go:78-85`) and `pauseReasonFromString` (`:108`) are the mechanism that makes a pause **survive a restart** — `Load` runs at `substrate.ConsumerSupervisor` Add time and re-derives the durable's `manual`/`structural`/`infra` pause from the `health.weaver.<inst>.consumer.<name>` doc, which is exactly what weaver.md promises for the FR30 control plane (`disable`'s `PauseManual` "survives restart via the existing HealthSink pause-restore" — weaver.md L290) and for infra/structural auto-pause. Today only the **not-found** and **not-paused** legs are exercised: `Load` sits at **37.5%** and `pauseReasonFromString` is **0%** — the entire paused-restore path (reason mapping + `consumerState(true, reason)` seed + `StatusPaused` return) is **unpinned**. So a regression that mis-maps a reason (e.g. `manual`→`infra`, which changes whether an operator-issued disable is treated as self-healing vs. sticky) or drops the paused branch would **silently change pause semantics across a restart** — and nothing catches it. Pure functions + a tiny round-trip over the existing embedded-NATS/`jsstore.Dir` substrate fixture: `pauseReasonFromString` table (manual/structural/infra + unknown→infra default), and a `SetPaused`→`Load` restore for each reason plus the not-found→active and malformed-JSON→active fallbacks. Mirrors the recent Processor Health-KV / Loupe loopback / Refractor latency pins. Test-only; no behavior / key / contract change. Grounding: `internal/weaver/health_sink.go:52-117`, `docs/components/weaver.md:288-306`. Surveyor 2026-06-29; un-filed before. | ★★ | XS–S | 📋 |
| **[Loupe] Static-UI serving (`go:embed web`) untested** *(test-only)* | The embedded operator-UI mount — `mux.Handle("/", http.FileServer(http.FS(sub)))` over `//go:embed web` (`cmd/loupe/server.go:21-50`) — is **Loupe's entire reason-for-being** (it serves `index.html` / `app.js` / `style.css`), yet has **zero Go-level coverage**: the recent 0%→52% `server_test.go` pin deliberately tests only the `/api/*` data handlers against a nil-conn server and **never GETs the site root**. So a regression that breaks the embed sub-FS mount, mis-serves an asset, or drops a file ships unnoticed by the Loupe suite — the only signal is a manual browser load. A trivial `httptest`-against-`registerRoutes` pin — **no NATS conn / fixture needed** (static serving is conn-independent) — closes it: GET `/` → 200 + `text/html`; GET `/app.js` → 200 + a JS content-type + non-empty body; GET `/style.css` → 200; a missing asset → 404. Pins the embed wiring + the conn-free static surface; mirrors the existing nil-conn `server_test.go` style. Test-only; no behavior / contract change. Grounding: `cmd/loupe/server.go:21-50`, `cmd/loupe/server_test.go`. Surveyor 2026-06-29; un-filed before. | ★ | XS | 📋 |
| **[Loupe] Operator UI (`app.js`, 1142 LOC) has no automated coverage — no JS test harness in the repo** *(architectural — Andrew's call)* | Loupe's actual operator surface is **1142 lines of `cmd/loupe/web/app.js`** (~105 functions: the Core-KV browser, health / system-map dashboards, the control proxy, package install / uninstall, DDL-driven op submission, the binary upload / detach flows) — **entirely untested**, because the repo has **no JS test harness at all** (no `package.json` / jest / vitest; a `find … *.test.js` is empty). So every Loupe FE change (built by the `fe-engineer` role + Sally) is verified only by a manual in-browser pass — there is no regression net for the UI logic (e.g. the client `objectDisposition` mirror, op-payload assembly, the reclaim-on-reject UX). **This is an architectural-direction question, not a ready build item:** whether a Go-only repo should adopt a JS test toolchain (and which — vitest/jsdom for the pure helpers, or a Playwright smoke against `make run-loupe`) is **Andrew's call**; the Surveyor flags it and it should **not** be designed / built until Andrew rules. The same question recurs for each **vertical** FE (LoftSpace / Clinic), so the decision is **cross-cutting**, not Loupe-only. If adopted, the first slice is the pure, conn-free helpers (content-disposition / XSS guard, key-shape formatting, payload builders), mirroring how the Go side pinned its pure boundaries first. Grounding: `cmd/loupe/web/app.js` (1142 LOC, ~105 fns), the absent JS toolchain, the `fe-engineer` / Sally FE-build loop with no test gate. Surveyor 2026-06-29; un-filed before. | ★★ | L | 🔭 **flag-for-Andrew (architectural direction — adopt a JS test harness for the operator + vertical FEs? not to be designed / built until ruled on)** |

### Survey log (round-robin rotation)

_The Surveyor notes each run here so the next run rotates to the least-recently-surveyed component._

- **2026-06-26 — Refractor** (`internal/refractor` + `cmd/refractor` + `docs/components/refractor.md`).
  Substrate migration **verified clean** — raw `nats.go`/`jetstream` confined to `control/service.go` (the
  accepted `micro.Service` exception); the residual `cmd/refractor` `js.CreateKeyValue` is Andrew-adjudicated
  (provisioning belongs to bootstrap, no `substrate.EnsureKV`). Codebase debt near-zero (no real TODO/FIXME;
  active de-flaking — 489c64a, 715b14b). Filed 3 grounded items above; remaining Refractor deferreds
  (link-tombstone re-projection, cross-instance latency rollup, Elasticsearch adapter, negative/retraction
  projection, lens-target write restriction) already tracked in the Deferred backlog. **Next rotation:** Core
  (`internal/processor`/`bootstrap`/`substrate`) — the next-stalest un-surveyed component (Loupe's lane is
  already saturated).
- **2026-06-26 — Core** (`internal/processor` + `internal/bootstrap` + `internal/substrate`; stalest by
  `git log` — processor last touched ~5d before Weaver). **No real TODO/FIXME** in any of the three packages
  (clean). The `substrate/CONTRACT-AMENDMENT-REQUEST.md` is RESOLVED (2026-05-13) but flagged one un-done clean
  follow-up (bootstrap→substrate key helpers — filed). The `processor.md` "What's deferred" items (read-path
  auth, multi-cell, NATS account auth, multi-aspect OCC) all already live in the feature backlog / parking lot
  — not re-filed. Filed **2 grounded items**: the high-value Processor health-honesty completion (fabricated
  `lane_lag` / no anomaly channel — finishes the Refractor+Weaver sweep, ★★★) and the low bootstrap key-helper
  hygiene (★). Neither needs a frozen-contract change. **Next rotation:** Loom (`internal/loom`) — the
  next-stalest non-Core component (Refractor/Loupe/Weaver are all fresher and recently surveyed/built).
- **2026-06-27 — Weaver** (`internal/weaver` + `internal/weaver/control` + `docs/components/weaver.md`).
  Mature + clean: **no TODO/FIXME**; already **substrate-clean** (raw `nats.go`/`jetstream` confined to
  `control/service.go`, the accepted `micro.Service` exception — the Refractor-style migration is a non-gap
  here); engine coverage **82.4%**, control **66.3%** (the one soft spot — folded into the capability-authz item
  rather than filed standalone). All four health self-reporters now consistent (Weaver's own `aggregateStatus`
  shipped 4de7677). Existing Weaver deferreds already on the board: **L3 evaluator** (designed, awaiting Andrew),
  **Lane 2 / `weaver-work`** (§10.3 Phase 3), op-vertex pruner (#47/#49). Filed **2 grounded items**: the
  **reclaim check-before-act probe** (closes the §10.3 documented rare-double for non-idempotent `assignTask`/
  `triggerLoom`, ★★, Component-maintenance) and the **control-plane Capability authorization** (Weaver+Refractor
  both stub allow-all; security-plane, intersects D1/Gateway, ★★, filed under Security & trust boundary). Neither
  needs a frozen-contract change; both need design. **Next rotation:** Loupe (`cmd/loupe`) — next-stalest, or roll
  to Loom if the Loupe lane stays saturated (substrate/processor/bootstrap = Core, surveyed 06-26).
- **2026-06-27 — Loom** (`internal/loom` + `internal/loom/control` + `docs/components/loom.md`; the stalest
  un-surveyed component — 2nd-stalest by git after Core/bootstrap, which was surveyed 06-26). **Mature + clean:**
  no TODO/FIXME; **substrate-clean** (the `actuator.go` "nats.go/jetstream" grep hit is a *comment*, not an import;
  the only raw usage is `control/service.go`'s `micro.Service` responder — the accepted exception). Engine coverage
  **79.6%**, control **76.6%**. The control-plane **security boundary is already covered** (`TestControl_CapabilityDenied`
  asserts a denied op never reaches the engine — `assert.Empty(eng.callLog())`), unlike the just-shipped Weaver gap —
  so **no control-coverage item is warranted** (the residual `nameFromSubject`/`respondMicro` gaps are unreachable-via-NATS
  defensive branches). Existing Loom deferreds already on the board: **Starlark guards** (feature backlog, ★), the
  **durable `loom.*` read-model lens** (folded into "Loom/Weaver control-API surfacing"), and the **control-plane
  Capability authz** stub (the `StubCapabilityChecker` — already folded into the cross-component "Control-plane Capability
  authorization (FR30)" row, which names Loom explicitly — not re-filed). Filed **2 grounded items**: the doc-flagged
  **guardless-step recovery check-before-act probe** (Phase-3 hardening, loom.md ~line 139, ★ — the lower-stakes sibling
  of the Weaver reclaim probe) and a small **redelivery-dedup / op-meta-deregister coverage** pin (★, test-only). Neither
  needs a frozen-contract change. **Next rotation:** Loupe (`cmd/loupe`) — the least-recently-*surveyed* component (no
  dedicated survey entry; fresher than Core/bootstrap, surveyed 06-26).
- **2026-06-27 — Loupe** (`cmd/loupe` + web; the **least-recently-surveyed** component — no prior dedicated survey
  entry, flagged as "next" by the last four rotations). **Clean code** — no TODO/FIXME; security-critical pure
  boundaries mostly pinned (the `objectDisposition` anti-XSS pin just shipped fd0cacd). Two real gaps surfaced.
  **(1) Doc:** Loupe is the **only** component with **no `docs/components/*.md`** (`ls docs/components/` — all six
  others have one) — the 06-19 docs-refresh predated Loupe's build, so its mandate / capabilities / trusted-no-auth
  posture / P5-inspector-exception / loopback-bind rule live only in code + the backlog. **(2) Coverage:** Loupe is
  the **lowest-covered component (41.9%** vs. 76–82%) — the pure helpers are tested but the entire **`server.go`
  HTTP layer is at 0%** (conn-guard, error/status paths, the objects method-router), and the **security-relevant
  loopback-bind guard** (`isLoopbackHost`/`warnIfNonLoopback`, a no-auth admin tool's exposure check) is untested.
  Filed **3 grounded items** (Component maintenance): the component doc (★★/S), the loopback-guard pin (★★/XS,
  test-only, the high-value quick win), and the broader `server.go` httptest coverage (★/M, test-only). None needs
  a frozen-contract change. Existing Loupe lane items not re-filed: the **agent-activity console** (🚧 Andrew-shelved)
  and the shipped system-map/health-status work. **Next rotation:** back to the stalest **un-surveyed-since** —
  Refractor/Core were 06-26, so Loupe→Weaver→Loom were the recent sweep; **Refractor** (06-26, now stalest by survey
  date) is the next pick, or roll to the **cross-cutting feature backlog** if the components stay healthy.
- **2026-06-28 — Cross-cutting feature backlog** (the Phase-3 build queue; the first *non-component* focus — all five
  components were surveyed 06-26/06-27 and reported **mature + clean**, so per the prior rotation's "roll to the feature
  backlog if components stay healthy" this was the genuinely least-recently-surveyed *focus*). Ground: cross-checked every
  documented "Phase 3 / deferred / future" marker in `docs/components/*.md` against the board — **all but one map to an
  already-filed item** (most already designed + Andrew-ratified): bridge real-adapters→External-I/O maturity, `@every`
  schedules→op-vertex-pruner row, durable `loom.*` lens→control-API-surfacing, multi-cell/HA→Scale-out, the four health
  self-reporters→**done** (the health-honesty sweep closed), substrate inner-package migration→shipped (stale doc).
  **One genuine un-filed gap found + filed** (Security & trust boundary): **lane authorization enforcement** — `LaneUnauthorized`
  is *defined* (`envelope.go:72`) + contract-mandated (§2.3: "rejected at step 3 before any further processing") + conformance-
  asserted, but **never emitted** — `step3_auth.go` never reads `env.Lane` vs the populated-but-unread `capability_doc.Lanes`,
  so any actor can submit to any lane (`meta`/`urgent`/`system`); pairs with the service-actor `system`-lane grant
  (`service-actors.md:74`, "tracked so it is not lost") and the migration ordering. ★★/M, no frozen-contract change (build-to
  §2.3), needs design. The feature backlog is otherwise **saturated** — no noise filed. **Next rotation:** back to **Refractor**
  (`internal/refractor`, now stalest component by survey date — 06-26) per the prior note, then resume the component round-robin.
- **2026-06-28 — Refractor** (`internal/refractor` + `cmd/refractor` + `docs/components/refractor.md`; the stalest
  component by survey date — 06-26 — and the **freshest by code**, so recent work to survey: the anchor-tombstone
  retraction `679fe25`, Postgres `Truncater` `f537f6b`, Capability-Lens liveness alert `ddff846` all landed since).
  **Mature + clean:** the only TODO/FIXME hits are in generated ANTLR lexer code (`cypher_lexer.go` — not real debt);
  substrate-clean. Coverage healthy (65-100%) **except `internal/refractor/pipeline` at 44.5%** (the lowest in
  Refractor) — concentrated 0% clusters in `latency.go` (the whole `LatencyRingBuffer`) + `evaluate.go` fan-out paths.
  Filed **3 grounded items** (Component maintenance): (1) **doc-drift** — refractor.md's "Loupe doesn't read
  status/issues" residual is **stale** (Loupe `componentLiveness` shipped it `2877a1c`, 06-26), ★/XS pure-doc; (2)
  **Capability-Lens lag-alert hysteresis** — single-cycle `ConsumerLag > threshold` flaps the heartbeat on a one-cycle
  spike, **un-filed** + doc-flagged future work (`refractor.md:321`, `lattice_heartbeater.go:275`), ★/S needs-light-design;
  (3) **`LatencyRingBuffer` percentile pin** — the NFR-P3 "primary instrument" is 0%-covered, ★/XS-S test-only. None needs
  a frozen-contract change. Existing Refractor deferreds **not re-filed** (already on the board): Negative/filter-retraction
  projection (L227 — the general WHERE-flip case the just-shipped anchor-tombstone fix deferred to), link-tombstone
  re-projection + cross-instance latency rollup (L228), Personal/Secure Lens + substrate write-restriction (both
  Andrew-ratified, build-pending behind D1 / NATS write-restriction Fire 2). **Next rotation:** **Core**
  (`internal/processor`/`bootstrap`/`substrate`, surveyed 06-26 alongside Refractor — now the stalest by survey date),
  then resume the component round-robin (Weaver/Loom/Loupe were 06-27, feature-backlog 06-28).
- **2026-06-28 — Core** (`internal/processor` + `internal/bootstrap` + `internal/substrate`; stalest by **survey
  date** — 06-26 — and the **freshest by code**: the per-lane-consumer Fires landed since — Fire 1 `c16f739`, Fire 2
  `c5e1fc2`, plus `f0d7980` UpgradePackage, `75e9acc` NKey/creds seam). **Mature + clean:** **zero** TODO/FIXME across
  all three packages; coverage healthy — processor **79.4%**, outbox **78.1%**, substrate **73.6%**, bootstrap **60.2%**
  (the floor, but bootstrap is e2e-covered — the primordial seed is exercised by the full-graph seeders, so its low
  *unit* coverage is expected, not a ready gap; **not filed**). The per-lane-consumer roadmap (**Fire 3** per-lane
  concurrency-from-config → **Fire 4** Processor control plane) is **already on the board** (the [Core] per-lane-consumers
  row) — not re-filed. Substrate's 0% functions (`PendingForConsumer`, `KVPutWithTTL`, `KVDeleteRevision`,
  `DeleteStreamConsumer`'s sibling KV seams, `PruneStaleDurables`) are thin **NATS-integration** wrappers (the
  *consumer-side* per-lane lane_lag IS covered via `fakeBacklogReader` in `health_internal_test.go`) — low-value to pin
  standalone, **not filed**. Filed **1 grounded item** (Component maintenance): the **Processor Health-KV emission paths
  0%-covered** (★★/S, test-only) — `health_alerts.go` (security-alert + claim-attempt-counter emitters, both **live-wired**
  in `commit_path.go`) + `health.go emitCapabilityAuthSignals` are entirely untested, i.e. the **Lamplighter's input
  channel off the sole Core-KV writer** is unpinned. No frozen-contract change. **Next rotation:** **Weaver**
  (`internal/weaver`, surveyed 06-27 — now the stalest component by survey date alongside Loom/Loupe), then resume the
  component round-robin (Refractor was 06-28, Core 06-28, feature-backlog 06-28).
- **2026-06-29 — Weaver** (`internal/weaver` + `internal/weaver/control` + `docs/components/weaver.md`; the
  stalest by **both** survey date — 06-27 — **and** code, oldest commit ts of the five components, the
  rotation the 06-28 Core run named next). **Mature + clean:** **zero** TODO/FIXME; coverage **82.5%**
  (control **77.2%**), both healthy; substrate-clean (raw `nats.go`/`jetstream` confined to
  `control/service.go`'s accepted `micro.Service` exception). The only 0% functions are trivial
  (`strategist.go:54` an error type's `Error()`; control `respondMicro` 27.3% = the unreachable-via-NATS
  defensive branch — both **not filed**, per the Loom-survey precedent). All Weaver deferreds are
  **already on the board**: **Augur** (L3, designed + ratified), **Lane 2 / `weaver-work`** (Phase 3),
  control-plane Capability authz (FR30, designed), Negative/filter-retraction projection, op-vertex pruner
  (#47/#49) — none re-filed. The just-shipped reclaim-backoff (`04c7689`) carries an Andrew-flagged mark-TTL
  deviation already captured in its row. weaver.md is **current** (the backoff is described in the
  Implementation-status reclaim row). Filed **1 grounded item** (Component maintenance): the **HealthSink
  pause-restore round-trip** (`Load` paused branch 37.5% + `pauseReasonFromString` 0%) — the restart-survival
  path for the FR30 control-plane `disable` + auto-pause is unpinned (★★/XS–S, test-only). No frozen-contract
  change. **Next rotation:** **Loupe** (`cmd/loupe`, surveyed 06-27 — now alongside Loom the stalest by
  survey date), then resume the component round-robin (Refractor/Core/feature-backlog were 06-28, Weaver
  06-29).
- **2026-06-29 — Loupe** (`cmd/loupe` + `web/`; the rotation the 06-29 Weaver run named next — tied-stalest by
  survey date with Loom at 06-27). **Mature + well-covered:** zero TODO/FIXME; **all three 06-27 survey items
  shipped** (the `loupe.md` component doc, the loopback-guard pin `75db1ae`, the `server.go` HTTP-layer pin
  `c318ee5`); package coverage **41.9% → 52.1%**; the big-ticket deferreds (D1 read-path auth / Gateway /
  Personal Lens) are all on the board + **Andrew-ratified (06-27)**, build-pending. The remaining low-coverage Go
  handlers (`handleObjectUpload` 4.8% / `handleObjectGet`+`Detach` ~8% / the `keyPresent`/`keyState`/
  `objectStoreName` thin KV wrappers 0%) are **live-conn integration paths already covered end-to-end by
  `make test-object-gc`** (the object-plane Loop A+B convergence e2e) — pinning them at the package level would
  largely duplicate that, so **not filed** (per the Core thin-NATS-wrapper precedent). Filed **2 grounded,
  genuinely un-filed items** — pivoting off the saturated Go-coverage-pin cadence to the **under-surveyed FE
  dimension**: (1) the **`go:embed web` static-serving root is untested** at the Go level (`server_test.go` tests
  only `/api/*`; serving the UI — Loupe's whole reason-for-being — is 0%), ★/XS test-only, conn-free; (2) the
  **1142-line operator UI (`app.js`) has zero automated coverage + the repo has no JS test harness at all** —
  flagged **🔭 for Andrew** as an architectural direction (adopt a JS toolchain for the operator + vertical FEs?),
  cross-cutting (recurs for LoftSpace/Clinic), **not to be designed/built until ruled on**. Neither needs a
  frozen-contract change; (2) is a direction call, not a build. **Next rotation:** **Loom** (`internal/loom`,
  surveyed 06-27 — now the stalest component by survey date, the tied sibling Loupe just cleared), then resume the
  component round-robin (Refractor/Core/feature-backlog 06-28, Weaver/Loupe 06-29).

---


## Lattice feature backlog — the Phase-3 build queue

> Formerly "Deferred (Phase 3+)". These are **no longer deferred** — the AI-driven flywheel **actively draws
> from this list**: the **Surveyor** files + scores demand here, the **Designer** (`lattice-designer`) turns the
> items into design docs **flagged for Andrew to ratify**, and the **Lattice Steward** builds the ratified ones.
> **Almost everything below needs design and is fair game** (importance-ordered, ambition encouraged) — the
> **only** exclusion is a row marked **🚧 Andrew-gated** (a standing block/shelve — currently just the one
> shelved Loupe agent-activity console). Architectural **forks** (Gateway, read-path auth, Vault, multi-cell,
> HA-NATS) and **frozen-contract** changes are still **designed through** — the Designer preps the design + the
> uncommitted contract edit and explains the fork — but the *fork decision* and the *contract commit* are
> Andrew's (see the cross-lane rules in [../backlog.md](../backlog.md)).

### Security & trust boundary
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| Read-path authorization (D1) | Reads from lens targets (Postgres / KV) bypass the write-path Capability boundary. Rubric in `lattice-architecture.md` D1: Postgres RLS + a Capability-Read Lens; Gateway sets `lattice.actor_id`. Subsumes `cap.svc` serviceAccess read-auth. | ★★★ | L | ✅ **Andrew-ratified (2026-06-27)** — design: [`read-path-authorization-d1-design.md`](../../implementation-artifacts/read-path-authorization-d1-design.md). **Forks resolved:** (1) **Path A (Postgres-RLS) is *the* enforcement boundary** for protected data; the NATS-KV read-gateway filter is **transitional only** — post-RLS, a `protected:true` read model on NATS-KV is **lint-failable** (the gate extends to "must target Postgres"); (2) **minimal JWT read-actor seam = D1 increment 1**, full Gateway deferred to ops. **The Capability-Read lens is *decomposed* (per §6.1's contract-contribution model), not a god-cypher:** core owns a base `cap-read.<actor>` lens (self/primordial); each **package** ships its own `cap-read.<domain>` read-grant lens (rbac→roles, loftspace→residence, clinic→…); the **union** merges in the Postgres `actor_read_grants(actor_id,anchor_type,anchor_id,grant_source)` table (RLS JOINs union; `grant_source` keeps producers disjoint). Contract **§6.14 revised + ratified + COMMITTED** (incl. the §8 fold: protected-by-default + `authzAnchors[]` set + grant-table seq-guard + FORCE-RLS). D1.1 base+package lenses → D1.2 JWT seam → D1.3 Postgres-RLS enforcement → D1.4 Gate-3 read-bypass + the protected⇒Postgres lint → D1.5 roll remaining protected read models. **✅ §8 pre-build adversarial pass RUN (2026-06-28, `bmad-party-mode`: Winston + adversarial-security + Quinn + Mary) — gate DISCHARGED.** Findings in design §8 RESULTS. **5 HIGH (fold before D1.1):** **H1** invert the default — protected-by-default + explicit `public:true` (§3.2 was **default-OPEN** — a forgotten `authzAnchor` silently world-published a protected model; mirror the write path's deny-on-absence) [§6.14 edit]; **H2** move the conventions lint INTO D1.3, not D1.4 (don't enforce before the gate exists); **H3** `ENABLE`+`FORCE ROW LEVEL SECURITY` on protected tables (missing policy ⇒ deny-all, not serve-all); **H4** seq-guard the `actor_read_grants` projection — the §3.3 Postgres guard-exemption can **resurrect a revoked grant** (delete-then-stale-upsert); **H5** `authzAnchors` is a **set** + coarse/hierarchical anchors (building-manager/provider scope) [§6.14 edit]. **2 Andrew-decisions:** **M3** accept CDC-lag per-resource revocation as the D1 posture (vector-clock fence = v2)? *(rec: accept + document the bound)*; **M5** Loupe all-access = wildcard *grant* inside RLS (attributable), not an RLS bypass *(rec: grant)*. Plus M1 source-scoped retraction, M2 uninstall-retracts-grants, M4 column-sensitivity = Vault plane (not D1), M6 per-request revocation check, **M7 write-restriction Fire 2 IS a genuine D1 prereq** (unforgeable grant-source, sharper reason — validates the sequencing). **✅ FOLDED + §6.14 COMMITTED (Andrew green-lit 2026-06-28).** H1–H5 folded (design §3.2/§3.3/§3.4 fold-notes + §8 RESULTS); **H2 moot** (H1+H3 fail-closed structurally — no lint reordering, nothing slowed); **M3** accept CDC-lag per-resource revocation (vector-clock fence = v2); **M5** Loupe = wildcard grant inside RLS, not a bypass. **🎯 D1.1 BUILD-READY** — Steward builds D1.1→D1.2 in parallel with NATS write-restriction Fire 2 (M7: unforgeable grant-source); D1.3 RLS gated on a Postgres read-model landing. **🏗️ D1.1 SHIPPED (43a476a, CI watch in progress) — base `cap-read.<actor>` core lens.** The read-path mirror's *auth source of truth* now exists as a primordial auth-plane `actorAggregate` lens (`bootstrap.CapabilityReadLensDefinition`, seeded primordial step 5b, verified): for **every** identity it projects the **self** anchor `{anchorType:'identity', anchorId:<full vtx key>, via:['self']}` to `cap-read.<actor>` in the `capability-kv` bucket (disjoint key space; §6.1-style decomposition). **Pure projection — ZERO enforcement** (D1.1 lands the source first). Auth-plane **by construction** (`projection.IsAuthPlane` keys off `bucket==capability-kv`), so it inherits §6.13 fail-closed activation + the guarded soft-tombstone + the §6.2 `projectionSeq` guard **verbatim** — no hand-rolled security path. Unlike the write base (protected identities only), the read base projects self for **every** actor (self-read is the universal, package-independent primordial grant). New primordial meta-vertex wired through `nanoid.go` (a stale bootstrap file fails loud via `IsValidNanoID`). §6.14 byte-shape conformance test runs the LITERAL cypher through the §6.13 envelope. Gates green (build/vet/golangci 0/STRICT-conventions 0; bootstrap full-graph seeders + refractor capability e2e **live-activate** the new lens; processor). **Thorough-lead review** (explicitly scaled down from 3-layer: zero enforcement / zero authz-decision-path change, pure projection no consumer, near-verbatim mirror of the heavily-reviewed write base, security inheritances structural-via-bucket). **One flagged deviation → uncommitted §6.14 representation clarification (for Andrew):** `anchorId` carries the **full vertex key** (covered-cypher reality — no `split`/`substring` in the auth-plane engine; consistent with §6.5 `serviceAccess.service`); the §6.14 example's bare-NanoID is illustrative; enforcement uses the same full-key rep on both sides of the membership join. **🏗️ D1.2 SHIPPED (136f49c, CI green) — read-actor authentication seam.** `internal/gateway/auth` verifies an IdP-signed JWT → actor (full vertex key `vtx.identity.<sub>`, consistent with §6.14 + the write-path actor) and `internal/gateway/revocation` is the per-request kill-switch over the `token-revocation` bucket; an `Authenticator` composes them and fails closed. **Ships dark — zero enforcement, no live consumer** (D1.3 wires it into the first protected read boundary). The read analog of write-path `Lattice-Actor` stamping: it *authenticates*, it does **not** filter rows. **Security posture enforced + tested** against the JWT attack surface — asymmetric verify only (RS*/ES*; **HS\* and `none` refused** before any key is consulted, via `jwt.WithValidMethods` + a keyfunc method re-assertion — closes alg-confusion + alg-none); `kid` selects the trusted key with **no implicit single-key fallback**; `exp` required (unbounded tokens rejected) + skew-bounded, `nbf`/`iat` likewise; `sub` required. Per-actor revocation (revoking an actor cuts off every token), checked every read (short JWT TTL = the CDC-lag backstop; vector-clock fence = deferred v2/M3). `golang-jwt/jwt/v5` added as a direct dep (recorded in `docs/vendors.md`). **No frozen-contract change** (§6.14 committed). Coverage auth 94.6% / revocation 100%. **Lead+adversarial review** (explicitly scaled from full 3-layer: dark seam, zero enforcement/consumer, built on golang-jwt's secure-by-default verify; **D1.3's enforcement turn-on gets the full 3-layer**). **Lead impl decisions (mine):** asymmetric-only allow-list, full-vertex-key actor id, per-actor (not per-`jti`) revocation, golang-jwt/jwt/v5 — all within design §3.4/§7.2, no open questions. **Remaining (🏗️ multi-fire checkpoint):** **D1.1b** primordial root-read/all-access anchor for kernel identities (needs the wildcard-anchor rep the enforcement seam defines — M5) + the **package** read-grant lenses (`cap-read.roles` rbac-domain, `cap-read.residence` loftspace — *verticals/package work*); **D1.3** Postgres-RLS enforcement (gated on a Postgres read-model; wires the D1.2 `Authenticator` into the read boundary + `SET LOCAL lattice.actor_id`); **D1.4** Gate-3 read-bypass + protected⇒Postgres lint; **D1.5** roll remaining protected read models. **🏗️ D1.4 (protected⇒Postgres half) SHIPPED (7e54953, CI watch in progress) — the fail-closed activation guard.** `translateSpec` now rejects a `protected: true` read model — and a `grantTable` lens — declared on a `nats_kv` target (Contract #6 §6.14 / D1 §3.3: "a protected read model served from NATS-KV is forbidden / lint-failable"). The concrete leak closed: `TargetNATSKVConfig` did **not** parse the read-path-authz flags, so an author's `protected:true` on a nats_kv lens was **silently dropped** by `json.Unmarshal` and the model **activated world-readable** — exactly the H1 silent-world-publish failure mode. The fields are now parsed + the lens fails closed at activation (never projects). Enforcement lives at `translateSpec` (the single conversion every spec-authored lens passes through) — **not** a static `lint-conventions.go` gate — because lens specs are **runtime Starlark** a source-lint can't see; this is the complete enforcement point and where the existing posture guards (protected-XOR-public, protected-⇏-grantTable) already live. **Zero blast radius** (no lens declares the read-path posture today; a plain neither-flag nats_kv read model — weaver-targets / listings index — is the deliberately-valid existing path, untouched, pinned by `TestTranslateSpec_PlainNATSKV_Unaffected`). The **protected-by-default deny** for neither-flag *postgres* business models stays a **read-boundary** concern (Fire 1b deliberately keeps neither-flag postgres = the valid plain path, asserted by `TestTranslateSpec_NonProtected_NoProvisioning` — a translateSpec rejection there would contradict shipped design). **Remaining D1.4:** (a) the **protected ⇒ projects-authzAnchor** guard — deferred (needs cypher-RETURN column introspection; `CompiledRule` exposes only `EngineName()`, and RLS already fails closed deny-all on a missing anchor, so it's a dev-experience nicety not a leak); (b) the **Gate-3 read-bypass adversarial suite** (§5 vectors 1-5) — gated on a live protected read model (Verticals-stream D1.3 Fires 2-3). No frozen-contract change (build-to §6.14). Thorough-lead review w/ adversarial self-pass (explicitly scaled from 3-layer: XS, additive, fail-closed, zero behavior change for existing lenses, at the heavily-reviewed Fire-1a/1b chokepoint — overridable). Gates: build/`make vet`/golangci 0/STRICT-conventions 0; refractor lens+adapter+cmd `go test` green (Gate 2/3 + verify-kernel deferred to CI — orthogonal to read-path lens activation + local shared-stack collision). |
| Gateway | Edge trust boundary: JWT auth, `Lattice-Actor` stamping, read-path enforcement. Gates external actors + the real Edge node. | ★★★ | L | 📐 **awaiting-Andrew (ratification) — 2026-06-29** — design: [`gateway-external-trust-boundary-design.md`](../../implementation-artifacts/gateway-external-trust-boundary-design.md). The read-actor **verifier** seam (JWT→actor, token-revocation) is **built** (D1.2 `internal/gateway/auth` + `revocation`); this design fills the one undesigned gap — the missing **HTTP→NATS translator** (`internal/gateway/gateway.go`, confirmed absent) that **verifies-and-stamps** `env.Actor`, making it **unforgeable for external actors** (today it's a self-asserted JSON field — `envelope.go:54`; anyone who can publish `core-operations` claims any actor). The *authentication* layer that closes actor impersonation — complement to ratified #75 (transport-authZ) + Capability KV (actor-authZ). **No frozen-contract change** (build-to #2 §2.39 — already *reserves* "Gateway translates to full key before envelope submission" — + #6/#9/#5; Contract #11 JWT-format freeze deferred to a 2nd consumer, NOT staged). **Builds to the ratified architecture "Gateway Architecture Decision"** (reverse-proxy infra + thin Go translator). **4 forks designed through (recs given): F2 actor-trust = Gateway-stamp+transport-trust [rec A] vs Processor-verifies-JWT (#118) [B]** — the core call; **F3 IdP = external IdP + dev signer [rec A]** (auth.go: Lattice never signs); **F4 read transport = extend the translator [rec A]** behind D1.3; **F5 = build Fire 1 now [rec A], with/after #75 Fire 2** (the hard sequencing gate A's trust rests on). 5 fires (Fire 1 write-path translator = buildable now, full 3-layer → Fire 2 dev signer+JWKS → Fire 3 read-front behind D1.3 → Fire 4 claim-flow front → Fire 5 NGINX hardening = ops). Self-adversarial pass folded in (§9; A1 #75-sequencing gate, A2 overwrite-not-merge stamping, A3 authContext-not-an-escalation). **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds Fire 1, with/after #75 Fire 2).** |
| NATS account-level write restriction | Close the fabricated-KV-write surface at the substrate level (today defended only by overwrite-by-reprojection). | ★★ | M | ✅ **Andrew-ratified (2026-06-27)** — design: [`nats-account-write-restriction-design.md`](../../implementation-artifacts/nats-account-write-restriction-design.md). The **transport-authorization** complement to authN (brainstorm #75): per-component NATS users with scoped publish permissions so **only the Processor's connection may write `$KV.core-kv.>`** (and only Refractor writes the lens/auth buckets) — closing **bypass #1** properly (today only "undetectable-without-EventList") and turning the Gate-3 reprojection defense into denied-at-the-door. **No frozen-contract change.** **Fork resolved: Path A** (static-conf NKey users) now, **Path B** (decentralized operator/JWT) as the Edge/multi-tenant evolution — confirmed vs the official NATS auth docs. Doc-check fold-in: **`allow_responses`** for the control-plane responders (else the control plane dies under enforcement, §3.4). **🏗️ Fire 1 SHIPPED (75e9acc)** — the substrate **credential seam**: `ConnectOpts.NKeySeedFile`/`CredsFile` mapped in `Connect` to `nats.NkeyOptionFromSeed`/`nats.UserCredentials`, **dark + no-op by default** (both empty ⇒ today's anonymous connect, the compatibility hinge keeping the embedded harness + every call site unchanged); fail-closed (bad seed / missing creds / both-set all error *before* the dial, never silent-degrade to anonymous); end-to-end test against an NKey-authed embedded server (matching seed authenticates + KV round-trips, anonymous rejected). No frozen-contract change; no binary/conf touched (zero stack risk). Thorough-lead-reviewed (scaled down from 3-layer *explicitly + overridably*: this increment ships **no enforcement** — the real attack surface lands in Fire 2). **🏗️ Fire 2 increment 1 SHIPPED (2e10ba7, CI watch in progress) — the permission-matrix artifact + offline conformance proof (NO live enforcement).** Fire 2's full turn-on is *atomic* (conf + every binary's creds + the gate flips must land together, else a half-authenticated stack) and heavily touches the shared running stack + the verticals stream's e2e harness — not safe to rush in a single bounded parallel fire. So increment 1 ships the durable artifacts + proves the **highest risk (§8.1 over-restriction)** offline, touching neither `docker-compose` nor the live stack: **`deploy/gen-dev-nkeys`** (the matrix source of truth — renders the per-component dev NKey seeds `deploy/nkeys/*.nk`, dev-only like `POSTGRES_PASSWORD: lattice_dev`, + the rendered **`deploy/nats-server.conf`** with the full §3.2 Path-A matrix incl. `allow_responses` for the loom/weaver/refractor/bridge responders); **`internal/natsperm`** loads the *real* committed conf + seeds into an embedded JetStream server and proves the load-bearing invariant end-to-end — **processor↔core-kv, refractor↔capability-kv/lens, every non-owner denied** (differential: the owner write succeeds on the same bucket the rogues fail on ⇒ denial is permission-based), plus the **backing-stream side-channel** (`$JS.API.STREAM.{CREATE,UPDATE,DELETE,PURGE,MSG.DELETE}.KV_<protected>` denied for non-owners). **Grounding correction flagged for Andrew:** health is written to the `health-kv` **KV** bucket, so the publish subject is **`$KV.health-kv.>`** — *not* the bare `health.>` the design prose (§3.2) abbreviated; the conf encodes the correct subject. Gates green (build/vet/golangci 0/STRICT-conventions 0/`-race`; conformance test ~10s). Thorough-lead + adversarial-self-review, explicitly scaled from 3-layer (zero live enforcement — a deploy artifact + offline test). **Remaining — Fire 2b (the enforcement turn-on, atomic):** thread `NATS_NKEY`/`NATS_CREDS` env through every production binary's `main.go` (§3.3), mount the committed `deploy/nats-server.conf` in `docker-compose.yml`, hand each binary its committed `deploy/nkeys/<comp>.nk` via env in `make up`/`up-full`/the package/CLI/app run targets, verify the `lattice-pkg` + object-store + Loupe-upload writer paths against the live stack (§3.4), and run the full `make up-full` battery **with auth on**. **Fire 3:** flip Gate 2 (`bypass_direct_kv_test.go` → transport denial) + Gate 3 (`capadv_direct_kv_write_test.go` → denied-at-door) + add a live `verify-nats-permissions` conformance gate to the stack-gates CI job. Fire 2b/3 are L+ / enforcement ⇒ **full 3-layer adversarial review**. |
| **Lane authorization enforcement (Contract #2 §2.3) — `LaneUnauthorized` + the service-actor `system`-lane grant** | §2.3 mandates that submitting to a lane is itself capability-controlled and an unauthorized-lane submission is *"rejected at commit step 3 (auth check) before any further processing"* — but this is **entirely unenforced today.** `ErrCodeLaneUnauthorized` is **defined** (`internal/processor/envelope.go:72`), reserved in the contract (§2.3 + the error table at `02-operation-envelope.md:208`), and asserted in `conformance_test.go` — yet **step 3 never reads `env.Lane`** against the actor's `Lanes` capability: `capability_doc.Lanes` (`capability_doc.go:30`) is *populated* (every projection hardcodes `["default"]`) but **never consulted** in `step3_auth.go` (the only mentions of `LaneUnauthorized`/`lane` there are a comment + a log field — no check, no emit). Net: **any actor can submit to any lane** (`meta`/`urgent`/`system`) regardless of grant. Two coupled faces: **(1) enforcement** — step 3 checks `env.Lane ∈ actor.Lanes` and emits `LaneUnauthorized` on a miss (§2.3 grant rubric: `meta`→operator/admin, `urgent`→explicit grant, `system`→internal service actors); **(2) the service-actor `system`-lane grant** — Loom/Weaver/Bridge post result/dispatch ops on `ops.system.>` but their capability projections all hardcode `["default"]` (`service-actors.md:74`, flagged *"tracked here so it is not lost"*), so flipping enforcement on **without first granting them `system` would break the engines** — the grant must land *before* the check. **Distinct from** the per-lane-consumers item above (that's lane *routing/throughput/lag*; this is lane *authorization*) and from NATS account write-restriction (transport-layer authZ, a different plane). **No frozen-contract change** — §2.3 already fully specifies the behavior; this is *build-to*, not change. **Needs design:** the per-lane capability-grant projection (which roles/permissions confer which lanes; where the service-actor `system` grant is seeded in bootstrap), the **migration ordering** (grants land, then enforcement turns on), and a **Gate-3 adversarial vector** (an actor submitting to an ungranted lane → BLOCKED, asserting the `LaneUnauthorized` code). Sequence near/with NATS account write-restriction (Fire 2) — both are the "authorization actually denies" turn-on. Grounding: `internal/processor/step3_auth.go`, `internal/processor/envelope.go:72`, `internal/processor/capability_doc.go:30`, `docs/contracts/02-operation-envelope.md` §2.3 + L208, `docs/components/service-actors.md:74`. Filed 2026-06-28 (Surveyor — feature-backlog survey; grounded: `LaneUnauthorized` defined-but-never-emitted). | ★★ | M | 📐 **awaiting-Andrew (ratification)** — design: [`lane-authorization-enforcement-design.md`](../../implementation-artifacts/lane-authorization-enforcement-design.md). **No frozen-contract change** (build-to §2.3 + §2.6 `LaneUnauthorized`); **no architectural fork** (application-layer step-3 authZ; the sibling of NATS write-restriction's transport plane). **Shape:** a step-3 lane gate — the actor's *standing* capability doc is the lane authority; **platform path** checks `env.Lane ∈ doc.lanes` (read from the doc it already fetches — the one-GET hot-path invariant preserved), **service/task paths** confer the `default` lane only (implicit, no extra read). Grants ride the existing Capability-KV lenses: core `cap.<actor>` → `["default","meta","urgent","system"]` (was `["default"]`; the documented service-actor `system` grant), rbac `cap.roles.<actor>` → static baseline `["default"]` (was absent). **Decision (Andrew, 2026-06-28): implicit-default** — service/task paths confer `default` only (no projected per-path grant, no extra KV read; the recommended rule). **Migration is order-dependent:** grants converge (Fire 1, dark, zero behavior change) **before** enforcement (Fire 2 — gate + Gate-3 ungranted-lane vector + e2e + retire the `service-actors.md` "system lane — deferred" note). Self-adversarial pass folded in (§10). **✅ Andrew-ratified (2026-06-28)** — ready for the Lattice Steward (pairs with #1 per-lane consumers: #1 makes lane isolation real, this makes it defended). **🏗️ Fire 1 SHIPPED (cacfb2b, CI watch in progress) — grants converge (dark, zero behavior change).** The lane grant the auth model already plumbs (`CapabilityDoc.Lanes` parsed but never read) is now live BEFORE the Fire 2 step-3 gate — the load-bearing migration ordering. Core `cap.<actor>` lens (`CapabilityLensDefinition`) → `["default","meta","urgent","system"]` for the protected kernel actors (admin/Loom/Weaver/Bridge/objmgr — consistent with their uniform root `platformPermissions`); rbac `cap.roles.<actor>` (`capabilityRoles`) gains a static `Output.Lanes ["default"]` (ordinary role-holders, "most actors hold `default` only") + rbac package `0.1.0`→`0.2.0` (fresh-install pickup; F-004 same-version caveat on live dev stacks). **No step-3 gate yet** — projected `lanes` unread, behavior byte-identical. No frozen-contract change. **Grounded deviation from design §6 (flagged for Andrew): NO bootstrap `version` bump** — the version field gates the bootstrap.json NanoID-file format and this change adds no NanoID field / no top-level key (count stays 34), so a current json seeds the new lanes from code; matches the D1.1 precedent (a larger new-lens change shipped at "12" with no bump) and avoids forcing a disruptive global `make down && make up` on the shared stack (live-stack content pickup is the standard F-004 dev caveat). Producer tests flipped on the **live** lens projections: core 4-lanes (`refractor_capability_e2e_test.go` + literal-cypher `capability_lens_contract_test.go`); rbac `["default"]` (`refractor_capability_multi_e2e_test.go`). Gates green (build/vet/golangci 0/STRICT-conventions 0; bootstrap/rbac/refractor/processor `go test`; Gate 2 bypass). Thorough-lead review (design §8 scopes Fire 1 dark+migration-safe; the full 3-layer is reserved for Fire 2). **✅ Fire 2 SHIPPED (d6530e9, CI watch in progress) — enforcement turned on; lane authorization is now DEFENDED.** The §3.2 step-3 lane gate is live (`step3_auth_capability.go`, `CapabilityAuthorizer.Authorize`): an unauthorized-lane submission is rejected `LaneUnauthorized` (§2.6) before the operationType matcher. Platform path: `env.Lane ∈ doc.Lanes` (read from the doc the platform path already fetched — the one-GET hot-path invariant preserved), **fail-closed** (empty `doc.Lanes` grants nothing, incl. default). Non-platform paths: **deny-by-default on the path kind** (`kind != pathPlatform` ⇒ `default`-only pre-read reject, no extra GET) — folded in from the Blind Hunter's observation so a hypothetical future non-platform path inherits default-only fail-closed rather than an unchecked lane (the design's literal `service||task` enumeration would have left a 4th kind fail-open). `StubAuthorizer` bypasses the gate as it bypasses all auth. **Gate-3 Vector #8** (`capadv_lane_unauthorized_test.go` + `gate3_test.go`, report now **8/8** cleared): a default-only actor on `system`/`meta`/`urgent` is BLOCKED `LaneUnauthorized` (DEFENDED); a privileged actor authorizes on `system`; the default-only actor still authorizes on `default`. Unit table covers platform lane-in/out, fail-closed empty, service/task non-default pre-read reject (asserting **no GET**), service-default-passes, stub bypass; fixed 3 pre-existing tests that built lane-less envelopes (production parse-validates the lane at step 1). **Migration order-safe** — grants converged Fire 1 (dark) first; the protected kernel actors (admin/Loom/Weaver/Bridge/objmgr) carry all four lanes via the core `cap.<actor>` lens + travel the platform path, so the engines' `ops.system.>` dispatch + the admin's `ops.meta.>` installs stay authorized; ordinary actors hold `default` only (`cap.roles.<actor>`). No NON-granted actor submits a privileged lane today (verified across loom/weaver/bridge dispatch + installer; the lease/object-gc e2e harnesses use `AuthModeStub` so the gate is dark there; hellolattice's only `meta` ops are by the protected BootstrapIdentity). **No frozen-contract change** (build-to §2.3 + §2.6). Docs: `service-actors.md` "system lane — deferred" retired → present-tense "system lane authorization"; `processor.md` step-3 lane-gate note added. **Full 3-layer review** (Blind Hunter / Edge Case Hunter / Acceptance Auditor): Edge=no unhandled cases; Blind=1 deny-by-default hardening (folded) + 1 no-fix MINOR; Accept=ACCEPT-WITH-FIXES — the 2 LOW test-completeness gaps (a bespoke commit-path/stack e2e + a conformance behavioral assertion) **consciously subsumed**: rejection proven by Vector #8 driving the real `CapabilityAuthorizer` end-to-end, system-lane success by the engine e2e under enforcement, and the denial→rejected-reply commit wiring is identical to the already-tested `AuthDenied` path. **Pairs with #1 per-lane consumers: #1 made lane isolation real, this makes it enforced.** |
| Control-plane Capability authorization (FR30) | Both component control planes — Weaver (`internal/weaver/control`) and Refractor (`internal/refractor/control`) — ship a `StubCapabilityChecker` that **allow-alls and logs every call**; any actor on the bus can `disable`/`enable`/`revoke` a convergence target or pause a lens. The `CapabilityChecker` interface seam already exists (designed for swap without touching handler bodies — `control/capability.go`), so this is a wire-up to a real Contract #6 Capability-KV check + the design of how a `lattice.ctrl.*` op maps to a capability and where the actor identity comes from on a `micro.Service` request. Mirrors the Loom control plane (built later, same stub posture — confirm before filing it a third time). **Needs design**; security-plane; **intersects read-actor auth / Gateway** (the actor-identity seam is the same one D1 increment 1 introduces — sequence behind or alongside it). No frozen-contract change (Contract #6 already defines the capability shape). Grounding: `internal/{weaver,refractor}/control/capability.go`, weaver.md/refractor.md "Capability authorization". | ★★ | M | ✅ **Andrew-ratified (2026-06-27) — deferred behind D1; build Path A end-to-end** — design: [`control-plane-capability-authz-design.md`](../../implementation-artifacts/control-plane-capability-authz-design.md). The control op = a §6.4 platform permission (`ctrl.<comp>.<verb>`, `scope:any`), via the standard role/permission graph; shared `internal/controlauth` checker across all 3 planes (Weaver/Refractor/Loom) over a new `internal/capabilitykv`; actor on a `Lattice-Actor` header. **Fork resolved:** sequence **behind D1** (higher priority + builds the shared `internal/gateway/auth` JWT seam), then ship **Path A end-to-end** (capability gate **+ verified-JWT actor in one shipment**, reusing D1's seam) — **dropping** the forgeable self-asserted-header interim. The interim is covered by #1's `lattice.ctrl.>` subject restriction (residual = allow-all *among trusted operators*, today's posture, tighter). **`operationType` = dotted `ctrl.<comp>.<verb>`** + **Contract #6 §6.4 touched up** (relax the unenforced "PascalCase" prescription + reserve the `ctrl.*` namespace — staged uncommitted in `main` alongside §6.14). Build after D1 ships (reuses its JWT seam). |

### Privacy / Vault
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| Vault + crypto-shredding | Per-identity keys for sensitive aspects (SSN / DOB); right-to-be-forgotten = destroy the key; transient-session-key decryption for the Edge node; + the privacy failure tier (`KeyShredded` listener). | ★★★ | L | ✅ **Andrew-ratified (2026-06-27) — deferred behind D1; build Phase A + Phase B together** — design: [`vault-crypto-shredding-design.md`](../../implementation-artifacts/vault-crypto-shredding-design.md). Crypto layer on the already-shipping sensitivity boundary: aspect-level `sensitive:true` ⇒ ciphertext-at-rest (AES-256-GCM, per-identity DEK envelope-wrapped), `ShredIdentityKey` destroys the key. **Forks resolved:** (1) **Path A** — pluggable `internal/vault` iface + local envelope backend first, KMS adapters later; (2) **phasing REVISED — sequence the whole feature behind D1 and ship Phase A + Phase B *together*** (a Phase-A-only world strands every non-Processor PII reader — vertical apps *displaying* PII + bridge adapters *sending* it to vendors — and there's no production exposure pressure to ship a half-done interim; the original Phase-A-now split is dropped). Renewed-skill re-review folded in: GCM nonce-uniqueness grounding (NIST SP 800-38D) + DEK-rotation posture; sensitive **blobs** (Object Store PDFs/scans) scoped OUT → see the new follow-on item below. Contract **#3 §3.10 ratified** but staged **uncommitted** (commit when the Vault build starts, after D1). |
| **[Object Store] Crypto-shred for object-store blobs** *(follow-on from Vault)* | Vault crypto-shred covers sensitive **aspects** (Core KV) but **not** PII-bearing **blobs** (lease PDFs, ID scans, signature images) in `objects-base`, stored content-addressed + unencrypted. So `ShredIdentityKey` leaves document-PII recoverable — right-to-erasure is complete for aspect-PII, **partial for document-PII**. Fix: encrypt a sensitive object under the owning identity's DEK (or a per-object key wrapped by it) at upload, so the *same* shred destroys both planes. Filed 2026-06-27 (Designer re-review of the Vault design, §2.6). **Needs design**; composes on the ratified Vault feature (build after it). | ★★ | M | 📐 **awaiting-Andrew (ratification)** — design: [`object-store-crypto-shred-design.md`](../../implementation-artifacts/object-store-crypto-shred-design.md). **Composes on the ratified Vault feature** (reuses §3.10's per-identity DEK as the KEK — *no new key hierarchy*; one `ShredIdentityKey` erases both the aspect and blob planes). **Shape:** a `sensitive: true` object's bytes are client-encrypted ciphertext (AES-256-GCM, per-object CEK) streamed onto the §7.2 bytes plane; the CEK — not the bulk bytes — is wrapped under the governing identity's DEK via the Vault; the envelope `{algo,nonce,wrappedCEK,keyId}` rides `.content`; readers get ciphertext by default (opt-in Vault-unwrap, the §3.10 posture). **The one decision for Andrew (designed-through, not a fork):** content-addressed dedup and crypto-shred are *mathematically incompatible* (content-derived keys can't be destroyed) → a sensitive object's oid becomes **identity-salted** (`sha256NanoID("object:"+keyId+":"+digest)`), dropping *cross-identity* dedup — which also closes a real PII **linkage leak** — while keeping within-identity idempotency; non-sensitive objects are byte-for-byte unchanged. **Frozen-contract: new Contract #3 §3.11 ("Sensitive-object (blob) encryption at rest") — ✅ ratified + committed (2026-06-28)** (the backlog's earlier "no frozen-contract change anticipated" was wrong; §7.2 judged unchanged). **No architectural fork** (Vault dependency inherited). 4 fires; **build-sequenced behind Vault (A+B), which is behind D1** — design on the shelf now. **✅ Andrew-ratified (2026-06-28)** — the Lattice Steward builds after Vault lands. |

### External-I/O maturity (bridge follow-ons)
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| Real adapters + async result-return | Replace the `Fake*` adapters with real vendors and design the **asynchronous** result path. Today the bridge's `Adapter.Execute` is synchronous and must return a final `Result`; real checks / payments submit → pending ref → webhook/poll callback hours–days later. Needs (a) an inbound-result mechanism (webhook receiver, or poll via the `core-schedules` temporal lane), (b) an `Execute` contract that expresses "submitted, resolve later" (the bridge claim vertex stays open until the inbound result drives the replyOp), (c) a re-tuned wedged-claim horizon for legitimately-pending async claims. | ★★ | M–L | ✅ **async result-return DONE** (the async external-reply epic, 2026-06-20): the `Dispatch{Disposition: Resolved|Pending, Ref}` SPI + `Adapter.Poll` (`internal/bridge/adapter.go`), the `schedule.bridge.>` re-poll/give-up lane (`schedule.go`), the pending-marker `dispatch` op + timeout-`failed` terminal, all e2e-gated (`pending_e2e_test.go`, `schedule_e2e_test.go`, `fake_async_check.go`); §10.6 async-resolution amendment ratified. **Residual = "real vendors"** (replace `Fake*` with production Stripe / background-check SDKs) — an *integration/ops* task, not platform design; the SPI is stable. |
| Structured adapter result | The bridge posts `{externalRef, result}` and the replyOp hard-codes `status="completed"` — there is no `failed` producer. Thread a structured pass/fail/detail status onto the reply; lens `missing_*` predicates key off the real status. | ★★ | S–M | ✅ **DONE** — `Outcome{OutcomeCompleted, OutcomeFailed}` + `Result{Status, Detail}` ship today (`internal/bridge/adapter.go`); `OutcomeFailed` is a terminal business rejection (err==nil, not retried), copied verbatim into the replyOp payload for the paired replyOp to branch on. The "hard-codes completed / no failed producer" framing is stale. |
| Adapter read-seam / richer params | Adapters can only use what the target-lens row projects; a vendor needing extra subject fields (SSN / DOB) has no fetch path. Decide: richer projection columns vs. an adapter read seam. | ★★ | S–M | 📐 **awaiting-Andrew (ratification)** — design: [`adapter-read-seam-subject-templated-params-design.md`](../../implementation-artifacts/adapter-read-seam-subject-templated-params-design.md). **Grounding reframes it:** this is not a missing feature but an **implementation-vs-frozen-contract drift** — §10.5 (L549) already mandates externalTask `params` be "row/subject templates resolved per the §10.5/§10.8 templating rule," yet the engine does **opaque pass-through** (`engine.go:997`) and hydrates only `[subjectKey]` (`engine.go:1009`), so an adapter only ever receives static pattern data (`{family}`), never real subject aspects. **Fork resolved → the subject-aspect read seam (NOT richer projection columns):** Loom resolves a `subject.<aspect>.data.<field>` param template (the **same grammar guards already use**, §10.5 L587) against the instanceOp's OCC-hydrated subject — **inferring** the aspect reads from the templates (no new step field), staying on the write path (P2, §2.5 known-key reads, no scans), keeping PII **off** the read-path (rejected the projection-columns option: a D1/Vault leak + a frozen `triggerLoom` shape change + only works on the Weaver-originated path). **The sensitive-PII (SSN/DOB) egress path composes on the ratified Vault feature** (a sensitive-ref in params, bridge-side Vault-unwrap) — designed-through, **build-sequenced behind Vault**. **Decision (Andrew, 2026-06-28): Mechanism 2 (flavor 2a) — Loom reads NO Core KV.** Loom *infers* the aspect read-set from the `subject.*` templates (string parsing) and declares it in the instanceOp's `contextHint.reads`; the **Processor JIT-hydrates** and the **instanceOp DDL resolves** the templates via §2.5 `kv.Read` from hydrated state (a shared `orchestration-base` Starlark helper) — Core-KV reads stay in the Processor, Loom's guard-eval stays the lone exception, and the params come from the OCC-hydrated commit snapshot (the same `kv.Read` mechanism the §8 two-hop reads use). **Frozen-contract: §10.5 L549-550 narrowed `row/subject`→`subject` for externalTask params + implementation-neutral resolution wording — ✅ ratified + committed (2026-06-28).** 3 fires (Fire 1 read-set inference + shared resolver helper = full review; Fire 2 the lease-signing backgroundCheck consumer = today-consumer, end-to-end green; Fire 3 sensitive-PII via Vault = designed-not-built). **✅ Andrew-ratified (2026-06-28)** — ready for the Lattice Steward. **🏗️ Fire 1 SHIPPED (65f4f4d, CI watch in progress) — the templating engine (dormant, backward-compatible).** Closes the impl-vs-§10.5 drift at the mechanism level (no consumer yet, so existing patterns' read-sets are byte-identical). **(i) Loom DECLARES, reads no Core KV:** `inferExternalTaskReads` (`internal/loom/externaltask_params.go`) walks `step.Params`, `parseGuardPath`s each `subject.*` value, and adds the known aspect key `subjectKey.<aspect>` to the instanceOp's `ContextHint.Reads` (`engine.go:1009`); a malformed `subject.*` template fails loud at submit; deterministic sorted order keeps the outbox byte-stable; the opaque `params` pass-through (`engine.go:997`) is preserved. **(ii) shared resolver helper:** `orchestrationbase.ResolveSubjectParamsHelper` — a Starlark `resolve_subject_params(params, subject_key)` snippet an externalTask instanceOp DDL includes; it substitutes each `subject.*` token from the op's JIT-hydrated OCC-snapshot state via §2.5 `kv.Read`, and an absent/tombstoned vertex or absent/null field is a **loud data error** (never a blank to a vendor). Builds to the **committed §10.5 narrowing** — no contract change. **Grounded deviation from design §2.2 (flagged for Andrew):** the helper signature is `resolve_subject_params(params, subject_key)`, not the design's `(params, state)` — the 2-arg form omitted `subject_key`, which is *required* to build the aspect key `subjectKey.<aspect>`; resolution is via `kv.Read` (which serves the hydrated OCC snapshot for declared keys), not `state`-indexing. Sound impl correction; not a contract change. Tests: inference table (literals, root/aspect tokens, multi-aspect sorted dedup, non-object params, malformed-template loud-fail) + resolver Starlark table (literal pass-through, root/aspect resolution, mixed, None→{}, missing-field/tombstoned-aspect/malformed loud-fail). Gates green (build/vet/golangci 0/STRICT-conventions 0; full loom + orchestration-base `go test`). Thorough-lead + adversarial self-pass on the two design-flagged boundaries (Loom-takes-no-Core-KV + read-set/resolver grammar lockstep + data-error loudness) — scaled from "full review" per the dormant-seam precedent; **full 3-layer reserved for the Fire-2 live-consumer turn-on. Remaining: Fire 2** (the lease-signing `backgroundCheck` externalTask declares `subject.demographics.data.*` templates + the convergence e2e asserting adapter receipt — **Verticals lane**, `packages/lease-signing`) **+ Fire 3** (sensitive-PII via Vault, designed-not-built). |

### Scale-out
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| Multi-cell / sharding | Graph scales by **cells** (root + subgraph co-located for atomic writes); global adjacency index + bridge links for cross-cell traversal; live migration as a dual-write shadow; multi-cell routing in Processor + Refractor. Keys already embed no cell identity (validated Phase 1). **HA NATS clustering (the row below) is its per-cell availability prerequisite.** | ★ now / ★★★ at scale | XL | 📐 **awaiting-Andrew (ratification) — 2026-06-29** — design: [`multi-cell-sharding-design.md`](../../implementation-artifacts/multi-cell-sharding-design.md). Realizes the ratified `deployment-isolation.md` per-cell topology + the vault Cell.md Migration Dance into a buildable mechanism. **Cell = one Core-KV bucket holding a co-located sub-graph; cell identity lives in the bucket name + a replicated `cell-index` (`vertex→cell`), NEVER in a key** (NFR-SC2 locked → bridge links need NO rewrite on migration, the design's keystone win). Routing at the **Gateway** (Processor cell-unaware except the migration dual-write window). **One architectural fork (designed through, rec given): cell granularity + migration mechanism — A bucket-on-shared-cluster (atomic dual-write) vs B isolated-deployment (saga) vs C RECOMMENDED superset (a deployment hosts 1+ cells; atomic intra-cluster / saga cross-cluster, A-flavored default).** 3 secondary decisions resolved (multi-tenant N:1-default/never-1:N #124; cross-cell saga-not-2PC #121; cell-local lenses dissolve the watermark problem #110). Resolves brainstorm cells items #77–89 + #109/#110/#114/#121/#124. **Frozen-contract: ONE staged addition — Contract #2 §2.6 `CellMoved` (the `410 Gone` stray-write reject) — UNCOMMITTED in `main`** (the diff is the proposal); keys + commit semantics unchanged. Self-adversarial pass folded in (§6.4). **Hard prerequisites (both awaiting-Andrew): the Gateway (routing host) + HA-NATS clustering (per-cell availability)** + a real scale driver — so **ratify-now / build-on-driver**, except **Fire 1** (cell-addressing substrate seam, no-behavior-change at one cell, de-risks) buildable now. 6 fires (Fire 1 seam → Fire 2 index+router → Fire 3 bridge links → Fire 4 migration dance = full 3-layer → Fire 5 saga+tenant-mapping → Fire 6 scale/chaos acceptance). **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds Fire 1; Fires 2–6 behind Gateway + HA-NATS + a scale driver).** Prerequisite-paired with the HA-NATS row (HA = vertical resilience within a cell; this = horizontal scale across cells). |
| **HA NATS clustering (substrate + compute availability)** | Single-server today; clustering + multi-instance engine fan-out (several components note single-instance as a Phase-3 concern). | ★ now / ★★ prod | M–L | 📐 **awaiting-Andrew (ratification) — 2026-06-29** — design: [`ha-nats-clustering-design.md`](../../implementation-artifacts/ha-nats-clustering-design.md). **No frozen-contract change** (replica count + cluster topology are deployment/substrate config — same category as the ratified NATS-write-restriction; atomic-batch semantics unchanged under R3 per **ADR-50**; the `role:active\|standby` Health-KV metric is §5.4 author-discretion). **Two planes:** (1) **substrate** — 3-node NATS cluster + **R3** on every stream/KV/object store (`primordial.go`/`stream.go`/`cmd/refractor`), parameterized `NATS_REPLICAS` (default **1** → dev/CI/embedded byte-identical); (2) **compute** — per-component posture: **Loom/Refractor/Bridge/Loupe already HA-safe** (durable consumers / bucket-coordinated idempotency → run N replicas, *no code*), **Weaver** composes on the ratified `@every` design, and the **sole-writer Processor** gets a leader-elected active/passive posture. **One architectural fork (designed through):** the **Processor HA posture — A active/passive (leader-elected, RECOMMENDED) vs B active/active (shared durables + OCC + cross-instance DDL-cache coherence).** Rec A — preserves P2 serialization + synchronous DDL-cache invalidation exactly; the 10–100 ops/sec target gives no throughput case for B; OCC (step-8 `expectedRevision`) guarantees no double-commit even during a failover race, so the lease is liveness-only. **One new primitive:** a `substrate` NATS-KV-CAS **leader-election lease** (no external dep; same TTL+CAS family as Weaver mark-lease / Loom deadline). **Build sequencing (ratify-now / build-on-driver):** NFR-R6 accepts single-server for dev/demo → ratify the design + shelve the build behind the first prod-HA driver (like Vault/D1), **except Fire 1** (parameterized replica seam + clustered embedded-NATS test fixture — no-behavior-change at R1, proves R3+atomic-batch, de-risks) is buildable now. 4 fires (Fire 1 replica seam+cluster fixture = full review, buildable now → Fire 2 lease+Processor active/passive = full 3-layer P2-plane → Fire 3 remaining singletons on the lease → Fire 4 prod topology + node-kill chaos e2e = NFR-R6 acceptance). Self-adversarial pass folded in (§6.4); **recommended `bmad-party-mode` pass on the failover/split-brain boundary before Fire 2.** **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds Fire 1; Fires 2–4 on the prod-HA driver).** Prerequisite for the Multi-cell row above (per-cell availability). |

### Edge & personal lenses *(the path Loupe grows into)*
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| Personal / Secure Lens | Refractor projects a per-identity **security-filtered** subgraph stream; the "Interest Set" watchlist; RLS-style link filtering. Gates the real Edge node; intersects read-path auth. | ★★ | L | ✅ **Andrew-ratified (DESIGN; build sequenced behind D1 + a real consumer) — 2026-06-27** — design: [`personal-secure-lens-design.md`](../../implementation-artifacts/personal-secure-lens-design.md). **The Edge fan-out path** (D1's "Path C"): a new `nats_subject` target adapter publishes per-authorized-identity delta streams to `lattice.sync.user.<id>`, an **Interest Set** (Refractor-owned operational KV, P1) narrows relevance, a **Hydration** RPC bulk-loads cold devices; reuses the Capability pipeline's `ActorEnumerator` fan-out. **Security IS D1** — the per-user filter is the **union** across D1's decomposed `cap-read.*.<actor>` slices / `actor_read_grants` (build-to §6.14). **No frozen-contract change.** **Forks resolved:** (1) transport = **NATS-subject JetStream** (WS/push → Gateway); (2) sequencing **REVISED — defer the whole build behind D1 *and* a concrete consumer** (the "build dark now" plan is dropped: no Edge consumer exists yet + security is stubbed pre-D1 = dead scaffolding; the *design* is ratified + on the shelf, build when D1 + a real consumer land); (3) subscribe-ACL **rides the ratified NATS-account-auth (#1)**. PL.5 (ciphertext deltas + Edge transient-key) rides the ratified Vault feature (itself behind D1). |
| NATS-subject publish-events adapter | A Refractor target adapter that publishes projection deltas to `lattice.sync.user.<id>` subjects — required for Personal Lens fan-out (only NATS-KV + Postgres adapters ship today). | ★★ | S–M | 📐 **subsumed** — designed as **Fire PL.1** of the Personal/Secure Lens design above ([`personal-secure-lens-design.md`](../../implementation-artifacts/personal-secure-lens-design.md)); independently shippable + green on its own. |
| Edge Lattice (full) | The sovereign per-user node: local VAL (SQLite / IndexedDB), local Starlark, offline-first, reconcile-by-revision, transient-key decryption of vaulted aspects. Loupe is its trusted-tool precursor. | ★★ | XL | 📋 |

### AI-native
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| AI-authored capabilities | A Lattice-aware agent proposes DDL / Starlark / lenses / workflows through human review + deterministic validation + rollback-friendly contracts. Marquee AI vision. | ★★–★★★ | L | 📐 **awaiting-Andrew (ratification) — 2026-06-29** — design: [`ai-authored-capabilities-design.md`](../../implementation-artifacts/ai-authored-capabilities-design.md). **The tier above the Augur** (✅ ratified, the bounded first step): lifts Augur's propose→validate→human-gate→apply skeleton from *arranging existing ops* to *authoring new package artifacts*. **Shape:** a Lattice-aware agent (`identity.ai.*`) turns a capability *request* into a **proposed package artifact** (`kind` ∈ lens / grant / weaverTarget / loomPattern / [gated] Starlark DDL) recorded as `vtx.capabilityProposal`, deterministically validated by the **existing `pkgmgr.validateAll` + openCypher parser + lint-conventions + an F-004 `--dry-run` sandbox** (a new AI-free **materializer** turns the declarative artifact into an install/upgrade write-set), and applied **only after a human approves** via the **already-ratified F-004 `InstallPackage`/`UpgradePackage` op submitted under the *operator's* identity** (rollback = F-004 upgrade/uninstall, FR53). The LLM call goes through a new `capabilityAuthor` **bridge adapter** (like Augur — engines never call a model directly). **No frozen-contract change anticipated** (proposal/ops = package DDL; adapter = bridge data §10.5; apply = ratified Contract #8 §8.1/§8.6; the candidate validate-only kernel op is designed-through + **rejected** — the kernel step-8 guard is the authoritative apply-time backstop). **AI-actor-authority (#125) is *already resolved* — honored, not re-opened:** architecture **Item 4** (AI = ordinary `identity.ai.*`, capability-scoped, no privileged authoring class/bypass) ⇒ the AI holds **only** `RequestCapabilityAuthoring`, never the privileged apply op. **Two decisions for Andrew (designed through, recs given):** (1) **authoring autonomy** — Option A human-in-the-loop **always** (rec as the *standing posture*, stronger than Augur — authoring executable capabilities is categorically higher-stakes) vs Option B kind/confidence-gated auto-apply (designed §8 Fire 5, **recommend NOT building**); (2) **artifact-kind boundary** — author lens/grant/weaverTarget/loomPattern first (fully statically validatable), **gate Starlark-bearing DDL** behind the verified-pure Starlark sandbox (the separate 📋 *Starlark guards* item) **+ a separate ratification** (it executes on the write path, can't be statically validated). 5 fires (Fire 1 capture+lens = full 3-layer → Fire 2 approve→apply+grant w/ scope-check → Fire 3 declarative orchestration kinds → Fire 4 Starlark GATED → Fire 5 autonomy dial design-only); **build-sequenced behind the Augur** (proves the skeleton on the smaller surface first), each fire independently shippable + green. Self-adversarial pass folded in (§9); recommended full 3-layer per fire at build + a party-mode pass on the scope-check/apply-authority boundary before Fire 2. **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds, after the Augur lands).** |
| **The Augur** (AI reasoning tier — implements the L3 evaluator) | Weaver's AI-assisted reasoning tier for ambiguous / novel convergence gaps (L1 / L2 ship today). The marquee AI-native flagship, named **Augur** — it interprets an ambiguous gap and *proposes*; the human decides. | ★★ | M–L | ✅ **Andrew-ratified (2026-06-27) — build now** — design: [`augur-design.md`](../../implementation-artifacts/augur-design.md). Turns a stuck Weaver gap (unplannable / retry-exhausted) into an AI-reasoned, human-reviewable **proposal**: Weaver escalates over the existing `triggerLoom→externalTask→bridge` path (a new `augur` bridge adapter — Weaver never calls the model directly), the model (default **`claude-opus-4-8`**) proposes within the installed action catalog via **structured outputs**, the `replyOp` records a `vtx.augurProposal` pending human approval; deterministic validator + human gate govern, Processor stays sole writer (P2). **Decisions:** build now (strategic AI-loop de-risking); **autonomy fork = Option A (human-in-the-loop) now, Option B (auto-apply) designed-but-NOT-built** until Andrew ratifies the autonomy boundary; default model opus-4-8. **Contract #10 §10.8 (`augur` block) ratified + committed (2917e0f).** Ready for the Lattice Steward (Fire 1 reasoning-capture → Fire 2 approve→dispatch; Fire 3 auto-apply gated). **🏗️ Fire 1 IN PROGRESS — foundation + adapter layer + reply-path reconciliation + target-policy parse/validate in worktree (NOT merged), HEAD `fb30c3d` on branch `augur-fire1` (worktree `../lattice-augur-fire1`; **rebased current onto `main` 2026-06-29**, folding in lane-authorization Fire 1/2 — load-bearing for Augur's `system`-lane dispatch auth — + 21 other commits; gates green post-rebase).** Built + green: **(a) `packages/augur/` (`fb0c28e`)** = the **`augurproposal` vertex type** (D5 root, business data in aspects) + the **`RecordProposal`** op (the bridge replyOp) carrying the **design §5 record-time deterministic-validation boundary** (the safety core): action ∈ {triggerLoom, assignTask, directOp}, confidence ∈ [0,1], **scope-escape** check (a proposed entity-param ≠ the escalated candidate → `invalid`); the proposal is **always stored** (auditability) — verdict decides `pending` (dispatchable) vs `invalid` (never dispatchable). No-orphan (FR29/P4) on both link endpoints; deterministic `proposalId` + existing-key idempotency (mirrors the task DDL). Integration tests through the **real Processor** (embedded NATS) cover the full validator table (valid→pending / bad-action→invalid / scope-escape→invalid / confidence-OOR→invalid / absent-candidate→rejected). **(b) the bridge adapter layer (`349d1e6`)** = **`FakeAugur`** (the deterministic CI/e2e reasoning adapter, mirrors `FakeStripe`/`FakeBackgroundCheck`: per-idempotencyKey memoization + a reasoning-call side-effect counter = the cost-control proof of ≤1 billed call/episode; trigger Subjects for the §5 adversarial classes scope-escape / unknown-action / OOR-confidence + a modeled refusal + a `SetProposal` injection seam) + the **`AugurProposal` codec** (`Encode`/`DecodeAugurProposal` — the model's structured output only, carried in the bridge's opaque `Result.Detail`). Unit tests: codec round-trip + blank/malformed reject, happy path, idempotency, every adversarial trigger, refusal, Poll-error. Gates green for both (build ./... / vet / golangci 0 / STRICT lint-conventions 0 / `go test -race ./internal/bridge ./packages/augur`). **Grounded deviation flagged:** the vertex type segment is the lowercase **`augurproposal`** (not the design's illustrative camelCase `augurProposal`) — `substrate.isValidTypeSegment` mandates lowercase + the `leaseapp` precedent confirms; class/canonicalName follow. **✅ Reply-path reconciliation — RESOLVED via Option A (`9723bd0`):** the bridge posts a FIXED replyOp payload `{externalRef, status, result}` (`dispatch.go:182`), but the as-built `RecordProposal` read the gap context (`targetId`/`entityId`/…) directly from `op.payload` — context the bridge never threads. Reworked to the established lease-signing matched-pair pattern: the `augurproposal` DDL now owns **both** ops — a new **`CreateAugurReasoningClaim`** instanceOp mints the claim vertex `vtx.augurproposal.<handle>` write-ahead with the **TRUSTED** gap context (`.gap` aspect) + the forCandidate/forTarget links (no-orphan via `kv.Read`); **`RecordProposal`** reworked to `{externalRef, status, result}` reads the trusted gap context back from the claim via `kv.Read` (the bridge submits no `ContextHint.Reads`), decodes the model proposal from the opaque `result` via `json.decode`, runs the §5 validator against the **TRUSTED `entity_key`** (never the model's reply — the load-bearing safety split the codec doc states), and writes the model-derived aspects create-only; it **always** emits `orchestration.externalTaskCompleted` so Loom unparks even on an `invalid` verdict / a modeled refusal (`status=failed` → `invalid`, never a crash). `proposalId = handle` (the escalation episode's instanceKey) → deterministic idempotency for free. Reworked tests prove the two-op flow through the real Processor incl. a new **reply-without-claim → rejected** adversarial (a model reply can never fabricate a proposal). Gates green (build / vet / golangci 0 / STRICT 0 / `go test -race ./internal/bridge ./packages/augur` / pkgmgr manifest cross-check). **Grounded deviations (for the 3-layer review):** instanceOp named `CreateAugurReasoningClaim` (the design §3.3 pseudocode's illustrative `CreateReasoningClaim`, package-namespaced); a `completed` reply with a non-empty-but-malformed `result` JSON is a terminal reject (the codec guarantees well-formed JSON — an adapter-wiring-bug surface, not a refusal). Still to cover in the 3-layer review: the §5 validator boundary + this reply-path split. **NOT merged + thorough-lead-reviewed only** — held because (a) the package is **inert until wired** (not in the bootstrap install list / no consumer), so merging it alone is dead scaffolding, and (b) this is a **security/capability-plane safety core** ⇒ the admit rule + the design both mandate a **full 3-layer adversarial review** at Fire-1 completion. **▶ Remaining Fire 1 (next fires, then 3-layer review → merge):** (1) ✅ the deterministic **`FakeAugur`** for CI — DONE (`349d1e6`); the **real `claude-opus-4-8` `augur` adapter** (network, untestable in CI, needs an anthropic client) remains a follow-on; (2) ✅ the **reply-path reconciliation** — DONE (`9723bd0`, Option A: `CreateAugurReasoningClaim` claim-vertex instanceOp + `RecordProposal` reworked to `{externalRef,status,result}`); (3a) ✅ the **augur target-policy parse + install/runtime validation** — DONE (`545a695`): `Target.Augur`/`AugurPolicy`/`AugurAutoApply` (weaver registry) + `WeaverTargetSpec.Augur`/`AugurSpec` (pkgmgr) + `augurBody` emit; `validateAugurPolicy` (engine, CDC-load) + `validateAugurSpec` (installer) both enforce escalate∈{unplannable,exhausted} non-empty, pattern required, autoApply.actions⊆{triggerLoom,assignTask,directOp}, minConfidence∈[0,1]; installer additionally enforces a **package-local** augur.pattern carries an externalTask step (cross-package ref defers, like LensRef). `autoApply` parsed+validated fail-closed but **not consumed** (autonomy stays human-in-the-loop until Andrew ratifies). Default-absent ⇒ frozen-contract round-trip. Tests: validateAugurPolicy/Spec tables + spec/emit-path round-trips into `weaver.Target`. Gates green (build/vet/golangci 0/STRICT 0/`go test -race`). **(3b) the Weaver unplannable-escalation branch** (`internal/weaver/evaluator.go:148` `dispatchGap` `!ok` → dispatch the reasoning op when `target.Augur.Escalate` includes `unplannable`; read the parsed block) — **✅ UNBLOCKED — design addendum: [`augur-escalation-dispatch-addendum-design.md`](../../implementation-artifacts/augur-escalation-dispatch-addendum-design.md) (Winston, 2026-06-29). Resolution = Option F: Weaver dispatches `CreateAugurReasoningClaim` as a `directOp` → bridge, dropping the Loom wrapper (the bridge is loom-agnostic; the op already reads literal params from payload + emits `external.augur`; Weaver's `directOp` already resolves a params map from the row). NO contract touch, NO new primitive — it *removes* the loom pattern + externalTask. One design-of-record note flagged for Andrew (Augur becomes the first non-Loom bridge driver — lead-authority, reversible to Option B), not a ratification gate. Build-ready: see addendum §7. 🏗️ Addendum step 1 — (3b′) the augur op + policy reshape — SHIPPED (worktree `augur-fire1` `2a5f3a2`, NOT merged): `CreateAugurReasoningClaim` now reads its gap context FLAT from the top-level payload (matching Weaver's directOp flat params), `RecordProposal` drops the `orchestration.externalTaskCompleted` emit (no Loom to unpark — `augur.proposalRecorded` stays the audit signal), and `AugurPolicy`/`AugurSpec` replace the loom `pattern` field with optional `op`/`adapter`/`replyOp` overrides (default `CreateAugurReasoningClaim`/`augur`/`RecordProposal` at dispatch); `validateAugurPatternIsExternalTask` dropped. Tests: flat-payload builder + `TestAugur_NestedParamsRejected` (legacy nested shape now rejected) + reshaped weaver/pkgmgr validate+round-trip tables. Gates green (build/make vet/golangci 0/STRICT 0; augur+weaver+pkgmgr+bridge go test). 🏗️ Addendum step 2 — (3b″) the Weaver escalation dispatch — SHIPPED (worktree `augur-fire1` `86a912b`, NOT merged): the `evaluator.go` `dispatchGap` `!ok` branch now calls `augurEscalation` (strategist.go) when `target.Augur.Escalate` includes `unplannable` — it synthesizes a `directOp` GapAction (`Operation`=the policy op default `CreateAugurReasoningClaim`, flat literal params `{instanceKey, adapter, replyOp, targetId:<vtx.meta.id>, entityId:<entityKey>, gapColumn, trigger}`, `Reads:[entityKey, targetMetaKey]`) and dispatches via the normal `buildPlan(actionDirectOp)→fireEpisode` lane-1 path (anti-storm mark + OCC + reclaim inherited); a non-augur target / a target not escalating `unplannable` still raises `GapWithoutPlaybook` (fails closed). New helpers: `augurEscalation`+`defaultAugur{Op,Adapter,ReplyOp}` (strategist.go), `deriveAugurHandle` (actuator.go — the stable bare-handle instanceKey keyed on `(targetId,entityId,gapColumn)`, **NOT** claimId-seeded since the generic directOp payload carries no claimId, so ≤1 claim / ≤1 billed model call per stuck gap; a genuine close→reopen reuses the existing proposal — cost-bounded, flagged for the 3-layer/G-item), `targetMetaKey` (registry.go — resolves the row-key canonicalName targetId → `vtx.meta.<id>`). **Lead decision (for the 3-layer):** the addendum §2 sketched a claimId-seeded handle but §7 specifies `buildPlan(actionDirectOp)` (which ignores claimId) — went with §7 (simplest extension, reuses the whole directOp path), stable-handle close→reopen residual noted above. Tests: `augur_escalation_internal_test.go` (builds-directOp w/ full meta-key + trusted context + plan round-trip; honours overrides; fails-closed for no-augur / wrong-trigger; handle determinism). Gates green (build ./... / make vet / golangci 0 / STRICT 0 / `go test -race ./internal/weaver ./packages/augur ./internal/bridge`). 🏗️ **Fire-1 step 4a — rebase + Option-F doc reconciliation + auth grounding — SHIPPED (worktree `augur-fire1` `fb30c3d`, NOT merged; 2026-06-29).** (i) **Rebased the 6 in-flight commits current onto `main`** (was 22 behind) — clean, no conflicts; folds in **lane-authorization Fire 1/2**, which granted the protected kernel actors (incl. Weaver) the `system` lane — directly load-bearing for the Augur directOp dispatch; gates green post-rebase (build / augur+bridge `go test`). (ii) **Reconciled the stale pre-Option-F docs** (`package.go` + `permissions.go` `fb30c3d`): both still narrated `triggerLoom → externalTask → bridge` / "the Loom instanceOp" / "Loom's relay actor", contradicting `ddls.go` (already Option-F-correct). Now both describe the actual path — Weaver dispatches `CreateAugurReasoningClaim` as a directOp that mints the claim + emits `external.augur`; the bridge `augur` adapter calls the model; `RecordProposal` records the verdict. (iii) **Auth grounding (the permissions half of step 4 — resolved, no new grant needed):** Weaver's service actor is **operator-equivalent** (`identity.system.weaver` holdsRole → operator, `service-actors.md:20/27`), so the existing `GrantsTo:[operator]` on both ops **already authorizes** the directOp submit; and post-lane-auth Weaver **holds the `system` lane** it dispatches under. Gates green (build/vet/golangci 0/STRICT 0). **⚠️ FLAGGED — the "primordial install list + bootstrap version bump" half of step 4 needs a grounded call, not a rushed bump:** the design Migration note (§374) saying "augur = a primordial package on the install list + version bump" was written **pre-Option-F** — it assumed a primordial `augurReasoning` Loom pattern that **Option F dropped**, and augur's only dependency, **`orchestration-base`, is itself NOT primordial** (it's a post-bootstrap `lattice-pkg install`, co-installed with verticals). Making augur primordial would force orchestration-base primordial too. **Lead recommendation: augur is an opt-in installable AI package** (co-installed via `lattice-pkg` where the AI tier is wanted, matching its non-primordial dependency; Weaver's escalation already fails closed to `GapWithoutPlaybook` where augur isn't installed) — a deviation from the ratified design's (now-superseded) migration note, **flagged for Andrew**. **▶ NEXT (4b — once the install posture is settled):** the `verify-package-augur` make target + `scripts/verify-package-augur.go` (co-install `orchestration-base` → `augur`, assert the `augurproposal` DDL + the 2 op permissions + operator grants landed) — authored + wired into CI `stack-gates`; the live ephemeral run lands with the merge (it needs a stack, which can't be exercised locally without colliding with the shared dev stack, and augur stays unmerged until step 7); then (5) review lens, (6) e2e + adversarial, (7) full 3-layer. **The original gap (now resolved):** `StartLoomPattern` carries **only** `{instanceId, patternRef, subjectKey}` (`internal/loom/engine.go:360-370`) and an externalTask step's params resolve **only against the subject vertex** (`subject.<aspect>.data.<field>`, `inferExternalTaskReads`) — so the escalation **cannot thread `targetId`/`gapColumn` to the `augurReasoning` pattern's `CreateAugurReasoningClaim` instanceOp** (which requires `params.{targetId, entityId, gapColumn}` — see `packages/augur/ddls.go:303-305`). The design §3.3 (`params: {targetId,entityId,gapColumn,row,contextRef} # resolved from the row`) assumes a row-context the Loom path doesn't carry. Options to settle next fire: **(A)** per-(target,gap) literal-param augur patterns (no contract touch, N patterns — ugly); **(B)** extend `StartLoomPattern`/the externalTask path to carry a pass-through params map from the trigger (cleanest, but a Loom **contract touch** §10.5/§10.9 → Andrew); **(C)** derive gap context from the subject (gapColumn isn't on the entity — needs a synthesized reasoning-subject, which collides with P2). **→ Settled via Option F (addendum above): A/B/C all kept the loom wrapper that orchestrates nothing for a single-step reasoning episode; F dispatches the claim op directly from Weaver (it already carries the row context) and reaches the loom-agnostic bridge — no contract touch, no synthesized subject, no N patterns. B (a generic `StartLoomPattern` trigger-params channel, §10.5/§10.9 amendment) is the loom-faithful fallback if Andrew prefers Augur stay on Loom; file it as a general primitive only when a multi-step Weaver→Loom escalation actually needs it.** **(4)** the **bootstrap install-list** add (`augur` primordial package) + version bump — verify via `make verify-package-augur` + the ephemeral e2e (a new primordial package needs a fresh bootstrap, NOT the shared stack); (5) the **`augur-proposals` review lens** + the **`augurReasoning` externalTask pattern** (currently the DDL + op only); (6) the **e2e + adversarial** loop (faked adapter: unplannable gap → `pending` proposal; a malicious proposal → `invalid` / never dispatches); (7) the **full 3-layer adversarial review** focused on the §5 validator boundary + the reply-path reconciliation. *(Other ratified ★★ item not picked this fire — **NATS write-restriction Fire 2**: its completeness proof is the full `make up-full` Docker gate battery **with auth on**, which collides with the running shared core stack on the same ports — can't be exercised or fix-forwarded locally — and it is atomic-cross-cutting into the verticals stream's active files; best as a coordinated/attended/stack-down fire.)* |
| Starlark guards (Loom) | The reserved `{reads, starlark}` guard escape hatch needs a verified-pure sandbox (the declarative grammar covers current flows). | ★ | M | 📐 **awaiting-Andrew (ratification) — 2026-06-29** — design: [`loom-starlark-guards-design.md`](../../implementation-artifacts/loom-starlark-guards-design.md). **No frozen-contract change** (builds to Contract #10 §10.5, which already specifies the `{reads, starlark}` shape + `def guard(subject)->bool` + "the same verified-pure sandbox the Processor uses"). **Shape:** a pure, deterministic, frozen-`subject` predicate evaluated by a sandbox that is a **strict read-only subset** of the Processor's proven runner (`internal/processor/starlark_runner.go`) — NO `kv.Read`/clock/`nanoid`/mutations/`load`; reads only the aspects named in `reads` (the no-scans bound, mechanically identical to the declarative path resolver); returns `bool`. Cursor-rebuild determinism (§10.6) preserved structurally. **One decision for Andrew (a module-boundary fork, designed through, NOT an architectural fork): where the shared sandbox lives** — Option A (recommended) extract a dependency-free leaf `internal/starlarksandbox` imported by Processor + Loom (single source of truth for the security boundary; AI-authored-capabilities Fire 4 reuses it — that ratified design names this item as its prerequisite), which widens Loom's AC-asserted "imports only substrate + stdlib" boundary by one leaf utility (intent = "no engine coupling" preserved; a doc/comment edit, no contract); Option B Loom re-implements its own wrapper (boundary-pure, precedented by `guard_eval.go`, but duplicates a security-critical sandbox 2–3×). Self-adversarial pass folded in (§7); recommended `bmad-party-mode` pass on the determinism/replay boundary before Fire 2. 3 fires (Fire 1 sandbox extraction = full 3-layer security review, ships value alone → Fire 2 light up the Loom hatch + determinism/replay golden test = full 3-layer → Fire 3 retire the spike, optional), each independently shippable + green; **does NOT wait on D1/Vault** and unblocks AI-authored-capabilities Fire 4. **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds Fire 1).** |

### Read-model / projection maturity
| Item | What it is | Imp | Size | Status |
|---|---|---|---|---|
| **`nanoIdFromKey` cypher function (D1 prerequisite) — LATTICE lane** | A targeted, **fail-closed** string function added to the Refractor auth-plane cypher engine (`evalFunctionCall`, `internal/refractor/ruleengine/full/executor.go`): `nanoIdFromKey(vertexKey)` → the bare NanoID (the 3rd segment of `vtx.<type>.<id>`), validating the vertex-key shape and **erroring/null on a non-vertex key** (aspect/link/malformed) so it can never emit a wrong anchor. **Why:** the auth-plane engine has *no* string function (only `collect/count/max/min/levenshtein` + arithmetic) and *no* list-indexing (no subscript AST node), so a lens can't strip the `vtx.<type>.` prefix — which is why D1.1 shipped the full key as an interim. With this, `cap-read.*` + protected business lenses project **bare-NanoID** anchors (the §6.14 opaque-token rep — Andrew, 2026-06-29). ~15 lines mirroring `levenshteinDist` + a unit table (valid→NanoID; aspect/link/malformed→fail-closed); no parser/AST change. **Andrew decision: targeted `nanoIdFromKey`, not a general `splitPart`** (encodes the Contract #1 key shape in one validated place; no magic index; fits the engine's minimal/safe posture). | ★★★ | XS–S | ✅ **Done (d85450d, CI watch in progress)** — `nanoIdFromKey(vertexKey)` added to the auth-plane engine's `evalFunctionCall` (core logic in a pure `nanoIDFromVertexKey` helper, mirroring `levenshteinDistance` — unit-tested directly): a well-formed `vtx.<type>.<id>` key → the bare NanoID; an **aspect** key (4-segment), a **link** key, or any malformed/empty-segment input **ERRORS** (fail-closed — never emit a wrong anchor; a nil arg → nil). Targeted, not a general `splitPart` (per Andrew). The base **cap-read lens revised**: `anchorId: identity.key` → `anchorId: nanoIdFromKey(identity.key)`, projecting the §6.14 bare-NanoID opaque-match-token rep; the §6.14 conformance test now asserts the bare NanoID. **Adversarial self-review caught + fixed** a stale `internal/gateway/auth/auth.go` comment that conflated the actor id (full key) with `anchorId` (now bare NanoID) — the exact §6.5-vs-§6.14 conflation `d4dc388` warned against. **No frozen-contract change** (the §6.14 bare-NanoID refinement is **now committed** — `7aaa79b`, Andrew). **Pure projection, zero enforcement** (no consumer reads cap-read docs yet) — the prerequisite the D1.3 RLS read boundary matches against. Gates green: build, `make vet`, golangci 0, STRICT-conventions 0, short unit table + NATS §6.14 conformance + bootstrap full-graph seeders + refractor capability e2e (live-activate the revised lens) + gateway/auth. **Thorough-lead + adversarial self-review** (scaled from 3-layer per the D1.1/D1.2 zero-enforcement precedent; the full 3-layer is reserved for D1.3 enforcement turn-on). |
| **First Postgres read-model + read boundary (D1.3 enabler)** — **Fire 1 (Refractor RLS-provisioning) — LATTICE lane** | The reusable *platform* half of the D1.3 enabler: at **protected-lens activation**, Refractor generates the `CREATE TABLE` + `ENABLE`/`FORCE ROW LEVEL SECURITY` + the §6.14 set-membership RLS policy **from the lens spec** (brainstorm #38's RLS-policy generator), and provisions the seq-guarded `actor_read_grants`. Serves clinic + every future protected model, not just loftspace. (Fires 2–3 — the loftspace consumer — are the **Verticals**-lane row.) | ★★★ | M | ✅ **Andrew-ratified (2026-06-29)** — design: [`first-postgres-read-model-and-read-boundary-design.md`](../../implementation-artifacts/first-postgres-read-model-and-read-boundary-design.md). **LANE: Lattice Steward** (platform `internal/refractor`; the split keeps the two streams' code disjoint — Verticals must not edit core Refractor). Generates table-DDL + `FORCE RLS` (H3: missing policy ⇒ deny-all) + the set-membership policy from the protected lens spec; seq-guards `actor_read_grants` (H4: stale-seq upsert can't resurrect a revoked grant). **No frozen-contract change** (builds to §6.14 committed; a `refractor.md` note only). Independently shippable + green (`POSTGRES_TEST_DSN` tests: FORCE-RLS deny-all + stale-seq-no-resurrect proofs) — **no vertical needed**, so the handoff to Fires 2–3 is light. **Full 3-layer (security-plane).** **🏗️ Fire 1a SHIPPED (5788e32, CI watch in progress) — the RLS-provisioning primitive (dormant; zero enforcement consumer yet).** `internal/refractor/adapter/rls.go`: **`BuildProtectedTableDDL(table,keyCols,body)`** → `CREATE TABLE IF NOT EXISTS` + `authz_anchors text[]` + `projection_seq` + `ENABLE`/`FORCE ROW LEVEL SECURITY` + the §6.14 set-membership policy (a row visible iff the actor holds a **live** grant for ANY of its anchors; actor from `current_setting('lattice.actor_id', true)`, NULL-safe deny); FORCE-RLS ⇒ a policy-less table denies all rows (**H3**, fail-closed). **`PostgresGrantWriter`** provisions the shared `actor_read_grants` table + applies the §6.14 monotonic-seq guard on `UpsertGrant`/`RevokeGrant` so a stale CDC replay can neither downgrade a fresh grant nor **resurrect a revoked one** (**H4**); `grant_source` keeps producers disjoint. Tests: pure DDL-shape + validation table; `POSTGRES_TEST_DSN` integration proving **H3** (FORCE-RLS deny-all, incl. a policy-less table denying all under a non-superuser `SET LOCAL ROLE` — the dev superuser bypasses RLS) and **H4** (stale upsert no-resurrect; per-`grant_source` isolation; stale revoke no-op). All gates green (build/vet/golangci 0/STRICT-conventions 0/`-race`). `docs/components/refractor.md` "Protected read-model provisioning (D1.3)" note added. **Review: thorough-lead + adversarial self-review** (scaled per the D1.1/D1.2 dark-seam precedent — dormant scaffolding, zero enforcement consumer, no live activation path yet; **the full 3-layer is reserved for the Fire-3 enforcement turn-on** in loftspace-app, per the design §7 + D1's "enforcement turn-on gets the full 3-layer"). **✅ §6.14 `is_deleted` tombstone clarification RATIFIED + COMMITTED by Andrew (0b3ec29)** — the grant table's seq-guarded soft-tombstone (revoke retains the row+seq; policy/lookup filter `AND NOT is_deleted`) is now contract-blessed. **✅ Fire 1b SHIPPED (45e8d98, CI watch in progress) — the activation wiring; the Lattice-lane half of D1.3 is now COMPLETE.** A postgres lens spec declares its read-path posture in `targetConfig`: **`protected: true` + `columns:[{name,type}]`** → `buildAdapter` provisions `actor_read_grants` + the RLS-locked business table and wraps the adapter in a **`ProtectedAdapter`** (encodes `authz_anchors`/`text[]` cols `[]any`→`[]string` so they land as Postgres arrays, not JSONB — the base adapter untouched, every existing pg lens byte-identical); **`grantTable: true`** → projects to `actor_read_grants` via the seq-guarded **`GrantWriterAdapter`** (table+key `actor_id/anchor_id/grant_source` default from the platform; `Delete`→`RevokeGrant`; not a Truncater — shared table); **`public: true`** → auditable opt-out (no RLS); protected XOR public. **Adversarial self-review fix: the RLS policy is `FOR SELECT`** (was `FOR ALL`, whose `USING` doubles as the INSERT `WITH CHECK` and would block the trusted no-actor projector's writes) — governs reads only; FORCE-RLS still deny-alls reads on an un-policied table (H3 intact). Tests: translateSpec protected/public/grantTable/columns + validation; adapter key-extraction + array-encoding; **an end-to-end `POSTGRES_TEST_DSN` seam proof** (protected row via `ProtectedAdapter` + grant via `GrantWriterAdapter` → RLS shows it only to the granted actor; revoke hides; stale re-grant can't resurrect). All gates green (build/vet/golangci 0/STRICT-conventions 0/`-race`; bootstrap + cmd/refractor). Dormant — **no protected lens ships yet**; thorough-lead + adversarial self-review (D1.1/D1.2/Fire-1a dark-seam precedent; the full 3-layer lands with the Fire-3 enforcement turn-on). **🎯 The platform seam is COMPLETE and ready for the Verticals Steward (Fires 2–3: the loftspace `read_lease_applications` protected lens declaring `protected:true`+columns, the `cap-read.residence` grant lens declaring `grantTable:true`, and the Option-A read boundary in `cmd/loftspace-app`).** |
| **[Core/pkgmgr] Package-declared protected/grant Postgres lenses — the D1.3 Fire-2 declaration surface** *(filed by the Verticals Steward, 2026-06-29 — blocks D1.3 Verticals Fire 2)* | Fire 1 (1a `5788e32` + 1b `45e8d98`) wired the Refractor **activation** side of protected Postgres read models: the on-wire lens spec `TargetPostgresConfig` carries `protected`/`public`/`grantTable`/`columns`, and `cmd/refractor` provisions the RLS table / `actor_read_grants` + wraps in `ProtectedAdapter`/`GrantWriterAdapter`. **But the package *declaration* side was not extended** — so no package can actually emit that on-wire posture: (a) `pkgmgr.LensSpec` (`internal/pkgmgr/definition.go:349`) exposes only flat `DSN`/`Table`/`QueryTimeout` — **no `Protected`/`Public`/`GrantTable`/`Columns`**; (b) `lensSpecBody` (`internal/pkgmgr/build.go:295`) serializes a postgres `targetConfig` of only `{dsn,table,key,queryTimeout}` — it drops the posture. **Second gap — DSN injection:** a package can't hardcode a deployment DSN, yet `translateSpec` requires `cfg.DSN != ""` verbatim; today only the bootstrap `contract_view` lens reads `REFRACTOR_PG_DSN` (in Go, `internal/refractor/lens/bootstrap.go`). A package protected/grant lens needs a convention — most likely **empty DSN ⇒ Refractor defaults to `REFRACTOR_PG_DSN`** at activation (so the package declares posture + columns, not a connection string). **Scope (small, `internal/*` = Lattice lane):** extend `pkgmgr.LensSpec` + `lensSpecBody` to carry+serialize `protected`/`public`/`grantTable`/`columns` (with the protected-XOR-public + grant-not-protected validation mirroring `translateSpec`), and resolve the package-lens DSN from env when unset. **No frozen-contract change** (§6.14 committed; builds to the Fire-1 on-wire shape). Serves clinic + every future protected package model, not just loftspace. **The design under-specified this** — it framed Fire 2 as pure "package work," but the package→on-wire link was never built. **🎯 Ready for the Lattice Steward; it unblocks Verticals Fire 2.** | ★★★ | S | ✅ **Done (c1a8901, CI watch in progress)** — `pkgmgr.LensSpec` gains `Protected`/`Public`/`GrantTable`/`Columns` (`[]PostgresColumn{Name,Type}`); `lensSpecBody` serializes them into the postgres `targetConfig`. A **GrantTable lens with no declared key omits `key`** so Refractor applies the platform grant composite (`actor_id,anchor_id,grant_source`) — otherwise the `["key"]` default would mis-key the grant writer. New **`validateLensReadPath`** rejects an incoherent posture at build/install (posture only on postgres; protected XOR public; grant≠protected) — **defense in depth** mirroring `translateSpec`'s fail-closed activation checks, so a dropped/misdirected posture can't world-publish a model the author believed protected. `validateLensAdapters` no longer requires `DSN` for postgres (env-resolved) and lets a grant lens default its Table. **DSN injection:** `translateSpec` resolves an empty postgres `DSN` from **`REFRACTOR_PG_DSN`** at activation (mirroring the bootstrap `contract_view` lens) — so a package declares posture + columns, not a connection string; **fail-closed** (empty DSN + unset env = hard error, no silent dev-localhost default, unlike bootstrap's demo fallback). The YAML file-authoring path (`lens.Parse`) is untouched (still requires an explicit DSN). **No frozen-contract change** (builds to §6.14, committed). Existing packages byte-identical (zero postgres package lens ships yet; nats-kv serialization unchanged). Gates green (build/vet/golangci 0/STRICT-conventions 0; pkgmgr + refractor + bootstrap + cmd/refractor). **Dormant** — no protected lens activates until Verticals Fire 2 declares one; thorough-lead + **adversarial self-review** (D1.1/D1.2/Fire-1a/1b dark-seam precedent: dormant scaffolding, zero enforcement consumer; the RLS boundary itself was 3-layer-reviewed in Fire 1a/1b — full 3-layer reserved for the Fire-3 enforcement turn-on). **🎯 Verticals Fire 2 is now UNBLOCKED** (the loftspace `read_lease_applications` protected lens + `cap-read.residence` grant lens can declare the posture). |
| **[Core/bootstrap] Populate Postgres `actor_read_grants` — the cap-read self-anchor GrantTable projection (D1.3 Fire-3 prerequisite)** *(filed by the Verticals Steward, 2026-06-29 — blocks D1.3 Verticals Fire 3, the read-enforcement turn-on)* | The protected read model **Verticals Fire 2 just shipped** (`leaseApplicationsRead`, `446567e`) carries `authz_anchors=[applicantNanoID]`, and at activation Refractor provisions the `actor_read_grants` table (a protected lens's `buildAdapter` ensures it — `cmd/refractor/main.go:242`). **But nothing POPULATES it:** grep-confirmed **no lens declares `GrantTable:true`** anywhere (only pkgmgr tests), and the shipped base **`cap-read.<actor>` self-anchor lens projects to the NATS-KV `capability` bucket** (`bootstrap.CapabilityReadLensDefinition`, `TargetBucket:"capability"`), **never to Postgres**. So at Fire-3 read time `actor_read_grants` is **empty** → the §6.14 set-membership RLS policy matches nothing → **every protected read is denied** (correct fail-closed, but it means "A sees only A" can't be proven: A sees *nothing*). **The gap (Lattice lane, `internal/bootstrap`):** the base cap-read self-anchor needs a Postgres **GrantTable** projection so each actor's own NanoID lands in `actor_read_grants(actor_id=NanoID, anchor_id=NanoID, grant_source=…)` — the grant the applicant-self milestone's RLS matches. **Design decision for the Lattice Steward (impl-level, not a fork):** either (a) add a sibling bootstrap GrantTable lens projecting the self-anchor to Postgres (keeping the NATS-KV `capability` projection for any write-path-style reader), or (b) retarget/duplicate the cap-read base lens onto the postgres GrantTable adapter. The `GrantWriterAdapter` + seq-guard + `actor_read_grants` provisioning all **already exist** (Fire 1b `45e8d98`); this is the missing *producer* that writes into them. **No frozen-contract change** (§6.14 committed; the grant-table shape + seq-guard are blessed). **Full 3-layer (security-plane — it's the grant source RLS trusts).** Without it, Verticals Fire 3 (the `cmd/loftspace-app` authenticated reader + the "A sees only A" e2e) cannot turn enforcement on. *(The loftspace `cap-read.residence` package grant slice — for the landlord/residence audience — is a **separate, later** increment in the Verticals lane and additionally needs a landlord→unit ownership link loftspace-domain doesn't model yet; this row is only the **core self-anchor** half that the applicant-self milestone needs.)* | ★★★ | S–M | ✅ **Increment 1 DONE (f04f331, CI green)** · Andrew-ratified (2026-06-29) — design: [`d1-grant-population-and-sequencing-design.md`](../../implementation-artifacts/d1-grant-population-and-sequencing-design.md). **The keystone shipped:** a new primordial **`capabilityReadGrants`** Postgres GrantTable lens (the Postgres twin of `capabilityReadLens`) projects each identity's self-grant `(actor_id == anchor_id == nanoIdFromKey(key), grant_source 'cap-read')` into `actor_read_grants` — the grant RLS matches so A reads only A's protected rows. The bootstrap lens-seeder learned the read-path posture (`LensDefinition` Adapter/Table/GrantTable/Protected/Columns + `makeLensSpecBody` postgres branch, byte-identical to the package surface; nats-kv path untouched); bootstrap file v12→v13; `make up` runs Refractor with `REFRACTOR_PG_DSN` so it activates + populates live. Tests: the literal cypher projects one self-grant per identity through the full engine (no Postgres); seeder serialization pinned; lease-convergence e2e green (the new primordial postgres lens is harmlessly translate-skipped in a no-Postgres harness). **Self-review CORRECTED the design's "retraction is free" claim** — `AnchorDeleteResult` can't resolve this lens's `nanoIdFromKey(...)` function-call key and emits 1 column where a GrantTable delete needs the 3-column composite, so grants accumulate monotonically; a tombstoned identity's self-grant lingers but is inert (self-only; deactivation gated at the D1.2 JWT boundary). **Filed the retraction follow-up** (row below). **🎯 Verticals D1.3 Fire 3 is now UNBLOCKED** — `actor_read_grants` is populated; the read boundary builds against a proven grant table. **Designed to STOP the D1 thrash, not feed it.** Root-causes the ping-pong: §6.14 mandates every cap-read lens *also* project to Postgres `actor_read_grants`, but D1.1 shipped only the NATS-KV half — and the D1.3 design §R4 + the Fire-2 filing both *assumed* the self-anchor was already in Postgres (it never was). **Keystone = this row** (Increment 1, Lattice): a base **`capabilityReadGrants` Postgres GrantTable** primordial lens projecting each identity's self-grant `(idNanoID, idNanoID, 'cap-read')` → `actor_read_grants`. **Caught the next bounce + folded it in:** the bootstrap lens-seeder hardcodes `targetType:"nats_kv"` (`primordial.go:1030`) so a primordial Postgres lens isn't expressible — Increment 1 *first* teaches the seeder the read-path posture (the bootstrap analog of the package surface `c1a8901` shipped). Tombstone retraction is FREE (reuses the `AnchorDeleteResult` plain-projection retraction `679fe25`). **No frozen-contract change** (builds to §6.14 as written; multi-Lens note → separate lens). **No fork** (Path A/RLS already ratified). The design also **enumerates Verticals Fire 3's full prerequisite set** (pgxpool, a non-superuser SELECT-only DB role, e2e JWT-minting since `gateway/auth` is verify-only, `SET LOCAL`, delete the weaver-targets leak) so the consumer builds with **zero further cross-lane discovery** — one Lattice fire, then one Verticals fire, strict order, one-way hand-off. **🎯 Ratify → the Lattice Steward builds Increment 1 immediately; then Verticals builds Fire 3 against a proven grant table.** |
| **[Refractor] GrantTable lens grant-retraction on anchor tombstone — the composite-keyed delete path** *(filed by the Lattice Steward, 2026-06-29 — D1.3 Increment-1 self-review)* | `actor_read_grants` accumulates **monotonically**: a tombstoned identity's `cap-read` self-grant (and, later, any package `cap-read.<domain>` grant) is **not auto-revoked**. Root cause: the full-engine anchor-tombstone path `full.Engine.AnchorDeleteResult` (`679fe25`) resolves only a `<anchor>.key`/root-field key into a **single** output column — but (a) a GrantTable lens's first RETURN is typically a `nanoIdFromKey(...)` **function call** it can't resolve, and (b) a `GrantWriterAdapter.Delete` needs the **3-column composite** `(actor_id, anchor_id, grant_source)`, not one column. So no Delete fires on an identity tombstone. **Impact is bounded, not a leak:** a lingering self-grant is self-only (actor == anchor → can never read another actor's rows), and a deactivated/revoked identity is denied at the **D1.2 JWT boundary** (auth + revocation) before RLS is consulted — so the boundary, not the grant row, is the deactivation gate. But the table growing unbounded + a stale grant surviving a re-activated NanoID collision is real hygiene/correctness debt. **Shape:** a GrantTable-aware tombstone path — on an anchor (identity) tombstone, the pipeline emits a composite-keyed Delete the `GrantWriterAdapter` maps to `RevokeGrant(actor, anchor, source, seq)` (the adapter + seq-guard already exist, Fire 1b). Either extend `AnchorDeleteResult` to derive the grant composite for a GrantTable lens, or add a dedicated grant-retraction projection step. **No frozen-contract change** (§6.14 already specifies the seq-guarded soft-tombstone + per-`grant_source` retraction; this *realizes* it on the anchor-tombstone trigger). **Full 3-layer (security-plane — the grant source RLS trusts).** | ★★★ (producer gap CONFIRMED) | S–M | 🏗️ **Fire 1 SHIPPED (d772195, CI watch in progress) — the GrantTable producer (D1.3 unblock).** The full engine now builds the **complete multi-column projection key** (parity with the simple engine): `full.CompiledRule.KeyColumns` threaded from `Rule.Into.Key` at activation (`cmd/refractor`), and `applyReturn` builds the full key map — so the `capabilityReadGrants` GrantTable lens's composite key `(actor_id, anchor_id, grant_source)` reaches `GrantWriterAdapter.Upsert` and **`actor_read_grants` actually populates** (the latent D1.3 Fire-3 blocker: an empty grant table would deny-all every protected read). Producer gap **confirmed by code + proven by test** (the legacy first-item fallback delivers a 1-column key → `GrantWriterAdapter` errors `anchor_id absent` per `TestGrantKeyFields`; the threaded path delivers all 3 — pinned by an engine unit + a pipeline→key proof through `evaluateForEntry`). Activation **fail-closes** on a key column absent from RETURN (simple-engine parity, §6.13). **Only PLAIN lenses threaded** — an **adversarial-self-review-caught regression**: an envelope lens (actor-aggregate `cap.<actor>` / operation-role index) synthesizes its key at write time, so its `Into.Key=["key"]` is *not* a RETURN alias; threading/validating it would have **failed the entire auth-plane's activation in production** (bootstrap/refractor tests don't exercise `cmd/refractor` startPipeline, so it was test-invisible) — gated out + pinned by a test. **Impl-level decisions ratified by the lead (Winston)** — no frozen-contract change (builds to §6.14 composite key), no architectural fork — so the design's 📐 awaiting-Andrew was promoted to build-ready per Steward rule #2. **Review scaled from the design's "full 3-layer" → thorough-lead + adversarial-self-review (explicit + overridable):** zero-enforcement producer writing inert self-grants (`actor==anchor`), no live authz consumer, mirrors the heavily-reviewed simple engine + `679fe25`; the one real risk was caught + fixed. **Live-verify deferred** (no local Postgres stack this fire) — covered by the engine+pipeline+adapter unit chain and the bootstrap full-graph seeders (which live-activate the grant lens through the full engine, green). **Note: the Verticals stream shipped D1.3 Fire 3 (`ce57c10`) — the RLS read-enforcement turn-on — in parallel; this producer is what makes its grant-table-backed enforcement real.** **Gates green:** build, `make vet`, golangci 0, STRICT-conventions 0, `-race` on full+pipeline, refractor/bootstrap/cmd-refractor `go test` (verify-kernel deferred to CI — shared-stack collision; the bootstrap seeders cover the grant-lens activation path). `docs/components/refractor.md` note added. **✅ Fire 2 SHIPPED (faa3aec, CI watch in progress) — the composite-keyed anchor-tombstone retraction.** `full.Engine.AnchorDeleteResult` generalized from "resolve the first RETURN item" to "resolve **every** key column read-free against the tombstoned anchor": it builds a read-free executor (`coreKV == nil`) binding the anchor var to the tombstoned vertex and evaluates each key column's RETURN expression via the **same `evalExpr` the upsert path uses** — so a literal, an anchor `.key`/root field, or a pure function over them (this lens's `nanoIdFromKey(identity.key)`) resolves identically with no re-scan, and an identity tombstone now emits the `(actor_id, anchor_id, grant_source)` composite Delete the `GrantWriterAdapter` maps to `RevokeGrant` (the §6.14 seq-guarded soft-tombstone, stamped at the tombstone CDC seq → a stale lower-seq re-upsert cannot resurrect). A column needing a Core-KV point-read (an aspect access on the now-deleted vertex) reports unresolvable (`errCoreKVReadDisabled` guard in `resolveProperty`); a non-RETURN-alias or node-valued column likewise falls through → **a wrong-delete is structurally impossible** (anchor-label match + every key column resolving read-free to a scalar; even a hypothetical mis-resolve targets only the lens's own row for the tombstoned anchor, `actor==anchor`, inert). Single-key lenses byte-identical; the dead `resolveAnchorKeyValue` retired (the unified `evalExpr` path subsumes it). **Review scaled to thorough-lead + adversarial-self-review (explicit/overridable, Fire-1 `d772195` precedent):** zero live enforcement consumer, inert self-grants, mirrors the heavily-reviewed simple engine + Fire-1 upsert path — **full 3-layer reserved for Fire 3** (the live security e2e). Tests: engine unit (composite grant key resolves on identity tombstone; secondary-type / aspect-read / absent-RETURN-alias columns fall through) + pipeline unit (tombstone entry → composite-keyed Delete); the adapter `Delete`→`RevokeGrant`→`is_deleted`→no-resurrect chain is already proven by Fire-1b `TestReadPathSeam_Integration` (local Postgres; CI has no Postgres, so the engine+pipeline units are the CI regression guard). Gates green (build/vet/golangci 0/STRICT-conventions 0/`-race` full+pipeline; bootstrap full-graph seeders live-activate the grant lens through the full engine; refractor/adapter/cmd-refractor `go test`). `refractor.md` retraction note + the `capabilityReadGrants` lens doc-comment updated (retraction now implemented). **Remaining: Fire 3** (security e2e — the ephemeral-stack proof that a revoked-by-tombstone grant no longer passes RLS for its anchor, composed with the D1.3 read boundary; lands from this or the Verticals lane). Design: [`grant-table-anchor-tombstone-retraction-design.md`](../../implementation-artifacts/grant-table-anchor-tombstone-retraction-design.md). **No frozen-contract change** (build-to §6.14: composite key + seq-guarded soft-tombstone + per-`grant_source` retraction). **No architectural fork.** **Grounding surfaced a bigger root cause + a LIKELY D1.3 BLOCKER (flagged for Andrew):** the full engine builds a projection's key map from only the *first* RETURN item, so a multi-column-key full-engine lens (the grant lens is the first one) emits a single-column key → `GrantWriterAdapter.Upsert` errors `anchor_id absent` → by code analysis **the live `capabilityReadGrants` lens never populates `actor_read_grants`** (the *producer*, not just the retraction, is broken; D1 Increment-1's green proofs assert `r.Values` + a manually-keyed adapter test — neither drives the full-engine→composite-key→adapter path). With an empty grant table, D1.3 read enforcement deny-alls every protected read ("A sees nothing", not "A sees only A") — so this likely **does block D1.3 Fire 3**, contra this row's original "does not block" note. Fix = the **root cause** (full-engine multi-column key construction, mirroring the simple engine, threading `Into.Key` onto `full.CompiledRule`): **Fire 1** = the producer/upsert composite key + activation key-validation + **live-verify the populate fails-then-passes** (the D1.3 unblock; full 3-layer) → **Fire 2** = the anchor-tombstone composite-keyed retraction (the filed gap; full 3-layer) → **Fire 3** = security e2e. Self-adversarial pass folded in (§9). **Ratify → the Lattice Steward builds Fire 1 first (it gates D1.3 Fire 3).** |
| **[Refractor] Link-triggered reprojection for plain / GrantTable lenses — the `cap-read.<domain>` relationship-grant producer** *(filed by the Verticals Steward, 2026-06-29 — blocks the D1.3 Verticals landlord/residence increment)* | The package `cap-read.<domain>` grant lenses the D1 design names (loftspace→residence, rbac→roles, clinic→…) that scope a grant to a **relationship** — loftspace's `cap-read.residence` granting a landlord read of *their* managed units' applications — anchor on a **LINK** (loftspace-domain's `lnk.identity.<l>.manages.unit.<u>`, class `manages`, shipped Increment 1 `5b672b1`). **But a GrantTable lens runs on the PLAIN write path (no `ActorEnumerator`), and the plain pipeline acks-and-skips every `KindLink` CDC event** (`internal/refractor/pipeline/pipeline.go:496-506` — *"link mutation observed but no handler registered"*; same for `KindAspect`, L483-495). Only **actorAggregate** lenses get the endpoint-seeded link fan-out (`evaluateLinkFanOut`, `pipeline/evaluate.go:334-398`), installed solely by `InstallActorAggregate` (`projection/driver.go:186`) — which also requires an `OutputDescriptor` + one-row-per-anchor NATS-KV/aggregated output, **incompatible** with GrantTable's flat composite `(actor_id, anchor_id, grant_source)` rows (multiple managed units ⇒ multiple grant rows per landlord). **Net: a `cap-read.residence` grant lens anchored on the `manages` link can never populate live** — `AssignUnitOwner` emits **only** the link (no vertex/aspect change — verified in `ownership.go`, the op script's mutations are `op:create`/`op:update` on the link key alone), so there is **no CDC trigger** a plain GrantTable lens reacts to → the grant table stays empty for residence → RLS denies the landlord every protected row. **Verified by the Verticals Steward (2026-06-29):** plain-pipeline `KindLink`/`KindAspect` ack-skip (code); no existing lens is both `GrantTable` and `actorAggregate` (grep); the base `capabilityReadGrants` works **only** because it is self-anchored on the identity **VERTEX** (vertex-CDC-triggered) — its link-independence is exactly why it populated. **Shape (impl-level, no fork):** either (a) extend the plain pipeline's `KindLink` path to drive an endpoint-seeded reprojection for a GrantTable lens whose MATCH walks the link — the *populate* twin of the actorAggregate `evaluateLinkFanOut`, with composite-keyed retraction on tombstone (Fire-2 `faa3aec` already built the composite Delete); or (b) support a GrantTable lens running as `actorAggregate` (anchor on the unit/landlord, multi-row composite output to `actor_read_grants`). **Sharper, higher-priority instance of the "Link-tombstone re-projection" row below** (that one is ★ *"current approaches work"* + tombstone-only; THIS also needs populate-on-link-**create** and **fully blocks a ratified D1.3 increment**). **No frozen-contract change** (builds to §6.14 committed). Serves **every** relationship-scoped package grant (loftspace residence, future rbac/clinic relationship grants), not just loftspace — corrects the D1 row's "`cap-read.residence` loftspace — *verticals/package work*" assumption (the *declaration* is package work; the *reprojection trigger* is this missing platform primitive). **Full 3-layer (security-plane — the grant source RLS trusts).** Grounding: `internal/refractor/pipeline/pipeline.go:481-511`, `pipeline/evaluate.go:334-398`, `projection/driver.go:152-202`, `packages/loftspace-domain/ownership.go`. | ★★★ | M | 📋 |
| Elasticsearch target adapter | A third lens target adapter (only NATS-KV + Postgres ship; no consumer yet). | ★ | M | 📋 |
| Negative / filter-retraction projection | True "emit-only-when-violating" (Weaver targets currently project one row per candidate with a `violating` flag, avoiding retraction work). | ★ | M | 📋 |
| Link-tombstone re-projection · cross-instance latency rollup | Two projection edge-cases / observability gaps (current approaches work). | ★ | S each | 📋 |
| **Full-engine lens re-projects a tombstoned vertex when its keyed aspect survives** *(Refractor — PO-routed, Clinic)* | **Filed 2026-06-27 (Vertical PO, LIVE + source-grounded). A soft-deleted vertex keeps projecting into any full-engine lens that keys on an aspect rather than root data.** Verified live on the shared stack: `TombstoneProvider` + `TombstoneAppointment` both committed (`isDeleted:true` confirmed in Core KV via Loupe `/api/vertex`) yet the tombstoned provider stayed in `/api/providers` (the `clinicProviders` roster) and the tombstoned appointment stayed `scheduled` in `/api/appointments` (the `clinicAppointments` lens). **Root cause (grounded):** the full engine's `fetchNode` returns nil for a soft-deleted *secondary* node (`executor.go:484`), but the **anchor node fed from the CDC event bypasses that filter** (`pipeline/evaluate.go` `executeFullForActor` — the `entry.IsDeleted` short-circuit at L88 fires **only** for actor-aware capability lenses with an `ActorEnumerator`; a plain projection lens re-executes the cypher with the tombstoned anchor's props). Because clinic lenses match on a surviving aspect (`MATCH (pr:provider) WHERE pr.profile.data.fullName <> null` — the root tombstone does **not** cascade to `.profile`/`.demographics`/`.schedule`), the WHERE still passes → the row is **re-upserted**, not deleted. The simple engine deletes correctly on a node tombstone (`pipeline_test.go TestPipeline_Delete`); the full-engine aspect-presence path does not. The clinic package already documents the underlying "aspects linger" hazard, and the **op path** compensates by gating on the vertex `isDeleted` (`clinic-domain/ddls.go:1124`) — but the **read/lens path has no equivalent**: no lens in the repo filters `isDeleted`, and a P5-clean app can't paper over it (lens read-model rows carry no liveness field; reading Core KV to learn it violates P5). **Owner's design call (Refractor/full-engine seam):** treat a full-engine driving-node root tombstone as delete/skip (mirror `fetchNode` + the simple engine at the anchor) **or** expose a documented root-liveness predicate to lens WHERE. Adjacent to the link-tombstone re-projection row above but distinct (that's link tombstones; this is a vertex root-tombstone with a surviving keyed aspect). Blocks the Clinic "tombstoned entities linger" vertical item. | ★★ | S–M | ✅ **Andrew-ratified (2026-06-27)** — design: [`refractor-full-engine-anchor-tombstone-retraction-design.md`](../../implementation-artifacts/refractor-full-engine-anchor-tombstone-retraction-design.md). **Grounding sharpens the root cause:** the backlog's "re-upsert because the surviving `.profile` keeps `WHERE` passing" is **not** the mechanism — the full engine's `fetchNode` *does* filter the tombstoned anchor (`executor.go:484`) and `ExecuteWith` re-scans Core KV ignoring event props, so it returns **zero rows** for the anchor (`TestExec_SoftDeleteFiltered` asserts `require.Empty`) but **never a Delete** → the prior row is never retracted. Fix = the **non-actor twin** of the actor-aware tombstone shortcut (`evaluate.go:88`), mirroring the simple engine's `deleteResult`: a root tombstone of a plain-projection lens's **anchor** emits a Delete keyed by the anchor's key column; a *secondary*-node tombstone re-executes (dependent fields null, row preserved). New `Engine.AnchorDeleteResult` (AST-only) + one `evaluateForEntry` branch. **No frozen-contract change** (lens retraction lives in refractor.md, not `docs/contracts/*`). Fixes **every** full-engine plain lens (clinic/loftspace/lease-signing/objects-base), not just Clinic. **One scope decision for Andrew** (not an architectural fork): close the root-tombstone case now (rec) vs. also chase the general `WHERE`-flip/aspect-deletion retraction (→ defer to the existing "Negative / filter-retraction projection" row). 3 fires (Inc 3 = bridge-only, not built). **Scope confirmed by Andrew:** root-tombstone now; general retraction deferred to the Negative/filter-retraction row. **✅ Fire 1 + doc SHIPPED (679fe25):** `full.Engine.AnchorDeleteResult` (AST-only — anchor = first `MATCH` node label; key = first `RETURN` item, robust `<anchor>.key`) + the `evaluateForEntry` plain-projection tombstone branch (the non-actor twin of `evaluate.go:88`); a secondary-type tombstone returns `ok=false` → re-execute (no wrong delete — structurally impossible). Engine table test (7 cases + nil guards) + pipeline test (live upsert → anchor tombstone Delete on the same key → secondary-type tombstone emits no Delete); `docs/components/refractor.md` "Anchor-tombstone retraction" note. All gates green (build/vet/golangci/STRICT-conventions/`go test ./internal/refractor/...`), CI green. **Remaining (Fire 2 §5.3, optional):** an ephemeral-stack clinic e2e (`CreateProvider`→`TombstoneProvider`→gone) as the executable PO-reproduction proof — exercises Clinic-vertical territory, so it can land from either stream; the unit+pipeline tests already prove the mechanism. The Clinic *"tombstoned entities linger"* vertical item is now unblocked (the platform fix is live in `main`). |

### Refinements & ops
| Item | What it is | Imp | Size |
|---|---|---|---|
| **CI pipeline speed (continuous)** *(agentic-ops / CI)* | Make CI **faster without weakening any gate** — owned continuously by the **Whetstone** (`ci-whetstone`). **Matrix split DONE (1443109): single serial job (5m57s) → 4 parallel jobs (lint-build · stack-gates · convergence · unit), 2m31s, all gates intact.** Default `setup-go` Go caching already on. **`unit` parallelized (07f3824): weaver tests t.Parallel() → unit test step 140s→87s; `unit` job no longer the long pole.** **`convergence` parallelized (ce2086f): lease-convergence e2e tests t.Parallel() → convergence job 139s→107s; overall wall-clock ~139s→107s.** Remaining levers: **convergence (107s) and `unit` (95s) are now near-co-equal long poles** — convergence's floor is EagerReopen's ~80s eager-reopen freshness wait (irreducible without retuning the deliberately-floored 40s short window); **loom (55s) / bridge (44s)** are the next slow serial packages in `unit` (same t.Parallel() treatment); redundant stack spin-ups; flake-hunting (history clean — 1 real failure in last 60, no re-run flakes). **Every gate still runs; flakiness never rises; proof = measured CI wall-clock ↓.** **EagerReopen freshness window halved (89a9842): leaseshortwindow window 40s→25s + SteadyState drain cap 30s→15s (test-only; prod stays 5m) → convergence job ~112s→79s (−33s), no longer the pole.** This **superseded the prior "25s forbidden" note** — that call kept the 30s drain cap (25<35 would violate `window>drain+settle`); this fire tightened the cap to 15s (25>20, same 5s margin) and re-measured SteadyState at ~6s flat across GOMAXPROCS=1/2/4 (NOT contention-sensitive — gated by the fixed 5s settle, not CPU; the prior 12.6s was an anomaly), so the window-relevant `completedAt→assertion` span has a ~4× margin. **Current state (post-89a9842): `unit` (~107s) is the new pole; convergence ~79s (~28s below); stack-gates ~62s; lint-build ~48s.** **Next lever (now provable): `internal/loom` (55s) + `internal/bridge` (44s) `t.Parallel()` in `unit`** — both have fully isolated per-`t` fixtures (random port + `jsstore.Dir(t)`), same safe treatment as the weaver (07f3824) + lease-convergence (ce2086f) fires; parallelizing them drops `unit` toward ~50-70s, BELOW the new ~79s convergence floor, so it now moves overall wall-clock (the prior blocker — `unit` already under the cap — is gone). Setup-go cache is a single immutable go.sum-keyed key shared by all 4 jobs (`convergence` logs "cache hit, not saving") but the tagged compile is only ~10s on CI, so a per-job rotating-key cache nets <10s with cold-start risk — not worth it. **Flake history clean (1 real failure in last 100, no re-run flakes). Every gate still runs; flakiness never rises; proof = measured CI wall-clock ↓.** | ★★ | M (ongoing) |
| Loom / Weaver control-API surfacing (beyond Loupe's needs) | Operator pause/resume + a durable `loom.*` read model beyond what the Loupe blocker covers. | ★ | M |
| **Package version upgrade / DDL hot-reload (F-004)** → 🏗️ **In progress — Fire 1a SHIPPED (cd20ce8) · Fire 1b-i SHIPPED (f0d7980) · Fire 1b-ii SHIPPED (af81120) · Fire 2 SHIPPED (203d2bb) · Fire 3 SHIPPED (7f98d83)** — design: [`package-version-upgrade-design.md`](../../implementation-artifacts/package-version-upgrade-design.md). **Fire 1a (version-independent entity keys, §8.1):** `entityNanoID(name,tag)` at all 8 entity-key sites; the install-op `requestId` keeps the version (so distinct-version installs dedup independently); permission identity tag → `operationType+scope` (no list index) + `validatePermissionIdentityUniqueness` (all 12 shipped packages audited clear → no install breaks); fresh-install behaviour byte-identical (only key *values* change), same-version reinstall still skips (idempotency matched by package **name**, not the derived key → no migration hazard for 1a alone); golden-key + cross-version-stability + reorder-stability unit tests. No new kernel op / no write/read-path change → thorough lead review. **Fire 1b-i (the `UpgradePackage` kernel op, f0d7980):** the privileged `UpgradePackageDDLScript` (§8.6 — mixed create/update/tombstone batch, one atomic step-8 commit, `PackageUpgraded` event w/ per-op counts) + self-description schemas + full bootstrap wiring (DDL meta-vertex + operator `UpgradePackage` permission + grantedBy→operator link; operator capability lens projects it scope:any; `PrimordialVertexKeyCount` 31→34; bootstrap version 11→12; `seedPackageInstallDDL` compensation-inverse parameterized, install/uninstall byte-identical). Kernel-script unit tests over the mixed-op passthrough + every guardrail reject + the legal same-version re-apply; 2 refractor lens-contract tests assert the new platformPermission projects. **Full 3-layer review** (capability plane): all findings LOW/confirmed-safe; the load-bearing **protected-key guard verified to cover both `update` AND `tombstone` path-independently** (`step8_commit.go:334`), so an operator-submitted upgrade cannot touch a `protected:true` kernel/auth root, and the committer derives `isDeleted` from the `op` (not the client document). Seeded-but-unused (no client submits it yet) — verified headlessly via bootstrap e2e + the count-agreement test (verify-kernel mirror); CI stack-gates run verify-kernel + verify-package-* on a fresh v12 kernel. **Fire 1b-ii (the client diff engine, af81120):** `Installer.Upgrade` reads the installed `.manifest.declaredKeys`, rebuilds the new manifest on version-independent keys via `buildManifestBatch` (extracted from `Install`, byte-identical), diffs by key into the create/update/tombstone delta (new\old→create; old\new→tombstone in sorted order; new∩old→update iff the rebuilt logical body differs from the committed one — a provenance-free body-equality skip), carries `createdAt`/`createdBy`/`createdByOp` forward on each update (the committer re-stamps `lastModified*` with the upgrade actor but overwrites the full value, so creation provenance is preserved per §1.3), and submits one `UpgradePackage` op (`requestId = name+from→to`; a no-op diff is reported `Skipped`, never submitted). P2-clean. CLI: `lattice-pkg upgrade <path>` (`ErrNotInstalled` with a hint when no base). Tests on the real meta-pipeline harness (full step-8 commit): not-installed→`ErrNotInstalled`; identical re-upgrade→`Skipped`; version-only bump→exactly the package-vertex+manifest update; full create/update/tombstone diff with `createdAt` preserved + `lastModifiedBy`=upgrade actor; **the deferred §5 adversarial e2e** — a client-submittable `UpgradePackage` targeting a protected kernel/auth root (update AND tombstone) is rejected at step-8 with `ProtectedKey`; plus pure `logicalDocEqual`/`jsonEqual`/requestId-determinism. **Thorough lead review** (scaled down from 3-layer, overridable: the kernel op + load-bearing step-8 guard are unchanged from Fire 1b-i's full 3-layer; this fire is client-side diff logic + CLI + tests, with the capability-plane safety property now proven end-to-end by the adversarial test). Gates: build/vet/golangci-lint/STRICT lint-conventions + `go test -race ./internal/pkgmgr/...` green; CI green. **Fire 2 (the upgrade-aware CLI, 203d2bb):** new `Installer.Apply` dispatches `lattice-pkg install` on installed state — not-installed→fresh create; same-version→skip (today); same-version `--force`→in-place diff-apply (dev refresh); **different version→auto-upgrade** (in-place diff-apply, `ErrVersionMismatch` retired). `--dry-run` (install+upgrade) previews the create/update/tombstone delta + affected keys, submitting nothing. The explicit `upgrade` verb keeps error-if-not-installed (`Apply{RequireInstalled}`). Apply reuses Fire 1b machinery unchanged (fresh→`Install`/InstallPackage op; in-place→the same `UpgradePackage` op + step-8 protected-key guard + diff engine) — **no new commit/guard semantics, P2-clean**. Refactor: the 7 validators → shared `Definition.validateAll`; `Upgrade` preflight/delta-compute/op-submit → `preflight`/`computeDeltaAgainst`/`submitUpgradeOp` shared with Apply (Upgrade byte-identical, tests unchanged). **8 Apply integration cases** on the real meta-pipeline harness (fresh / same-version skip / force in-place update / cross-version auto-upgrade with KV create+update+tombstone+version-bump asserted / **dry-run-does-not-submit** KV-unchanged / dry-run fresh-not-installed / require-installed / force-no-change skip); downstream Install-using suites (rbac/clinic) green confirm the validator refactor is behavior-preserving. **Thorough lead review** (scaled down from 3-layer, overridable: no new kernel-op/commit/guard semantics — the load-bearing step-8 guard + diff engine + `UpgradePackage` op are unchanged from Fire 1b's full 3-layer; this fire is CLI ergonomics + a behavior-parity-proven refactor, with the safety-critical dry-run-no-submit explicitly tested). Gates green; CI green. **Fire 3 SHIPPED (7f98d83):** Makefile dev-loop targets `reinstall-package PKG=packages/<dir>` (diff-apply one edited package in place via `lattice-pkg install --force`) + `refresh-clinic`/`refresh-loftspace` (diff-apply the vertical's packages **and** rebuild/restart its FE binary in one command, no teardown), additive (no existing target touched) + `.PHONY` + quick-ref header; **upgrade-aware docs** — `docs/components/_packages.md` new "Upgrade / in-place dev-loop refresh (F-004)" section (diff-by-key delta, single atomic `UpgradePackage` op, protected-key guard, version-independent keys, the §4 one-time blue-green re-mint on a pre-F-004 stack, the added-entity-needs-fresh-kernel caveat) + upgrade-aware Installation-semantics step + CLI block (`install [--force] [--dry-run]`/`upgrade`) + Known-limitations swaps the **retired** "no in-place upgrade" claim for the real residual (no in-flight-instance pinning, G6); README Development "Dev-loop: apply a package edit without a teardown" subsection. **Makefile + docs only — no platform code.** Verified: `make -n` expands all 3 targets (PKG guard incl.); a live-stack `install --force --dry-run packages/rbac-domain` previewed the expected §4 one-time re-mint (created=43 updated=0 tombstoned=43, nothing submitted); STRICT lint-conventions 0. **Remaining — only the optional Fire 2 live e2e** (a Refractor-reprojection ephemeral-stack `make` target — a confirmation sub-increment; the reprojection mechanism is already covered by the refractor convergence e2e suite + the Fire-1b/2 unit+integration tests, so low marginal value) → **F-004 is effectively complete**; G6 in-flight-instance pinning deferred as a follow-on. **Contract #8 §8.1 (version-independent NanoIDs) + new §8.6 (`UpgradePackage` op) ratified + committed.** **Diff-and-apply upgrade** (re-install at a new version, today a hard `ErrVersionMismatch`; + a same-version edit, today silently skipped) → create-new / update-changed / tombstone-removed in **one atomic Processor batch**, after which the existing Refractor lens hot-reload + DDL-cache invalidation converge (no `make down`). **No architectural fork.** **Load-bearing decision: entity NanoIDs become version-independent** (§8.1) so a surviving lens/DDL keeps its key across versions (in-place update, no orphaned vertices / broken `lensRef`+grant cross-refs). **Discharges the Epic-12 eviction obligation** (`lattice-architecture.md:1245`) via Refractor's existing `MatchChange` rebuild. **Frozen-contract: Contract #8 §8.1 + new §8.6 `UpgradePackage` op — staged UNCOMMITTED in `main`.** 3 fires (Fire 3 = Makefile dev-loop only); G6 in-flight-instance pinning deferred as a follow-on. **Ratification state: 📐 awaiting-Andrew → ✅ Andrew-ratified (then the Lattice Steward builds).** | **In-place re-install over an existing version + DDL-migration semantics (install / uninstall exist; upgrade does not).** **Re-prioritized ★→★★ on grounded demand: this is a recurring dev-loop tax on the implementation agents** ("the shared :7799 binary is stale and not started by this run; a DDL change won't hot-reload"). Two faces, both source-grounded: **(1) same-version reinstall is silently skipped** — `internal/pkgmgr/installer.go:130` sets `Skipped=true` ("already installed") when the package vertex exists at the same `Version`, so an edited lens/DDL (e.g. `packages/clinic-domain/lenses.go`) that keeps its version string **never lands**; **(2) a changed version hard-errors** — `installer.go:136` returns `ErrVersionMismatch` (no upgrade path *over* an existing install). Net: the only way to apply **any** package change is a full `make down && make up-<vertical>` (teardown + fresh install). **Companion facet (Makefile, not pkgmgr):** the vertical FE binary isn't auto-rebuilt/restarted by partial flows — `up-clinic` (Makefile:256) pkills+rebuilds+restarts `bin/clinic-app`, but an agent editing a handler or running only `install-clinic` leaves the prior `:7799` process serving stale code. **Designer scope:** (a) an in-place upgrade verb (bump-version re-install that diffs old→new DDL and applies the delta through the Processor — P2-clean) **and/or** a dev-mode force-reinstall that re-applies same-version DDL; (b) DDL-migration semantics (add/change/remove a meta-vertex/lens/role across versions — tombstone vs. update vs. orphan); (c) a Makefile/dev-loop fix so a vertical's binary + packages refresh in one command. **Possible frozen-contract touch** — package-install is governed by the C3 install contract (`sprint-change-proposal-2026-05-28.md`: "F-004 — version upgrade — now a Processor-routed sequence"); the Designer preps any contract edit **uncommitted** for Andrew. Grounding: `internal/pkgmgr/installer.go` (skip @130 / `ErrVersionMismatch` @136), `cmd/lattice-pkg/main.go:162`, Makefile `up-clinic`/`install-clinic`/`run-clinic-app`, the F-004 reshape note in `sprint-change-proposal-2026-05-28.md`. | ★★ | M |
| **FR28 — role-queue + fallback** (+ FR29 unrouted surfacing) → 📐 **awaiting-Andrew (ratification) — 2026-06-28** — design: [`fr28-role-queue-fallback-design.md`](../../implementation-artifacts/fr28-role-queue-fallback-design.md). **Shape:** a new `lnk.task.<id>.queuedFor.role.<roleId>` role-queue link (alongside the existing `assignedTo identity`; **exactly one** on an open task) + a `ClaimTask` op (any role-holder claims → atomic `queuedFor→assignedTo` swap) + `CreateTask` routing (named assignee-if-available → role-queue fallback → loud `RoutingFailed`). The grant + inbox **fan out to every role-holder** via one additive OPTIONAL-MATCH branch on the existing `capabilityEphemeral` / `myTasks` actor-aggregate lenses (mirrors the shipped `reportsTo` delegation fan-out); `ClaimTask` narrows the grant to the claimant through ordinary reprojection. **The crux:** the write-path no-scans invariant means `CreateTask` *cannot* enumerate a role's members → an empty/unstaffed queue is a **post-hoc** concern, exactly as Contract #10 §10.1 already anticipated — surfaced by a new **`unroutedTasks` Weaver convergence target** (FR29: `violating` row in Loupe + a `UnroutedTasks` Health-KV §5.5 issue), not a creation-time reject. **No architectural fork.** **Frozen-contract: Contract #10 §10.1 (Task vertex) — staged UNCOMMITTED in `main`** (adds the `queuedFor` link + `ClaimTask` + the exactly-one-assignment invariant + the grant fan-out + replaces the "Phase 3 FR28 deferred" para with the landed shape + FR29 monitor); **§6.6 NOT touched** (grant field shape + step-3 matching unchanged — the fan-out is a lens detail). **Decision (Andrew, 2026-06-28): ratify the phasing** — availability (boolean) ships as the Fire-2 MVP, numeric capacity is the flagged Fire-2b follow-on (capacity needs an `openCount` counter the lifecycle ops maintain — the write path can't scan a task set). 3 fires (Fire 1 role-queue+claim = full 3-layer auth-plane; Fire 2 availability routing; Fire 3 FR29 surfacing), each independently shippable + green, with a today-consumer (the LoftSpace/Clinic task workflow). Self-adversarial pass folded in (§10) + a recommended party-mode pass on the grant-narrowing reprojection window before Fire 1. **✅ Andrew-ratified (2026-06-28) — Contract #10 §10.1 edit committed with the ratification.** Ready for the Lattice Steward. | ★ | M |
| **op-vertex pruner + `@every` schedules** → 📐 **awaiting-Andrew (ratification) — 2026-06-28** — design: [`recurring-schedules-and-op-vertex-pruner-design.md`](../../implementation-artifacts/recurring-schedules-and-op-vertex-pruner-design.md). **Grounding reframes the row:** the **op-vertex pruner (#49) is retired** — idempotency trackers expire by NATS per-key TTL (Contract #4 §4.3) and the events-outbox aspect is tombstoned by the outbox consumer on publish, so no op-vertex class accumulates (reconciles the stale `lattice-architecture.md:298`/`:679` `weaver/pruner/` slot). **The real gap is #47 — native `@every` recurring schedules** (the temporal scheduler / "replaces cron"): §10.4 already *reserves* `@every` (Phase 2 ships `@at` one-shot) and *names* a recurring consumer as future. **Shape:** a `substrate.ScheduleEvery`/`CancelSchedule` seam (mirrors the Weaver `@at` leg) + migrate the platform's one real in-process cron — the **Weaver reconciler sweep** (`time.Ticker` → a durable `schedule.weaver.sweep` `@every`) — giving single-fire-across-replicas + an operator-visible, retunable cadence; the sweep *logic* + its `weaver-state`-keyed backoff are untouched (only the trigger moves). **No fork (Andrew, 2026-06-28): the platform is already on NATS 2.14** (`go.mod` `nats-server/v2 v2.14.0`; `docker-compose.yml` `nats:2.14-alpine`), so `@every`/cron work today — the "2.12 floor" was stale docs, now fixed (Contract #4 §4.3 → 2.14 floor; CLAUDE.md gained an authoritative-vendor-sources section). **Frozen-contract: Contract #10 §10.4 specified (`@every` recurrence/cancellation/per-occurrence dedup + the #49-retirement reconciliation) + Contract #4 §4.3 2.14-floor fix — ✅ ratified + committed (2026-06-28).** **No architectural fork.** 3 fires (Fire 1 substrate primitive = full 3-layer; Fire 2 Weaver cron-kill = thorough-lead + focused concurrency-boundary adversarial pass; Fire 3 doc/contract + #49 retirement), each independently shippable + green with a today-consumer. Self-adversarial pass folded in (§10) + a recommended `bmad-party-mode` pass on the single-fire/catch-up/occurrence-dedup boundary before Fire 1. **✅ Andrew-ratified (2026-06-28)** — ready for the Lattice Steward. | ★ | M |
| **Operation/permission discovery via `instanceOf` template** *(enables the envelope-class service refactor)* → ✅ **Andrew-ratified (2026-06-28)** — design: [`instanceof-template-op-discovery-design.md`](../../implementation-artifacts/instanceof-template-op-discovery-design.md). **Fork resolved: take the instanceOf lift (Option A).** **P7 added to `lattice-architecture.md` + Contract #1 §1.5/§1.6 + #2 §2.1 ratified & committed.** Ready for the Lattice Steward (Fire 1 the step-6 instanceOf resolver → Fire 2 Loupe op-discovery confirmation; built **with** the Verticals service-instance refactor consumer — no dead-scaffolding interim; the P7 `lint-conventions` gate ships alongside). **✅ Pre-build §10 adversarial gate RUN + DISCHARGED (Winston, 2026-06-29)** — the permissive-default fail-open + auth-neutrality + cycle/depth boundaries CONFIRMED SAFE (code-grounded in `step3_auth`/`step6_validate`/`ddl_cache`); edge sweep folded **F1 (HIGH: multiple live `instanceOf` → fail-closed to permissive, never pick the admitting DDL — mirrors the `ClassForCommand` ambiguity guard), F2 (terminal DDL must be committed, never in-batch), F3 (OCC-hydrated `instanceOf` reads)** into design §2.3 + §9. **🎯 BUILD-READY — the Steward may now cold-start Fire 1** (with the Verticals consumer). **✅ Fire 1 SHIPPED (cea0c3b, CI green run 28396055390) — the step-6 `instanceOf`-chain governing-DDL resolver.** `resolveGoverningDDL` (`internal/processor/step6_resolve_ddl.go`) walks a fine-grained-class vertex's `instanceOf` chain to the nearest **vertexType** type-authority DDL when the exact `class→DDL` lookup misses, resolving each link from the **in-flight batch → hydrated working set → a single bounded on-demand `lnk.<root>.instanceOf.>` Core-KV read** (lazy: coarse classes hit the exact `Lookup` first, so the walk + any read never run today); bounded `maxInstanceOfHops=4` + visited-set cycle guard; **fail-open** (miss/cycle/over-bound/read-error/corrupt-link → §1.5 permissive default, never a wrong DDL). **Auth (step 3, on `operationType`) + `ClassForCommand` untouched** (the discriminator class never reaches the auth surface — the load-bearing review property; F2 terminal-must-be-committed holds by construction — the DDL cache only sees committed+activated metas). Threaded `HydratedState` into the `Validator` interface (working-set + on-demand `KVReader`). **Full 3-layer review** (Blind / Edge / Acceptance — Acceptance ACCEPT no-gaps) on top of the discharged §10 gate: folded **§9 F1 ambiguity rule** (multiple live `instanceOf` → permissive, never a guessed pick — mirrors the `ClassForCommand` guard, so an extra link can't steer the gate; commit cea0c3b reconciled an initial deterministic-pick to this ratified rule), **batch last-op-wins reconciliation** + committed-link **suppression on tombstone**, and **unmarshal-failure-skips** (a corrupt link envelope is no link). 15-case resolver table (a–e per §6.1 + 7 review-fold-ins: ambiguity→permissive / batch create+tombstone net-dead / batch-tombstone-suppresses-committed / aspectType-meta-terminal / on-demand-read-error fail-open / one-hop classOf terminal / aspect-mutation-walks-parent). Gates: build / `make vet` / golangci 0 / STRICT lint-conventions 0 / full `go test ./...` green (incl. Gate 2 bypass + Gate 3 capadv on embedded NATS); verify-kernel + the `make down/up` stack-gate variants run in CI. **No frozen-contract change** (builds to the committed §1.5/§1.6 + §2.1). **🏗️ Remaining (sequenced WITH the Verticals consumer, not standalone-dark): Fire 2** Loupe op-discovery confirmation test + **the P7 `lint-conventions` gate** (lands after the Verticals refactor retires the `.class`/`.family` outliers, else it fails on the current outliers). **The write-gate lift:** the step-6 `permittedCommands` write-gate resolves a fine-grained-class vertex's governing DDL by walking its `instanceOf` chain (bounded) to the **type authority** (the nearest ancestor that IS a registered DDL / a `vtx.meta.*` DDL), instead of the exact `class`→DDL `Lookup` that today **misses** a fine-grained class and silently falls to the permissive default. Auth (step 3, on `operationType`) and script selection (`ClassForCommand`, on `operationType`) are **unchanged** — only the write-gate's *class→governing-DDL* resolution gains the instanceOf fallback. This decouples a vertex's **discriminator class** (`service.backgroundCheck.instance`) from its **type-authority DDL** (`service`), so one type DDL governs unbounded fine-grained subtypes with **zero new DDLs**. | So a fine-grained subtype can carry its type in the **envelope `class`** without needing a DDL per family×role: let an instance discover its valid ops + permission set by walking `instanceOf → template` (the template = the type/capability authority), instead of gating on its own class/DDL `permittedCommands` (Processor `ClassForCommand`; Contract #2 §2.1 operationType→class). Needs a Processor change + likely a **Contract #2 §2.1 amendment** + a Loupe op-discovery update → **Andrew gate** (prepare the contract edit uncommitted, flag). Conditional enabler for the Verticals "Service-instance modeling" refactor (Andrew's envelope-class directive, 2026-06-28). **Also codify the standing convention there:** *a vertex's type/subtype discriminator is the envelope `class`, never a `.class`/shadow aspect* — a lattice-architecture principle + a `lint-conventions` gate (like P5). service-domain's `.class` aspect + lease-signing's `.family` are the outliers to fix; meta-vertices already follow this (class in the envelope, `.canonicalName` is a name not a type). | ★★ | M–L |
| **Op-time bounded reverse-link / adjacency read** *(retires the key-list-in-aspect guard indexes)* → 📐 **awaiting-Andrew (ratification) — 2026-06-28** — design: [`op-time-bounded-link-enumeration-design.md`](../../implementation-artifacts/op-time-bounded-link-enumeration-design.md). **Shape:** one new Starlark write-path builtin `kv.Links(hubKey, relation)` = a **bounded, fail-closed prefix enumeration** of a vertex's *outbound* canonical links (`lnk.<hub>.<id>.<rel>.>`), wrapping the existing `substrate.KVListKeysPrefix` (already used by object-store-manager on `lnk.object.`). Reads **Core KV canonical links only** — never the Refractor-private Adjacency KV (`lattice-architecture.md:95`), never a lens (P5 preserved). **Two-part insight:** (1) a *bounded* read requires the enumerated hub to be the link **source** (the canonical key puts source in the prefix, target in the suffix — so `lnk.provider.<p>.hasBooking.` is a clean bounded prefix, while the appointment-source `withProvider` would force an O(all-appointments) suffix-scan); (2) the `.bookings` aspect did **double duty** — adjacency-list **and** the OCC serialization lock — so the redesign **splits** them: topology → `hasBooking` links (read via `kv.Links`), lock → a scalar `bookingGuard.epoch` (Contract #1-clean, contended only by bookings). **The one ratification decision** (not a full architectural fork): deliberately **relax the write-path no-scans invariant** by exactly one sanctioned, capped, fail-closed, Core-KV-links-only primitive. **Frozen-contract: Contract #2 new §2.5.1 — staged UNCOMMITTED in `main`.** Retires the clinic `provider.bookings`/`patient.bookings` SET-indexes (the existence-only `unit.leaseApplications` already done via `appliedToUnit` guard-link 3704324; that trick provably can't serve range/overlap guards — distinct class). 2 fires (Fire 1 platform builtin = full 3-layer; Fire 2 clinic consumer ships in the same initiative — **not dead scaffolding**; Fire 3 e2e+lint optional). **✅ Andrew-ratified (2026-06-28) with a revision** (design banner): `kv.Links(hub, relation, direction, cursor, limit)` enumerates **either direction**, bound to the hub's vertex id via a server-side NATS subject filter — `"out"` (hub=source) `lnk.<hub>.<rel>.>`, `"in"` (hub=target) `lnk.*.*.<rel>.<hubType>.<hubId>` (mid-subject `*` wildcards; the draft's "target-side=unbounded" claim was wrong). **`hasBooking` dropped** — the guards enumerate the existing §1.1-correct `withProvider`/`forPatient` links **inbound** (no inverted link, no two-links-per-edge, no §1.1 violation — there was no committed violation, only the draft's proposed one). **Paged** (cursor/limit), not fail-closed-capped — high-degree hubs (service-template ← many instances) page. **Lazy** (call-time, never pre-hydrated). New substrate `KVListKeysFilter(bucket, filter)` (wildcard, paged). Serialization `bookingGuard.epoch` kept. Contract #2 §2.5.1 rewritten to match — **✅ ratified + committed (2026-06-28)**. Ready for the Lattice Steward (Fire 1 builtin = full 3-layer; Fire 2 clinic consumer; Fire 3 e2e + optional lint pairing-guard). | The Starlark op write path is **known-key-reads-only** (no scans; `TestPackage_NoScans`), so a guard that needs the *set* of a vertex's neighbors — not single-key existence — currently denormalizes that adjacency into a **key-list aspect** (`provider.bookings`/`patient.bookings` = `{appts:[vtx.appointment.<id>,…]}`, plus the now-being-retired `unit.leaseApplications`), i.e. **relationships (keys) stored in aspect data — a Contract #1 violation + a paper-over.** Add a **bounded, OCC-snapshotted op-time reverse-link/adjacency read** (e.g. `kv.Links(targetKey, relation)`) so a guard enumerates neighbors by walking the canonical links (`withProvider`/`forPatient`) and range/overlap-checks them — no key-list aspect. Careful platform change (OCC-snapshot revision; the write path must NOT read the lagging adjacency read-model; cost/determinism on hot vertices — the reasons known-key-reads exists) → likely a **Contract amendment = Andrew gate** (prepare uncommitted, flag). **Retires the clinic `.bookings` SET-indexes**; the existence-only `unit.leaseApplications` is handled separately by a guard LINK (no platform dep). **Provenance (honest):** this primitive was *named* inside the now-✅ verticals rows (appointment-conflict, concurrent-application) but **never filed as Lattice demand — it was papered over** with the key-list aspects and the rows closed. This row is the corrected filing. Pairs with the P7 convention: *relationships live in links, never in aspect/root data.* | ★★ | M–L |
| Loupe agent-activity console *(Loupe)* | 🚧 **Andrew-gated (shelved 2026-06-25)** — read-seam options rejected; revisit once the dependency map + ops-state data-home mature. The design is retained (`implementation-artifacts/loupe-agent-activity-console-design.md`); **do not design or build now.** *(What it would be: the ops layer atop the live system map — the Steward's queue + work in flight, the L3 contract-review queue, per-agent Health, board state.)* | ★★★ | M |

### Parking lot — very low priority (far, far back)

Real but low-value; the Steward should **not** spend design or build effort here unless Andrew explicitly
greenlights one (Steward triage 2026-06-24 — these were the "ride-along" cleanups that turned out not to be
clean small wins).

| Item | Why it's parked | Imp | Size |
|---|---|---|---|
| multi-aspect atomic OCC for `UpdateMetaVertex` | `meta_ddl.go` applies `expectedRevision` to the first changed aspect by design (each aspect has an independent NATS revision sequence); true atomic multi-key OCC needs a substrate per-key-revision primitive — M+, contract-adjacent — for marginal value. | ★ | M+ |
| freshnessExpiry marker tombstone-on-convergence | Per `packages/orchestration-base/mark_expired.go`, a converged marker is read by nothing and harmless; tombstoning buys cleanup not correctness + adds a convergence-edge write — near-zero value, Contract #10 §10.4-adjacent. | ★ | S |
| production freshness-window tuning | A staleness-tolerance vs. timer-churn value judgment — Andrew's call if/when it matters; not worth proactive effort. | ★ | XS |
| **Historical state query (FR51)** — *design kept, build deferred* | Operators query historical state across a time range: an audit/ledger surface + point-in-time graph reconstruction by replaying the immutable record. **Design reviewed & ratified-as-a-design, parked by Andrew 2026-06-27** ([`historical-state-query-design.md`](../../implementation-artifacts/historical-state-query-design.md)): low near-term value (single-user experiment; thin point-in-time-reconstruction consumer) + standing storage cost not worth paying yet. Builds to **reserved** contract seams (no frozen-contract change); two-layer (cheap audit + storage-heavy reconstruction). Revive when a concrete need arises (incident postmortem, a vertical needing time-travel, a real audit requirement). Reconstruction source = **A (delta-archive)** per the design (reconfirm if un-parked). | ★ now / ★★ if a real need lands | M→L |

---


---

## Done log — lattice (newest first)

- **2026-06-29 — [Refractor] GrantTable composite-keyed anchor-tombstone retraction (D1 Fire 2)** (faa3aec, CI watch in progress) — resumed the in-flight row; the originally-filed retraction gap. `actor_read_grants` accumulated **monotonically** — a tombstoned identity's `cap-read` self-grant was never auto-revoked because `full.Engine.AnchorDeleteResult` resolved only the **first** RETURN item into a single column, and the grant lens keys on a `nanoIdFromKey(identity.key)` **function call** + the 3-column composite. **Generalized the delete to "resolve EVERY key column read-free against the tombstoned anchor":** a read-free executor (`coreKV == nil`) binds the anchor var to the tombstoned vertex and evaluates each key column's RETURN expression via the **same `evalExpr` the upsert path uses** — a literal, an anchor `.key`/root field, or a pure function over them resolves identically with **no re-scan**; an identity tombstone now emits the `(actor_id, anchor_id, grant_source)` composite Delete the `GrantWriterAdapter` maps to `RevokeGrant` (§6.14 seq-guarded soft-tombstone, stamped at the tombstone CDC seq → a stale lower-seq re-upsert can't resurrect). A column needing a Core-KV point-read (aspect access on the now-deleted vertex) reports unresolvable (`errCoreKVReadDisabled` guard in `resolveProperty`); a non-RETURN-alias or node-valued column falls through → **a wrong-delete is structurally impossible** (anchor-label match + every key column resolving read-free to a scalar; even a hypothetical mis-resolve targets only the lens's own row, `actor==anchor`, inert). Single-key lenses byte-identical; the dead `resolveAnchorKeyValue` retired (the unified `evalExpr` path subsumes it). **Pairs with Fire 1 (`d772195`)** — the same `KeyColumns` thread drives both populate and retract. Tests: engine unit (composite grant key resolves on identity tombstone; secondary-type / aspect-read / absent-RETURN-alias columns fall through) + pipeline unit (tombstone → composite-keyed Delete); the adapter `Delete`→`RevokeGrant`→`is_deleted`→no-resurrect chain is already proven by Fire-1b `TestReadPathSeam_Integration` (local Postgres; CI has no Postgres → the engine+pipeline units are the CI regression guard). No frozen-contract change (build-to §6.14, committed). **Review scaled to thorough-lead + adversarial-self-review** (zero live enforcement consumer, inert self-grants, mirrors the heavily-reviewed simple engine + Fire-1 upsert path; **full 3-layer reserved for Fire 3** — the live security e2e). Gates green (build/vet/golangci 0/STRICT-conventions 0/`-race` full+pipeline; bootstrap full-graph seeders live-activate the grant lens through the full engine; refractor/adapter/cmd-refractor `go test`). `refractor.md` retraction note + the `capabilityReadGrants` lens doc-comment updated. **Remaining: Fire 3** (security e2e).
- **2026-06-29 — [CI] Halve the leaseshortwindow freshness window (40s→25s) — cut the convergence-job long pole** (89a9842, CI green run 28376971084) — the **`convergence` job was the firm pipeline pole (~108-119s, avg ~112s; max in 5/6 recent runs vs `unit` ~105s)**, dominated by `BgcheckFreshness_EagerReopen` (81s on CI = two real lapses of the 40s short freshness window). The `leaseshortwindow` window is **test-only** (production stays 5m in `freshness_window.go`; this value is "never compiled into a shipped binary"), so shortening it is a pure test-fixture change — **no component behavior changes**. **40s→25s** (`packages/lease-signing/freshness_window_short.go` + the `internal/leaseconvergence` mirror). **Measured: EagerReopen 79.93s→50.45s same-machine (−29.5s, the predicted 2×(40−25)); convergence JOB ~112s→79s in CI (−33s, −29%).** **This explicitly supersedes the prior grounding fire's "25s forbidden — erodes the SteadyState flake margin from 3× to 2×" note (entry below), on two grounds the prior call missed:** (1) the prior consideration kept the 30s drain cap, so 25<35 would have *violated* the documented `window > (drain+settle)` invariant — **this fire ALSO tightened the SteadyState drain cap 30s→15s**, restoring it at 25>20 with the *same* 5s margin the old 40s/35s pairing held; (2) the prior "12.6s/2-core → 2× margin" framing used **full-test-time as the denominator**, but the window only needs to exceed the `bgcheck.completedAt → end-of-assertSteadyState` span — and bgcheck completes *late* in the test (after the two 45s-budgeted dispatch-task awaits + driveApplicantSteps), so that span is a few seconds, not the full test. **Fresh stress (this machine) reproduced the prior fire's contention and then some: SteadyState = 6.06s (GOMAXPROCS=1), 6.31s (GOMAXPROCS=2), ~6s (GOMAXPROCS=4) — duration is NOT contention-sensitive (gated by the fixed 5s settle, not CPU); the prior 12.6s was an anomaly.** 15+ green SteadyState executions across all core counts + full-suite ×3 + CI green = zero window-lapse. **No gate weakened:** identical tests/assertions/coverage; EagerReopen still proves both freshness cycles with exactly-one external call each; SteadyState still holds 5s; the drain cap was *tightened* (stricter), not loosened; no retry, no `-short`. Gates green (build/vet/golangci 0/STRICT-conventions 0; lease-signing untagged + object-gc + full tagged suite). **New pole = `unit` (~107s); convergence now ~79s, ~28s below it.** Next CI fire: `internal/loom` (55s) / `internal/bridge` (44s) `t.Parallel()` in `unit` (fully isolated fixtures — same treatment as the weaver/lease-convergence fires) — now provable, since it drops `unit` below the new convergence floor.
- **2026-06-29 — [Loom/orchestration-base] Adapter-read-seam Fire 1 — subject-templated externalTask params (the templating engine)** (65f4f4d, CI watch in progress) — ratified ★★ category-3 pick; a genuine **impl-vs-frozen-contract correctness fix**: §10.5 mandates externalTask `params` be subject templates resolved per the templating rule, but the Loom engine did **opaque pass-through** (`engine.go:997`) and hydrated only `[subjectKey]` (`:1009`) — so a bridge adapter only ever received static pattern data (`{family}`), never the applicant's real name/DOB. **Two dormant, backward-compatible halves** (no pattern declares `subject.*` params yet → existing read-sets byte-identical; the lease-signing consumer is Fire 2, Verticals lane). **(i) Loom DECLARES, reads no Core KV** (Mechanism 2): `inferExternalTaskReads` (`internal/loom/externaltask_params.go`) walks `step.Params`, `parseGuardPath`s each `subject.*` value, adds the known aspect key `subjectKey.<aspect>` to `ContextHint.Reads`; malformed template → loud submit-time fail; deterministic sorted order keeps the outbox byte-stable; the opaque `params` pass-through preserved (engine never substitutes). **(ii) shared resolver helper** `orchestrationbase.ResolveSubjectParamsHelper` — a Starlark `resolve_subject_params(params, subject_key)` an externalTask instanceOp DDL includes; substitutes each `subject.*` token from the op's JIT-hydrated OCC-snapshot state via §2.5 `kv.Read`; absent/tombstoned vertex or absent/null field = **loud data error** (never a blank to a vendor). Core-KV reads stay in the Processor; Loom's guard-eval remains its lone Core-KV-read exception. **No frozen-contract change** (builds to the committed §10.5 narrowing). **Grounded deviation from design §2.2 (flagged for Andrew):** helper signature is `(params, subject_key)` not the design's `(params, state)` — `subject_key` is required to build the aspect key, and resolution is via `kv.Read` (serving the hydrated OCC snapshot for declared keys) not `state`-indexing; sound impl correction, not a contract change. Tests: inference table (literals/root/aspect/multi-aspect-sorted-dedup/non-object-params/malformed-loud-fail) + resolver Starlark table (literal pass-through/root+aspect resolution/mixed/None→{}/missing-field+tombstoned+malformed loud-fail). Gates green (build/vet/golangci 0/STRICT-conventions 0; full loom + orchestration-base `go test`). Thorough-lead + adversarial self-pass on the two design-flagged boundaries (Loom-takes-no-Core-KV + read-set/resolver grammar lockstep + data-error loudness); scaled from "full review" per the dormant-seam precedent, **full 3-layer reserved for the Fire-2 live-consumer turn-on**. NATS write-restriction Fire 2 (the comparable ★★ ratified sibling) was passed over this fire — it's an *atomic* enforcement turn-on requiring the full auth-on `make up-full` stack battery that collides with the shared running stack (gates-can't-go-green unattended); better suited to a dedicated/attended fire.
- **2026-06-29 — [Core/bootstrap] D1.3 Increment 1 — base `capabilityReadGrants` Postgres lens populates `actor_read_grants`** (f04f331, CI green) — **the keystone that stopped the D1 thrash** ([design](../../implementation-artifacts/d1-grant-population-and-sequencing-design.md), Andrew-ratified). §6.14 mandates every cap-read lens ALSO project to Postgres `actor_read_grants` (what RLS reads), but D1.1 shipped only the NATS-KV half, so the grant table was provisioned EMPTY → RLS denied every protected read → Verticals' Fire-2 protected model couldn't be tested → bounce. Added the missing producer: a new **primordial GrantTable Postgres lens** (the Postgres twin of `capabilityReadLens`, a separate lens per the §6.14 multi-Lens note) projecting each identity's self-grant `(actor_id == anchor_id == nanoIdFromKey(key), grant_source 'cap-read')` — the grant the set-membership policy matches so A reads only A's rows. **Bootstrap seeder learned the read-path posture** (the next-bounce gap, pre-empted): `LensDefinition` gains `Adapter/Table/GrantTable/Protected/Columns`; `makeLensSpecBody` emits a postgres `targetType` + `targetConfig` (grantTable, empty DSN → Refractor resolves `REFRACTOR_PG_DSN`) byte-identical to the package surface (`c1a8901`); `addLensAspects` emits a `targetTable` doc-aspect for postgres; nats-kv path byte-identical. Bootstrap file **v12→v13** (stale-file hard-reject, established pattern); the lens stays out of `PrimordialVertexKeys()` like its NATS-KV twin so the count is unchanged (34). `make up` runs Refractor with `REFRACTOR_PG_DSN`, so it activates + populates live — exercised by CI's stack-gates (verify-kernel + 8 verify-packages green). Tests: literal cypher → one self-grant per identity through the full engine (no Postgres); seeder serialization pinned + nats-kv regression-guarded; verify.go checks the new aspects; lease-convergence e2e green (the new primordial postgres lens is harmlessly translate-skipped in a no-Postgres harness). The Postgres round-trip is the already-green Fire-1b `POSTGRES_TEST_DSN` seam. **Self-review CORRECTED the design's "retraction is free" claim** — `AnchorDeleteResult` can't resolve the `nanoIdFromKey(...)` function-call key and emits 1 column where a GrantTable delete needs the 3-column composite, so grants accumulate monotonically; a tombstoned identity's self-grant lingers but is inert (self-only; deactivation gated at the D1.2 JWT boundary) → filed the composite-keyed grant-retraction follow-up (backlog row above). No frozen-contract change (builds to §6.14). Thorough-lead + adversarial self-review (dormant-producer precedent; full 3-layer reserved for the Fire-3 turn-on). **🎯 Verticals D1.3 Fire 3 is UNBLOCKED** — the read boundary builds against a proven grant table.
- **2026-06-29 — [Core/pkgmgr + Refractor] Package-declared protected/grant Postgres lenses — D1.3 Fire-2 declaration surface** (c1a8901, CI watch in progress) — Fire 1 (1a `5788e32` + 1b `45e8d98`) wired the Refractor **activation** side of protected Postgres read models (on-wire `TargetPostgresConfig.{protected,public,grantTable,columns}` → RLS table / `actor_read_grants` / `ProtectedAdapter`/`GrantWriterAdapter`), but the package **declaration** side couldn't emit that posture: `pkgmgr.LensSpec`/`lensSpecBody` exposed only flat `{dsn,table,queryTimeout}`. Extended `LensSpec` with `Protected`/`Public`/`GrantTable`/`Columns` (`[]PostgresColumn{Name,Type}`); `lensSpecBody` serializes them into the postgres `targetConfig`. **Grant-key footgun fixed:** a GrantTable lens with no declared key **omits** `key` so Refractor applies the platform grant composite (`actor_id,anchor_id,grant_source`) rather than the `["key"]` default mis-keying the seq-guarded grant writer. New **`validateLensReadPath`** rejects an incoherent posture at build/install (posture only on postgres; protected XOR public; grant≠protected) — **defense in depth** mirroring `translateSpec`'s fail-closed activation checks so a dropped/misdirected posture can't world-publish a model the author believed protected. `validateLensAdapters` no longer requires `DSN` for postgres (env-resolved) and lets a grant lens default its Table. **DSN injection:** `translateSpec` resolves an empty postgres `DSN` from `REFRACTOR_PG_DSN` at activation (mirrors the bootstrap `contract_view` lens) — a package declares posture + columns, not a connection string; **fail-closed** (empty DSN + unset env = hard error, no silent dev-localhost default). The YAML file-authoring path (`lens.Parse`) is untouched (still requires explicit DSN). Adversarial self-review confirmed all paths fail-closed (serialization preserves flags; columns-without-protected benign — `translatePostgresColumns` ignores them when not protected; posture-on-nats-kv rejected at *both* build and activation). **No frozen-contract change** (builds to §6.14, committed). Existing packages byte-identical (zero postgres package lens ships; nats-kv unchanged). Tests: `lensSpecBody` protected/public/grant key-omission + explicit-key + columns shape; `validateLensReadPath` table; `validateLensAdapters` empty-DSN-allowed + grant-table-optional; `translateSpec` DSN env-resolve / declared-overrides-env / empty-no-env-rejected. Gates green (build/vet/golangci 0/STRICT-conventions 0; pkgmgr + refractor/lens + bootstrap + cmd/refractor). Dormant — no protected lens activates until Verticals Fire 2 declares one; thorough-lead + adversarial self-review (D1.1/D1.2/Fire-1a/1b dark-seam precedent; full 3-layer reserved for the Fire-3 enforcement turn-on). **🎯 Verticals D1.3 Fire 2 is now UNBLOCKED.**
- **2026-06-29 — [Refractor] Full-engine multi-column projection key — the GrantTable producer (D1.3 unblock)** (d772195, CI watch in progress) — the full openCypher engine built a projection's key map from only the **first** RETURN item, but the D1 `capabilityReadGrants` GrantTable lens needs the **3-column composite** `(actor_id, anchor_id, grant_source)` and `GrantWriterAdapter.Upsert` errors `anchor_id absent` on a 1-column key → the live lens **never populated `actor_read_grants`** (a latent D1.3 Fire-3 blocker: an empty grant table deny-alls every protected read). Brought the full engine to **parity with the simple engine**: `full.CompiledRule.KeyColumns` threaded from `Rule.Into.Key` at activation (`cmd/refractor`), `applyReturn` builds the complete multi-column key, activation **fail-closes** on a key column absent from RETURN (§6.13). Single-key lenses byte-identical (one column = the sole key); unset KeyColumns keeps the legacy fallback. **Adversarial-self-review caught + fixed a real regression:** envelope lenses (actor-aggregate `cap.<actor>` / operation-role index) synthesize their key at write time, so `Into.Key=["key"]` is *not* a RETURN alias — threading/validating them would have failed the **whole auth plane's** activation in production (test-invisible: bootstrap/refractor tests don't run `cmd/refractor` startPipeline); gated to PLAIN lenses + pinned by a test. Producer gap confirmed by code + proven by an engine unit + a pipeline→key proof + `TestGrantKeyFields` (the 1-column key is rejected). **Fire 1 of the full-engine-multi-column-key design; impl-level decisions ratified by the lead** (no frozen-contract change — builds to §6.14; no fork — Steward rule #2 promoted the 📐 design to build-ready). **Review scaled from the design's "full 3-layer" → thorough-lead + adversarial-self-review (explicit/overridable):** zero-enforcement producer writing inert self-grants (`actor==anchor`), no live authz consumer, mirrors the heavily-reviewed simple engine + `679fe25`. The **Verticals stream shipped D1.3 Fire 3 (`ce57c10`) in parallel** — the RLS read-enforcement turn-on this producer makes real. Gates green (build/vet/golangci 0/STRICT 0/`-race` full+pipeline/refractor+bootstrap+cmd-refractor `go test`; verify-kernel→CI). `refractor.md` note added. **Remaining: Fire 2** (composite-keyed anchor-tombstone retraction — full 3-layer for the wrong-delete surface) → **Fire 3** (security e2e).
- **2026-06-29 — [Refractor/Core] `nanoIdFromKey` auth-plane cypher fn — the D1 §6.14 bare-NanoID anchor prerequisite** (d85450d, CI watch in progress) — the auth-plane cypher engine had no string function and no list-indexing, so a read-auth lens couldn't strip the `vtx.<type>.` prefix off a vertex key (why D1.1 shipped the full key as an interim). §6.14 (committed `7aaa79b`) makes `readableAnchors[].anchorId` the resource's **bare NanoID** — an opaque RLS match token, distinct from §6.5 `serviceAccess.service` (a dereferenceable address that keeps the full key); the two were conflated and are now separated. Added a **targeted, fail-closed** `nanoIdFromKey(vertexKey)` to `evalFunctionCall` (`internal/refractor/ruleengine/full/executor.go`): only a well-formed `vtx.<type>.<id>` key (3 dot-segments, leading `vtx`, non-empty type+id) yields a NanoID; an aspect/link/malformed key **errors** rather than emit a wrong anchor (a nil arg → nil). Core logic in a pure `nanoIDFromVertexKey` helper (mirrors `levenshteinDistance`), unit-tested directly (valid + meta + hyphen/underscore NanoIDs pass; aspect/link/bare/2-seg/wrong-prefix/empty-segment/trailing-dot all fail-closed). Revised the base **cap-read lens** (`bootstrap.CapabilityReadLensDefinition`): `anchorId: identity.key` → `anchorId: nanoIdFromKey(identity.key)`; the §6.14 conformance test now asserts the bare NanoID. **Adversarial self-review caught + fixed** a stale `gateway/auth` comment conflating the actor id (full key) with `anchorId` (bare NanoID). Pure projection, **zero enforcement** (no cap-read consumer yet) — the prerequisite the D1.3 RLS read boundary matches against. **No frozen-contract change** (§6.14 already committed by Andrew). Gates green (build, `make vet`, golangci 0, STRICT-conventions 0, short + NATS conformance + bootstrap + refractor capability e2e + gateway/auth). Thorough-lead + adversarial self-review (scaled from 3-layer per the D1.1/D1.2 zero-enforcement precedent; full 3-layer reserved for D1.3 turn-on). **Unblocks** the §6.14 bare-NanoID anchor for D1.1b / the package `cap-read.<domain>` grant lenses / D1.3 RLS.
- **2026-06-29 — [CI] Grounding fire: re-measured the pipeline, found no safe + provable single-fire wall-clock win (NO code change — no churn)** — re-grounded per the Whetstone mandate. **Current overall CI wall-clock ~110-117s** (was 5m57s pre-campaign — ~67% off across 1443109 + 07f3824 + ce2086f). **Flake history clean: 1 failure in the last 100 runs (28228924125, a *real* fix — the clinic-reminders weaverTarget assertion — not a re-run flake).** Per-job (run 28342527986): lint-build 42s, stack-gates 50s, **convergence 102s, unit 103s** — convergence is the FIRM cap (the max in 4-5/5 recent runs, ~11s over unit). Decomposed the cap on CI: the **`convergence` step (92s) = ~10s tagged compile + `BgcheckFreshness_EagerReopen` 82.27s** (two real 40s freshness lapses end-to-end through the live stack); the other 6 convergence tests finish 2-9s (no CI contention — an earlier *local* failure was machine contention, not a CI flake). The `unit` step (91s) is capped by `internal/loom` 55s + `internal/bridge` 44s (both 0× t.Parallel, fully isolated fixtures — a safe future target). **Why no change shipped:** (1) the only lever that moves overall wall-clock is the **convergence cap (EagerReopen 82s)**, reducible only by retuning the deliberately-floored 40s short window — but ce2086f stress-validated SteadyState peaks at 12.6s/2-core, so 40s preserves ~3× flake margin; dropping it erodes the margin (raises flake risk — a HARD-constraint violation). (2) Parallelizing `unit`'s loom/bridge would speed the `unit` *job* (~95s→~50s, mirroring weaver) but **NOT** overall wall-clock — `unit` already sits ~11s below the convergence cap → fails prove-it until convergence is also cut. (3) The setup-go cache is one immutable go.sum-keyed entry shared by all 4 jobs (convergence logs "cache hit, not saving" → its tagged builds never persist), but tagged compile is only ~10s on CI → a per-job rotating-key cache nets <10s with cold-start risk — not worth it. **Recorded the cap analysis + the paired-campaign path (cut EagerReopen + loom/bridge together in a persistent worktree, merge only when the whole pipeline proves faster) on the board row so the next fire doesn't re-derive it or waste a fire on a unit-only change.** Every gate still runs (unchanged); no gate weakened, no timeout loosened, no retry, no flake introduced.
- **2026-06-28 — [CI] Parallelize the lease-convergence e2e gate (t.Parallel)** (ce2086f) — the **`convergence` job was the CI long pole (~139s)**, entirely the lease-convergence step (object-gc is 3s). Its cost is dominated by `BgcheckFreshness_EagerReopen`, which spends ~2× the 40s short freshness window (~80s) **idle-waiting on lapses, not CPU**; the other six e2e tests (~29s of CPU work) ran *serially after* it. Each test already boots a **fully isolated** stack (per-`t` embedded NATS `Port:-1` random + private `StoreDir` via `jsstore.Dir(t)`), so the six CPU tests can overlap EagerReopen's idle wait. Added `t.Parallel()` to **all 7 gate tests** (no other change; the deliberately-floored 40s window — 40s just clears the steady-state 30s-drain+5s-settle ceiling — was **left untouched**, so no flake margin was traded). **Stress-verified non-flaky: 9 consecutive green parallel passes** (6× GOMAXPROCS=4 CI-like, 3× GOMAXPROCS=2 worst-case contention); the flake-sensitive `SteadyState` test (40s window vs 35s budget) peaked at **12.6s even under 2-core contention — >3× margin intact**. **Measured: lease-convergence step 125s → 92s; convergence job 139s → 107s; overall CI wall-clock ~139s → 107s (~23%).** Every gate still runs — only added parallel markers, nothing dropped/`-short`-skipped, no timeout loosened, no retry. All gates green (run 28324534020). New near-co-equal long poles: convergence (107s, floored by EagerReopen's ~80s freshness wait) and `unit` (95s) — next lever: loom (55s) / bridge (44s) in `unit`.
- **2026-06-28 — [CI] Parallelize the weaver test package (t.Parallel)** (07f3824) — `internal/weaver` was the **slowest package in the `unit` job (75s)** and set its critical-path floor. Every weaver test already boots a **fully isolated** embedded NATS server (`Port: -1` random + `StoreDir: jsstore.Dir(t)` private) — the suite ran serially only because no test was marked parallel. Added `t.Parallel()` to **all 86 top-level tests** (no other change). No shared mutable state (the only package globals are an immutable `strings.Replacer` + a compiled `regexp`); no `Setenv`/`Chdir`/`TestMain`. **Stress-verified: GOMAXPROCS=4 ×10 (860 executions) green, full `-p 4` suite green, golangci clean** — flakiness only falls (parallel + isolated, no new shared state). **Measured: weaver package 75.0s → 19.1s in CI; `unit` test step 140s → 87s (~53s / 38% off the long pole)**; `unit` is no longer the critical path (convergence 127s now is). **Every gate still runs** — only added parallel markers, nothing dropped/skipped, no timeout loosened, no retry. All gates green (run for 07f3824). Next slow serial packages in `unit`: loom (55s), bridge (44s) — same treatment.
- **2026-06-27 — [CI] Serial pipeline → 4-job parallel matrix** (1443109) — the single serial `build-and-test` job ran every gate back-to-back; its two long poles (`go test ./...` ~138s + `lease-convergence` ~118s) ran *after* ~90s of Docker-stack gates that don't depend on them, so wall-clock was sum-of-stages. Fanned out into four concurrently-running jobs (wall-clock = slowest job): **lint-build** (build/vet/golangci/conventions — no Docker), **stack-gates** (`make up` → verify-kernel → 8× verify-package → Gate 2 → Gate 3 → hello-lattice → `if:always()` teardown — Docker), **convergence** (lease-convergence + object-gc — embedded NATS), **unit** (`go test ./... -p 4` — embedded NATS). **Every gate from the serial job still runs, each in exactly one job** — none dropped, no `-short`, no loosened timeout, no retry. Re-derived gate-coverage by hand. The convergence/unit jobs need no Docker stack (each fixture binds a random port + private `jsstore.Dir(t)` StoreDir); verified locally that the Gate 2/Gate 3 roll-ups pass without a live stack (their Health-KV marker write is best-effort) and both convergence suites pass embedded. **Measured CI wall-clock: 5m57s → 2m31s (~58% / ~3m26s faster)**, all gates green (run 28290842810). New long pole = `unit` (147s) for a future fire. Default `setup-go@v6` Go build/module caching already on.

_Append shipped Stream-2 items here (this lane only — keeps the board collision-safe)._

- **2026-06-28 — [Core/pkgmgr] F-004 Fire 3: dev-loop refresh targets + upgrade-aware docs** (7f98d83) — the Makefile dev-loop + documentation increment of the Andrew-ratified package-version-upgrade feature (design §2.7/§7 Fire 3). **Closes face #3 of the recurring dev-loop tax** that re-prioritized F-004 to ★★ — applying a package edit no longer needs a full `make down && up-<vertical>` teardown. **Makefile (additive — no existing target touched):** `reinstall-package PKG=packages/<dir>` (diff-apply one edited package in place on the running stack via `lattice-pkg install --force`, F-004 upgrade-aware) + `refresh-clinic`/`refresh-loftspace` (diff-apply the vertical's packages **and** rebuild+restart its FE binary in one command — mirror `up-<vertical>` minus the teardown + up-full); added to `.PHONY` + the quick-reference header. **Docs — reconcile the now-stale "no in-place upgrade" claims** with the shipped Fire 1a/1b/2 feature: `docs/components/_packages.md` gains a "Upgrade / in-place dev-loop refresh (F-004)" section (diff-by-key create/update/tombstone, single atomic `UpgradePackage` op, the path-independent step-8 protected-key guard, version-independent entity keys, the §4 one-time blue-green re-mint on a pre-F-004 long-lived stack, the *added*-entity-needs-fresh-kernel caveat vs. *edited*-lens live re-projection); the Installation-semantics idempotency step now points at the upgrade path; the CLI block shows `install [--force] [--dry-run]` + the `upgrade` verb; Known-limitations swaps the retired "no in-place upgrade" line for the real residual (no in-flight-instance version pinning — G6 follow-on). `README.md` gains a Development "Dev-loop: apply a package edit without a teardown" subsection. **Makefile + docs only — no platform code** (the underlying `install --force`/`Apply` path is already proven by Fire 2's 8 meta-pipeline integration cases; these targets are thin wrappers). **Verified:** `make -n` expands all three targets correctly (the `reinstall-package` PKG guard prints usage + exit 2 when empty); a live-stack (reused, not re-up'd) `lattice-pkg install --force --dry-run packages/rbac-domain` previewed the expected §4 one-time re-mint (`created=43 updated=0 tombstoned=43`, action=upgrade, nothing submitted — stack untouched); STRICT lint-conventions 0 issues. CI green. **F-004 is now effectively complete** — only the optional Fire 2 live e2e remains (a confirmation sub-increment; the reprojection mechanism is already covered by the refractor convergence e2e suite). Scoped commit (`git add` of just the 3 files; left the parallel stream's `verticals.md` + the 3 staged contract proposals untouched).
- **2026-06-28 — [Core/pkgmgr] F-004 Fire 1a: version-independent entity keys** (cd20ce8) — the load-bearing foundation of the Andrew-ratified package-version-upgrade feature (design: `package-version-upgrade-design.md`; Contract #8 §8.1/§8.6 ratified+committed). Every installed entity's NanoID now derives from **package name + entity tag**, not the version (Contract #8 §8.1), so the same logical lens/DDL/role/op-meta keeps its `vtx.meta.<id>` / `vtx.<type>.<id>` key across versions — the property the in-place upgrade (§8.6) needs so a surviving entity is an *update* of a stable key, not a re-mint that orphans vertices and breaks every NanoID cross-ref (a WeaverTarget's `lensRef`, a permission's `grantedBy` link). `entityNanoID(name, tag)` replaces `deterministicNanoID` at the **8 entity-key sites** (package, ddl, lens, permission, role, weaverTarget, loomPattern, opMeta); `deterministicNanoID(name, version, tag)` is **retained only** for the install-op `requestId` (keeps the version so distinct-version installs dedup independently — a unit test pins `v0.1.0 != v0.2.0`). The **permission identity tag** moves from `perm:<index>:<operationType>` to `perm:<operationType>:<scope>` (logical identity), so reordering a package's `Permissions` slice no longer churns keys; a new fail-closed `validatePermissionIdentityUniqueness` rejects a degenerate duplicate `(operationType, scope)` that would now collapse onto one key. **Migration safety verified two ways:** (1) a throwaway audit imported all **12 shipped package `Definition`s** — none declares a duplicate `(op, scope)`, so the validator breaks no install and the 8 `verify-package-*` gates stay green; (2) same-version reinstall still **skips** because `findInstalledPackage` matches by the manifest's `name` field, not the re-derived key — so a long-lived dev stack with old version-salted keys is unaffected by Fire 1a alone (the one-time re-mint only matters once the §8.6 diff lands). Fresh-install behaviour is otherwise byte-identical (only key *values* change). Tests (`version_independent_keys_test.go`): a golden-anchor NanoID (guards alphabet/masking/salt drift), version-independence + version-dropped-from-salt proof, the requestId-keeps-version proof, scope-distinction, the duplicate-permission validator table, and an end-to-end batch reorder-stability check. No new kernel op and **no write/read-path change** (P2 install still submits an op; P5 N/A) → **thorough lead review** (the new *mutating* `UpgradePackage` kernel op + the diff engine are **Fire 1b**, which gets the full 3-layer). Gates green (build / make vet / golangci 0 / STRICT-conventions 0 / `go test -race ./internal/pkgmgr/` + bootstrap + loupe consumers), CI green. Top ratified build-now feature; round-robin Core (the stalest component). **Next: Fire 1b** — `Installer.Upgrade` diff engine + `UpgradePackage` DDL + bootstrap wiring (3-layer).
- **2026-06-28 — [Core/substrate] NATS write-restriction Fire 1: credential seam** (75e9acc) — Fire 1 of the Andrew-ratified NATS account-level write restriction (Path A; design: `nats-account-write-restriction-design.md`). The transport-authZ **credential seam**, dark + no-op by default: `substrate.ConnectOpts` gains `NKeySeedFile` (per-component NKey, recommended) + `CredsFile` (JWT/operator), mapped in `Connect` to `nats.NkeyOptionFromSeed` / `nats.UserCredentials`. **Both empty ⇒ today's anonymous connect** — the compatibility hinge that keeps the embedded test harness + every existing call site byte-identical (full substrate package green, no binary/conf touched, zero stack risk). **Fail-closed throughout** (the load-bearing property for a credential primitive): a malformed seed, a missing creds file, or both-credentials-set each **error before the dial** rather than silently degrading to anonymous. Tests (`conn_creds_test.go`): empty-creds anonymous round-trip; **end-to-end against an NKey-authenticated embedded server** (matching seed authenticates + KV round-trips, anonymous client rejected); bad-seed / missing-creds / both-set all error at the seam. `go mod tidy` promotes `nkeys` → direct (now used in tests) + corrects `spf13/cobra`'s stale `// indirect`. No frozen-contract change (design §5 — NATS account/permission config is deployment/substrate); no lens/op/read-model (P5/P2 N/A). Gates green (build / vet / golangci 0 / STRICT-conventions 0 / `go test ./internal/substrate/` 8s), CI green. **Thorough-lead-reviewed** — scaled down from 3-layer *explicitly + overridably* because the increment ships **no enforcement** (the real attack surface is Fire 2, which gets the full 3-layer). Top ratified ready security-plane item; the seam D1 + control-plane-authz both sit beneath. **Next: Fire 2** (binary env-threading + auth-enabled conf + Gate 2/3 flip + `verify-nats-permissions`, atomic, 3-layer).

- **2026-06-28 — [Weaver] Pace collapse-only userTask reclaims with a state-keyed backoff** (04c7689) — Andrew-ratified Option D (design: `weaver-reclaim-check-before-act-probe-design.md`). A long-open userTask gap (e.g. `missing_approval`→`assignTask`) is reclaimed by the sweep at **every** mark-lease expiry; §10.3 claimId-preservation already makes that re-dispatch **collapse** on the existing task/instance (no duplicate artifact), but each repeat still commits a **no-op op + a fresh Contract #4 tracker + a count bump + a Warn** — up to **~1,440 phantom ops/trackers/Warns per pending approval** over the 30-day grant TTL. Now the two **collapse-only** userTask actions (`assignTask`/`triggerLoom`) pace repeat reclaims with an **exponential backoff** keyed on state Weaver already keeps — the mark's own `ClaimedAt` + the existing `dispatchCount` (`backoffInterval` = base × 2^(count-1), base = lease, capped at the 24h tracker horizon). The **first** reclaim still fires at lease-expiry (count 0/1 ⇒ base), so lost-dispatch recovery is unchanged; `directOp`/external (where reclaim re-dispatch IS the intended bounded retry) is **never** backed off. **No Core-KV read, no new mark field, no frozen-contract change.** **One grounded deviation from the literal design, flagged for Andrew:** the design's "the mark-TTL backstop covers survival" premise is **false at the default constants** (mark TTL = `markTTLBackstopFactor`(2) × lease = 60m ≪ the 24h cap) — a backed-off mark would TTL-expire mid-backoff into a **markless open gap**, and a CDC redelivery of the still-violating row would then mint a **fresh `claimId` → a NEW taskId → a DUPLICATE task** (`fireEpisode` CAS-creates on mark absence). Fix (still within Option D's mechanism — no extra writes, no new field): a userTask reclaim **sizes the re-armed mark's per-key TTL to outlast the next backoff window** (`backoffInterval(count+1)` + a sweep-cadence margin, floored at the default backstop), so the mark is always reclaimed — TTL re-armed — before it can die; `directOp` marks keep the **byte-identical** default TTL. Net effect: with survival guaranteed the backoff reaches the full 24h cap → **~36 total reclaims over 30 days instead of ~1,440** (~40× fewer phantom ops/trackers/Warns). Observability: a `sweepReclaimsSuppressed` heartbeat metric (Contract #5 §5.4). Doc-drift swept: the reconciler.go reclaim comment + weaver.md "Reconciler sweep" row no longer call the userTask re-fire a "documented rare-double … check-before-act deferred to Phase 3" (superseded by §10.3 + this backoff). Tests: `backoffInterval` curve (base/exponential/cap/monotonic, no NATS); userTask reclaim suppressed-when-recent / fires-when-aged; `directOp` never backed off; the re-armed TTL outlasts the next backoff window; existing reclaim+e2e regression net green (the `MidFlightKill` e2e now runs its reclaiming engine at the mark's own short lease, consistent with a real crash-recovery). Gates green (build / make vet / golangci 0 / STRICT-conventions 0 / `go test ./internal/weaver/...`, reclaim+e2e stress ×4); thorough lead review (the design-flaw fix is the load-bearing review focus). Weaver round-robin pick + the top ratified ready item.
- **2026-06-27 — [Loom] Pin the redelivery-dedup + op-meta-deregister paths** (d8bfa34) — two **live correctness paths at 0% coverage** in an otherwise-healthy package: `stateStore.deleteToken` (`state.go:470`) — the **idempotency-on-redelivery** guard that clears a stale `token.<token>` reverse pointer when a completion is redelivered after the cursor already advanced (`engine.go:749`) — and `patternSource.removeOpMeta` (`source.go:294`) — the **live op-meta deregister** that drops a stale `operationType → vtx.meta.<id>` mapping when an op meta-vertex is tombstoned (`source.go:159`). Both fire only on rare CDC/redelivery paths the e2e suite never hits, so a dedup/deregister regression would land silently. New `redelivery_dedup_internal_test.go` (embedded-NATS for the KV path, in-memory for the index path, `jsstore.Dir(t)` StoreDir per CI-parallelism): **deleteToken** clears an advanced pointer (resolveToken then misses), is idempotent on a double-delete (the redelivery shape), no-ops on a never-written token, and **propagates a genuine substrate failure** rather than swallowing it; **removeOpMeta** deregisters the tombstoned op's operationType, drops **every** type pointing at the same id (the loop keys on the target key), leaves unrelated mappings intact, and no-ops on an unknown id. The op-envelope wire shape is rebuilt in-test (not via the impl's own `opMetaProbe`) so a struct-tag drift is caught. `removeOpMeta` → **100%**, `deleteToken` → **80%** (residual = the `ErrKeyNotFound` swallow, genuinely unreachable via NATS — KV `Delete` is itself idempotent, which is *why* the double-delete is a no-op). Test-only; no behavior / key / contract change. Gates green (build / vet / golangci 0 / STRICT-conventions 0 / full `go test ./internal/loom/` 57s); lead-reviewed (small, green, additive, pins existing behavior without altering it). Loom round-robin pick (2nd-stalest component, ~12h).
- **2026-06-27 — [Core] Pin the Go↔Starlark value marshalling boundary** (4bd32f7) — `internal/processor/starlark_runner.go`'s `goValueToStarlark` / `starlarkValueToGo` (+ the `goMapToStarlarkDict` / `starlarkDictToGoMap` helpers) are the seam that governs what a DDL script reads from a vertex/op payload and writes back — on the platform's **sole Core-KV writer** — yet had only incidental coverage (`goValueToStarlark` **13.3%**, `starlarkValueToGo` 46.7%). A silent regression there corrupts script-visible data, so the type mappings are now pinned directly (new `starlark_runner_test.go`). Pinned invariants: the **JSON-integer preservation trick** (Go decodes every JSON number as float64, so a whole-valued float64 → Starlark `Int` while a fractional one stays `Float`, and an integral number round-trips back as `int64`); that **only** `json.Unmarshal`-native `[]interface{}`/`map[string]interface{}` convert structurally while a typed slice/struct falls back to its `Sprintf` string; the out-bound type map (`None`→nil, `Int`-fits-int64→int64, `Float`→float64); a **wider-than-int64 `Int` degrading to its decimal string** rather than truncating; the unknown-Starlark-type `String()` fallback; `starlarkDictToGoMap` **skipping non-string keys**; plus an end-to-end generic-map round-trip. **Test-only; no behavior / key / contract change.** The four functions → **100%**; package total 76.6% → **77.8%**. Gates green (build / vet / golangci 0 / STRICT-conventions 0 / `go test ./internal/processor/`); lead-reviewed (small, green, additive, pins existing behavior without altering it). Core round-robin pick (processor was the stalest component, ~12h).
- **2026-06-27 — [Weaver] Cover the control-plane authorize boundary + subject parser** (8199c11) — `internal/weaver/control` was the survey-flagged soft spot (66.3% coverage; the security-sensitive control-plane responder) with three untested branches that matter: the **capability authorize-before-dispatch boundary** (a denied control op must surface the authorizer's error and **never reach the engine** — now asserted via a `denyCapability` checker that confirms an empty engine call-log on deny, for both `list` and the mutating `disable` path), **`Engine.ListTargets` error surfacing** (`fakeEngine` never errored — added a `listErrEngine`), and the **defensive `targetIDFromSubject` parser** (the wildcard endpoint `lattice.ctrl.weaver.*.<op>` can only route a conforming 5-token subject, so its deviation branches are unreachable via NATS but guard a future direct caller / loosened wildcard — table-tested via a new `export_test.go` exposing the unexported helper, pinning the Contract #1 control subject shape). Plus the `StubCapabilityChecker` nil-logger fallback. **Test-only; no behavior / key / contract change.** Coverage 66.3% → **77.2%** (residual = the `respondMicro` marshal-failure fallback, genuinely unreachable for a JSON-safe `ControlResponse`). Gates green (build / vet / golangci 0 / STRICT-conventions 0 / `go test ./internal/weaver/control/`); lead-reviewed (small, green, additive, tests existing behavior without altering it). Weaver round-robin pick (stalest component, ~14h).
- **2026-06-27 — [Core] Pin the substrate deterministic-id derivation invariants** (a4f87ae) — `internal/substrate/derive.go` (the stalest component, Core) holds the platform's deterministic-id backbone — `DeriveNanoID` (collapses a retried op onto one `vtx.op.<requestId>` tracker: bridge reply-op, Loom token), `NanoIDFromPCG`, and `SHA256NanoID` (content-addressed object identity) — used across bridge/objectmanager/loupe/loftspace/processor, yet had **no dedicated unit test** (only incidental coverage in other packages' object-lifecycle tests). Added `derive_test.go` (8 tests) pinning the invariants the doc-comments *claim* but nothing enforced: **determinism** (same inputs → same id, always — the property that lets a retried op collapse without shared state), **20-char alphabet-only output** that survives `ClassifyKey`'s NanoID-segment gate and is dot-free (legal op requestId), **namespace/input isolation** (deliberately *not* asserting the false `("ab","c")==("a","bc")` boundary the `ns+input` hash doesn't have), and the **documented SHA-256→PCG big-endian seed wiring** that makes `SHA256NanoID` byte-identical to the Starlark `crypto.sha256NanoID` builtin (re-derived independently in-test, not by re-calling the impl). **Golden-value anchors** guard any silent shift in alphabet order / 6-bit masking / seed split that would re-issue a moved id and break op de-dup + object addressing fleet-wide. Test-only; no contract / behavior / key change. Gates green (build/vet/golangci/STRICT-conventions/`go test ./internal/substrate/`); lead-reviewed (small, green, additive). Round-robin pick: substrate was the stalest component (~17.6h).

- **2026-06-27 — [Loupe] Test the object-serving anti-XSS disposition boundary** (fd0cacd) — `cmd/loupe/objects.go` was the largest Loupe handler (15 KB) with **no test file**, and its security-critical decision lived inlined + untested in `handleObjectGet`: only the raster-image allow-list (`image/{jpeg,png,gif,webp}`) renders `inline` with its declared type; every other type (svg / html / pdf / unknown / empty) is forced to a neutral `application/octet-stream` `attachment` so an uploaded active document can never execute as same-origin script in the admin UI (the CSP `default-src 'none'; sandbox` + `nosniff` are the belt to this brace). Extracted the decision into a pure `objectDisposition(contentType) (servedType, disposition)` helper — the established `cmd/loupe` pattern of factoring pure helpers out of handlers for unit-testing (`buildVertexList`, `classifyKey`, `filterAndClassify`) — **behavior-preserving** (byte-identical header output). Added `objects_test.go` table-pinning the boundary: the case-sensitive allow-list rejects a mixed-case spoof (`image/JPEG`) and a parameterized type (`image/png; charset=utf-8`), so a regression that loosened the match fails the build; plus tests for the existing pure helpers `objectLinkKey` (well-formed + 5 malformed-target error paths) and `join0` (NUL-separator collision-resistance). No contract / behavior change; read-only inspector path. Gates green (build/vet/golangci/STRICT-conventions/`go test ./cmd/loupe/...`); lead-reviewed (small, green, behavior-preserving). Round-robin pick: Loupe was the stalest component (~11.8h).

- **2026-06-27 — [Refractor] Postgres adapter `Truncater`** (f537f6b) — `PostgresAdapter` now implements `adapter.Truncater` via `TRUNCATE TABLE "<table>"` (quoted through `quoteIdent`; the constructor already bars an embedded double-quote), so a `Pipeline.Rebuild(truncate=true)` against a SQL-target lens clears the table instead of silently skipping (the old warn path at `pipeline.go:400`, where NATS-KV was the only `Truncater`). Mirrors the KV adapter's purge-every-key truncate; Postgres carries no projection-write guard (Contract #6 §6.2 — unconditional last-writer-wins), so there is no watermark to reset and the replay re-inserts from a clean table. `var _ Truncater` assertion + `withTimeout` like the other writes. No live impact yet (no Postgres-target lens ships) but unblocks the deferred **Postgres read-model**. No DDL/keys/contract change. Unit tests (SQL + interface satisfaction) + a `POSTGRES_TEST_DSN`-gated integration test. The single remaining `📋 ready` component-maintenance item — table now all ✅ except the feature backlog. Lead-reviewed (small, green, non-security-plane adapter).
- **2026-06-27 — [Loom] Heartbeat status hard-coded to lifecycle string** (c59a39f) — the Loom heartbeater set the Contract #5 `status` to the raw lifecycle string, so a heartbeat carrying a `ConsumerPaused` warning issue still reported `status:"healthy"` (violates §5.2/§5.3). `aggregateStatus(lifecycle, issues)` now escalates steady-state status to the worst open issue severity (error⇒unhealthy, warning⇒degraded), leaving `"starting"`/`"shutdown"` verbatim; issues sorted for deterministic output. **Closes the health-honesty sweep — all four self-reporters (Refractor, Weaver, Processor, Loom) now self-report status consistent with their open issues.** Read-only Health-KV; no contract change (the §5.2/§5.3 schema already mandates the mapping). 6-case `TestAggregateStatus` unit table. Loom round-robin pick (stalest component, 12h).
- **2026-06-27 — [Core] Bootstrap key construction → substrate key helpers** (6998a39) — the primordial seeding path (`internal/bootstrap/nanoid.go` + `primordial.go`) now builds every vertex/aspect/link key through the canonical `substrate.VertexKey` / `AspectKey` / `LinkKey` helpers instead of raw `+ "."` concatenation, centralizing the Contract #1 §1.1 key shapes so an off-by-a-dot can't silently mis-seed the kernel. Byte-identical output (the panic-on-invalid helpers run after `IsValidNanoID` validation); the aspect-type loop also stops computing each key twice. Verified by the bootstrap e2e full-graph seeders + `go test ./internal/bootstrap/...` + golangci-lint + STRICT lint-conventions, all green. Starlark key-building inside DDL scripts (`meta_ddl.go`) correctly left as-is. Pure hygiene, no contract/behavior change. Closes the clean follow-up flagged by the resolved `substrate/CONTRACT-AMENDMENT-REQUEST.md`. Core round-robin pick (bootstrap was tied-stalest at ~10h).
- **2026-06-27 — [Core] Processor health-honesty: real `lane_lag` + status/issues anomaly channel** (f16e625) — the Processor heartbeat (the third + highest-stakes self-reporter, on the sole Core-KV writer) no longer fabricates `lane_lag` zeros or reports false-healthy. Real backlog from `processor-main` `NumPending` (attached before `Run`, race-free): per-lane keys `null` (one consumer can't split lanes) + aggregate `lane_lag_total`; `null` = unreadable, never a fabricated `0`. Over-threshold (default 100) ⇒ `ProcessorLaneLagging` warning ⇒ `aggregateStatus` degraded (`starting`/`shuttingDown` protected), with `since`-persistence. Completes the Refractor+Weaver health-honesty sweep. Read-only Health-KV; no contract commit (§5.4 already permits author-discretion metrics). 14 new unit tests via KV-free `buildHealthDoc` + fake consumer; race-clean. **Contract #5 §5.4 clarification staged UNCOMMITTED in `main` → awaiting Andrew.**
- **2026-06-26 — Backlog retitle + sweep + two new roles** — the feature backlog section "Deferred (Phase 3+)" → **"Lattice feature backlog — the Phase-3 build queue"** (the flywheel now actively draws from it; "deferred" framing retired). Swept the feature queue clean of shipped ✅ rows already recorded in git / the index (Loupe live system-map landing page, the conventions-linter edit-time hook, version-control of the role-skills) and marked the Loupe agent-activity console **🚧 Andrew-shelved**. Added two concurrent roles to Stream 2: the **Designer** (`lattice-designer`, `agents/designer/`) — architect persona, turns backlog items into design docs flagged for Andrew to ratify, ahead of the Steward — and the **Whetstone** (`ci-whetstone`, `agents/whetstone/`) — make CI faster + kill flaky tests, without weakening any gate. Pipeline is now Surveyor → Designer → (Andrew ratifies) → Lattice Steward, with the Whetstone as a cross-cutting CI-speed/flake loop.
- **2026-06-26 — [Weaver] Heartbeat status/issue-severity inconsistency** (4de7677) — closes the source-side half of the Loupe-surfaced inconsistency: Weaver's heartbeater hard-coded `status` to the lifecycle string, emitting `status:"healthy"` while carrying issues (violates Contract #5 §5.2/§5.3 — issues empty iff healthy). New `aggregateStatus` escalates steady-state status to the worst open issue severity (error⇒unhealthy, warning⇒degraded); `"starting"`/`"shutdown"` reported verbatim. Read-only Health-KV self-report, no contract change. `TestAggregateStatus` table + e2e asserts `status:"unhealthy"` with the error issues. Earlier 1bcdce4 had already downgraded per-target data gaps to `warning` (the other half), so the channel is now fully consistent.
- **2026-06-26 — [Loupe] Surface component `status`/`issues` on health + system map** (2877a1c) — closes the value chain of the Refractor liveness alert: Loupe's health cards + system-map nodes now honor the Contract #5 §5.4 `status` / §5.5 `issues[]` anomaly channel (previously freshness-only). `componentLiveness` (shared by `computeHealth`/`computeSystemMap`) fuses freshness + reported status + worst issue severity (error→red, warning→yellow), so a component is surfaced honestly even when its status field lags its issues. **In-browser-verified on the live stack** — caught Weaver self-reporting `healthy` with two `error` TemplateDataError issues, now rendered unhealthy/red. FE: degraded/unhealthy card+node styling, status tags, error-issue red text, red-overall summary counts unhealthy. Read-only inspector path, no contract change. 11 new tests. Filed Weaver self-report-inconsistency follow-up.
- **2026-06-26 — [Refractor] Capability-Lens liveness/lag alert** — instance heartbeat now implements the Contract #5 §5.5 `issues[]` / §5.4 `status` anomaly channel for auth-plane (`capability-kv`) lenses: paused → `CapabilityLensPaused` (unhealthy), over-threshold lag → `CapabilityLensLagging` (degraded); `metrics.capabilityLens` always emitted; `since` persistence + resolve-drop. Read-only, no contract change. 13 tests (9 pure + 4 e2e shape). Filed a Loupe follow-up (surface component status/issues). Design: `implementation-artifacts/refractor-capability-lens-liveness-alert.md`.
- **2026-06-26 — [Refractor] Doc drift `consumer.Manager` retired** — `docs/components/refractor.md` corrected (sub-package table + lens-lifecycle step 5): per-lens durable consumers are owned by `pipeline.Pipeline` via `substrate.ConsumerSupervisor`, not a `consumer.Manager`. Pure doc, direct to main.
