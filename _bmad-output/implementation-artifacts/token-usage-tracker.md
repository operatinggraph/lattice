---
title: Lattice Phase 1 Token Usage Tracker
purpose: Track actual implementation-session token usage against per-story budgets; flag model-tier deviations early.
maintainedBy: Winston (architecture lead)
updateCadence: Per implementation session at close
---

# Lattice Phase 1 — Token Usage Tracker

## Operating Rules

- **Each story runs in its own implementation session** (fresh context per story).
- **Each story's budgeted tier is locked** — Sonnet stories MUST run on Sonnet; Opus stories MUST run on Opus. Deviations require Winston's sign-off and a note in the table.
- **At session close**: record actual input+output tokens consumed; compute delta vs budget.
- **Overrun threshold:** if actual > 1.2 × budget, mark `OVERRUN` and add a root-cause note (under-scoped story / mid-implementation architecture surface / external blocker).
- **Underrun is not a problem** but a > 50% underrun should be noted — may indicate the story was over-scoped or that some AC was deferred.

## Per-Story Ledger

| Story | Title | Model (planned) | Model (actual) | Budget | Actual | Δ | Status | Session Date | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 1.1 | NATS Atomic Batch Spike | Sonnet | Sonnet (claude-sonnet-4-6) | 52K | ~78K | +26K | OVERRUN | 2026-05-13 | Prior aborted attempt ~28K (go get permission blocker). This session ~50K. All 4 AC behavioral tests PASS. API discovery (no high-level PublishBatch in nats.go) added investigation tokens. |
| 1.2 | Starlark Execution Spike | Sonnet | Sonnet (claude-sonnet-4-6) | 65K | ~55K | -10K | COMPLETE | 2026-05-13 | All 3 AC areas PASS. Sandbox: 4/4 forbidden ops rejected, 2/2 permitted ops succeed. API ergonomics: ScriptContext→RunScript→ScriptResult pipeline works, Contract #3-conforming output verified. Perf: mean=69µs p95=126µs p99=210µs (1000 iters, no caching). GO recommendation. nanoid.new() deferred to Story 1.6 as expected. |
| 1.3 | Dev Harness with Primordial Bootstrap | Sonnet | Sonnet (claude-sonnet-4-6) | 95K | ~85K | -10K | COMPLETE | 2026-05-13 | All 12 deliverables shipped. docker-compose + Makefile + cmd/bootstrap + cmd/refractor-stub + internal/bootstrap package + scripts/verify-bootstrap.go. Cold make up: 8.3s. Warm restart: 7.4s. verify-bootstrap exit 0 (29 assertions). Fixed primordial IDs approach taken (not random). LimitMarkerTTL must be ≥1s (not 1ns) — minor API discovery note. |
| 1.4 | `internal/substrate` Package | Opus | Opus (claude-opus-4-7) | 110K | ~80K | -30K | COMPLETE | 2026-05-13 | All 8 deliverables shipped. substrate package: doc/nanoid/keys/envelope/conn/kv/batch/errors (+ tests). 10K NanoID test PASS. Embedded NATS integration tests PASS (KV CRUD, AtomicBatch commit, all-or-nothing rejection, cross-bucket guard). Bootstrap refactored: envelope helpers delegate to substrate.NewDocumentEnvelopeAt + AspectEnvelope/LinkEnvelope; SeedPrimordial uses substrate.AtomicBatch (single 30-msg batch replacing sequential kv.Create loop); alphabet centralized via substrate const re-export. CONTRACT-AMENDMENT-REQUEST raised: bootstrap primordial IDs violate Contract #1 (12/12 forbidden chars, 7/12 wrong length); awaits adjudication. Bootstrap key construction not migrated to substrate.VertexKey (would panic on non-compliant fixed IDs). `make verify-bootstrap` passes 29/29. |
| 1.5 | Processor — Consume, Dedup & Auth Stub (Steps 1-3) | Opus | Opus (claude-opus-4-7) | 115K | ~70K | -45K | COMPLETE | 2026-05-13 | All 8 deliverables shipped. internal/processor package: doc/envelope/tracker/step1_consume/step2_dedup/step3_auth/steps_4_10_stub/commit_path/reply/health/errors (+ envelope/tracker/integration tests). cmd/processor/main.go runnable binary. StubAuthorizer (LATTICE_AUTH_MODE=stub default; capability reserved for Story 3.3). Tracker write via substrate.AtomicBatch with 24h TTL (single-message batch, Story 1.7 adds mutations). Integration tests PASS: first-delivery-accepted, duplicate-short-circuit, malformed-nack-with-term + malformed-operation Health KV marker, tracker-write-conflict-acked-as-duplicate (retry-safe), reply-shape unit. `make verify-bootstrap` regression 30/30. E2E manual exercise PASS: fresh op → step 10 ack; resubmit same requestId → DuplicateDetected short-circuit; malformed envelope → term=true. CONTRACT-AMENDMENT-REQUEST raised at cmd/processor/CONTRACT-AMENDMENT-REQUEST.md: bootstrap stream subjects `["ops.*", "ops.meta.>"]` disagree with Contract #2 §2.3 requirement of `ops.<lane>.>` — Story 1.5 used bootstrap-compatible single-segment subjects; awaits Winston adjudication (recommended fix: broaden bootstrap to `ops.>`). |
| 1.6 | Processor — Starlark Sandbox & JIT Hydration (Steps 4-5) | Opus | Opus (claude-opus-4-7) | 130K | ~144K | +14K | OVERRUN | 2026-05-14 | Sub-agent self-reported ~95K but outer task notification showed total_tokens=143722. Honest number recorded here. Mandatory budget-checkpoint rule from the brief did NOT trigger as designed (a third overrun in a row across 1.1, 1.5, 1.6). All 9 deliverables shipped. internal/processor: script_context.go (ScriptContext/VertexDoc/MetaVertex/MutationOp/EventSpec/HydratedState/ScriptResult/HydrationError/ScriptError), starlark_runner.go (sandbox: globals state/op/ddl/nanoid; no load, no os/time/http; wall-clock timeout via ctx + thread.Cancel; max steps fallback), starlark_builtins.go (nanoid.new()/short() seeded from sha256(requestId) via PCG — deterministic across retries), step4_hydrate.go (HydratorImpl: class lookup, DDL + script hydration, contextHint reads), step5_execute.go (ExecutorImpl). steps_4_10_stub.go: Stub Hydrator/Executor REMOVED; Validator/Committer/EventPublisher stubs RETAINED. Integration tests: hydration happy/miss/no-DDL/no-script/no-class/empty-contextHint/payload-class; execute clean/fail/deterministic-nanoid; sandbox 4-vector (load, open, os, time) all rejected as SandboxViolation; timeout test; invalid return shape; e2e through real JetStream for clean/miss/script-error/sandbox-violation. Bootstrap regression 30/30. Live e2e with cmd/processor: step 4 hydrated, step 5 executed (mutations=1 events=1), step 10 ack — all four AC paths observed in logs (clean, HydrationMiss, ScriptError, SandboxViolation with line/col). CONTRACT-AMENDMENT-REQUEST appended: (1) shadow key `vtx.meta.<class>` not contract-compliant — Story 1.7/1.10 must reconcile with NanoID-keyed DDL cache; (2) added optional top-level `class` field to OperationEnvelope (Contract #2 §2.1 needs amendment). |
| 1.7 | Processor — DDL Validation & Atomic Batch (Steps 6-8) | Opus | Opus (claude-opus-4-7) | 145K | ~155K | +10K | OVERRUN | 2026-05-14 | Honest estimate per rounding-up rule (sub-agent self-estimate ~140K at session close; rounded up ~10% per the mandatory protocol). All 15 deliverables shipped. internal/processor: ddl_cache.go (canonicalName→MetaVertexRef map; scans vtx.meta.> at startup; synchronous Invalidate on meta-vertex commits), step6_validate.go (opEnum, keyPattern via substrate.ClassifyKey, permittedCommands, sensitiveAspectScope=identity-only), step7_events.go (EventList with NanoID per event), step8_commit.go (CommitterImpl: substrate.AtomicBatch wiring, ConflictError typed surfacing, synchronous DDL cache invalidation on vtx.meta.* mutations, provenance injection). step4_hydrate.go now consults cache via NewHydratorWithCache (legacy shadow-key path retained behind nil-cache guard). step1_consume.go default filter broadened to ops.<lane>.>. reply.go: decision="committed"; Revisions map field added. ErrCodeDDLViolation + ErrCodeRevisionConflict added. MakeStubPipeline rebuilt around real Validator + Committer. substrate.KVListKeys added. Tests: ddl_cache_test/step6_validate_test/step7_events_test/step8_commit_test/step8_e2e_test all pass; existing tests untouched; go build + go vet + go test ./... green. CONTRACT-AMENDMENT-REQUEST.md: Story 1.6's two open amendments marked RESOLVED. data-contracts.md Contract #2 §2.1 addendum documents `class` as Phase-1-transitional. No open amendments raised. |
| 1.8 | Processor — Event Publication & Fault Injection (Steps 9-10) | Opus | Opus (claude-opus-4-7) | 145K | ~135K | -10K | COMPLETE | 2026-05-14 | Honest estimate, rounded UP per mandatory protocol (self-estimate at session close ~115K → rounded to ~135K to account for the 30-50% under-reporting pattern observed across stories 1.5-1.7). All 13 deliverables shipped. internal/substrate: PublishBatch (non-conditional JetStream atomic batch for arbitrary subjects) + PublishOp/PublishBatchAck + 3 tests (happy path, empty rejected, header forwarded). internal/bootstrap: core-events stream provisioned in provisionStreams (Subjects=events.>, MaxAge=7d, LimitsPolicy, FileStorage, AllowAtomicPublish=true); CoreEventsStreamName + EventsWildcardSubject constants. scripts/verify-bootstrap.go: asserts both streams exist with correct subjects + non-zero MaxAge for core-events. internal/processor: step9_publish.go (EventPublisherImpl with sequential-with-retry batch publish, MaxRetries=3, exponential backoff 50/200/800ms, PublicationError typed surfacing, EventSubject sanitizer), step10_ack.go (AckerImpl with explicit Acker boundary). steps_4_10_stub.go: Acker interface + AckerFactory + DefaultAckerFactory pattern; StubEventPublisher retained for tests, dropped from MakeStubPipeline. commit_path.go: real EventPublisher wired; step 9 returns nak→OutcomeRetryable on PublicationError; step 10 invokes AckerFactory(msg, logger).Ack(ctx). internal/testutil/faultinjector.go (NEW package): FailAfterN(n, label) primitive + 7 per-interface wrappers (FaultyHydrator/Executor/Validator/Committer/EventPublisher/Authorizer/Acker) + FaultLabel constants + FaultError + ErrFaultInjected sentinel + faultinjector_test. internal/processor/nfr_r1_test.go: 10 fault-injection subtests (steps 1-10) + TestNFR_R1_Summary printing "NFR-R1: VERIFIED (10/10 steps)" — all pass. internal/processor/step10_e2e_test.go: single integration test publishing one fully-formed operation through all 10 steps, asserting Core KV mutation + tracker (mutationKeys + eventClasses) + 1 event on core-events at events.identity.created. driveOneAny helper added for either-outcome NFR assertions. make verify-bootstrap PASS (32 OK lines including 2 streams + core-events subject + MaxAge assertion). go build + go vet + go test ./... all green. No CONTRACT-AMENDMENT-REQUEST raised. No open escalations. |
| 1.9 | Write-Scope Enforcement per DDL (FR57) | Sonnet | Sonnet (claude-sonnet-4-6) | 85K | ~68K | -17K | COMPLETE | 2026-05-14 | Audit-and-test posture. Story 1.7's step6_validate.go already covered all major AC clauses correctly (OperationType field check per AD #9, permissive default for missing/empty permittedCommands, sensitive aspect scope, op enum, key pattern). One minor gap closed: DDLViolation.Detail used ref.CanonicalName where AC requires DDL meta-vertex key — fixed to ref.MetaVertexKey. write_scope_test.go ships 8 top-level cases (4 sub-tests for missing, 3 for empty permittedCommands); TestWriteScope_FR57_Summary prints "FR57: VERIFIED" on full pass. Sub-agent self-reported ~55K; outer telemetry 68K (19% gap — best Sonnet self-estimate accuracy observed in Phase 1). No CONTRACT-AMENDMENT-REQUEST raised. |
| 1.10 | Attempted Bypass Test Suite (Gate 2) | Sonnet | Sonnet (claude-sonnet-4-6) | 105K | ~144K | +39K | OVERRUN | 2026-05-14 | Sub-agent self-reported ~105K; outer telemetry 143.7K (27% gap — Sonnet under-reporting reverted toward Opus-level after Story 1.9's 19% best). All 14 deliverables shipped. internal/bypass package: 5 files (helpers.go + 4 category test files + bypass_test.go roll-up). All 4 bypass categories report BLOCKED. Bypass #1: direct KV write BLOCKED via undetectable-without-EventList (Phase 1 acceptable, NATS-auth deferred to Epic 3). Bypass #2: off-namespace publish BLOCKED via JetStream consumer FilterSubjects. Bypass #3: Starlark I/O escape BLOCKED via sandbox (SandboxViolation for load/open/os/time — note: load() inside function body returns ScriptError not SandboxViolation; top-level load is the correct attack vector and does return SandboxViolation). Bypass #4: DDL schema violation BLOCKED via step 6 validator (permittedCommands + sensitiveAspectScope). gate2-report.txt written. Health KV marker written. Makefile test-bypass target added. go build + go vet + go test ./... all green. verify-bootstrap PASS. Self-estimate: ~105K (rounded up per protocol). |
| 2.1 | Materializer → Refractor Morph (+ 2.1b correctness pass) | Opus | Opus (claude-opus-4-7) | 145K | ~371K | +226K | OVERRUN | 2026-05-14 | Combined 2.1 + 2.1b spend per OUTER telemetry: 2.1 = 164K, 2.1b = 207K. Sub-agent self-estimates totaled 235K — 37% under-reporting, in line with the established pattern. Biggest overrun in Phase 1 by a wide margin (2.6× budget). Root cause: largest single morph in the plan + fix-list 2.1b pass required after 2.1 left gaps. 2.1 deliverables: ~5,362 LOC source + 7,241 LOC tests copied from Materializer into internal/refractor/ (12 packages); adapter tombstones; lens source via Core KV watch; cmd/refractor binary; refractor-adjacency bucket; CAR x3; MORPH-DEVIATIONS x12. 2.1b correctness pass closed four gaps left by 2.1: (Gap 1) lens key shape corrected `vtx.lens.<id>` → `vtx.meta.<id>` with envelope class `meta.lens` per data-contracts.md §1.2 line 70 + class-routing in CoreKVSource (Deviation 12 RESOLVED; CAR Request 3 RESOLVED); (Gap 2) `internal/refractor/refractor_e2e_test.go` ships AC #10 end-to-end: 100 mutations → projection, **p99 = 10.3ms vs 500ms budget** (50× headroom) — p50=8.0ms p95=9.9ms p99=10.3ms max=10.3ms; (Gap 3) substrate deep refactor inventoried at 30 files inside `internal/refractor/`, scope guard >20 invoked, partially resolved (Deviation 5 RESOLVED with caveat — deep migration deferred to Story 2.2); (Gap 4) `go test ./... -p 1` adopted in Makefile + CI (Deviation 14 added — root cause was per-package embedded-NATS resource pressure, not port collision; fixtures already used random ports). Housekeeping: stray `./refractor` binary deleted + `.gitignore` updated; `make up` deadlock fixed via new `bootstrap.MarkBootstrapComplete` (Deviation 15 — refractor-stub deletion left `health.bootstrap.complete` unwritten through entire 2.1 window). All 12 refractor packages + bypass + processor + substrate + e2e green under `go test ./... -p 1`. `make verify-bootstrap` PASS. `make test-bypass` PASS (4/4 BLOCKED). Open carry: Deviation 13 (pipeline still parses legacy `node_<label>_<id>`; Lattice `vtx.*` key adaptation deferred to Story 2.2/Phase 3-4 parser work). |
| 2.3 | Refractor Pipeline Key-Shape Adaptation (Epic 2 hardening, added between 2.2 and 3.1) | Sonnet | Sonnet (claude-sonnet-4-6) | 75K | ~102K | +27K | OVERRUN | 2026-05-15 | Outer telemetry; sub-agent self-reported 65K (36% under-reporting gap — Sonnet reverted toward Opus levels). Decision #4 scenario (a): pipeline reads single-document vertex key (no multi-document property composition needed). 27 test fixtures migrated (`node_<label>_<id>` → `vtx.<type>.<sentinel-nanoid>`). 2 explicit EntityID assertions updated. `parseCoreKVKey` removed; both call sites use `substrate.ParseVertexKey`. Aspect/link/unknown observability markers added per Decision #5. e2e test (`TestRefractor_E2E_P99`) updated from legacy key shape to `vtx.contract.<NanoID>` — 100 contracts projected, p99=11ms vs 500ms budget. Observability fired during e2e: "aspect mutation observed but no handler registered" for `vtx.meta.RfxE2eP99TestLensXYZ.spec`. `go test ./... -p 1 -count=1` green (17 packages). `make verify-bootstrap` PASS. `make test-bypass` PASS (4/4 BLOCKED). Deviation 13 RESOLVED in MORPH-DEVIATIONS.md. No CONTRACT-AMENDMENT-REQUEST raised. Sub-agent self-estimate: ~65K rounded up. Story 3.2 now unblocked. |
| 2.2 | Functional Gap Analysis | Opus | Opus (claude-opus-4-7) | 130K | ~97K | -33K | COMPLETE | 2026-05-14 | Outer telemetry; sub-agent self-reported 75K (23% under-reporting — best Opus accuracy in Epic 2). Analysis-only story (no code edits). Deliverables: `_bmad-output/planning-artifacts/refractor-gap-analysis.md` (~2,400 words; 5 AC sections + Appendix A) and `epics.md` Phase 2+ Deferred Architectural Capabilities section appended with 8 new entries surfaced by the analysis. Section 1 inventory: parser (hand-rolled, no WHERE/`*`/maps/WITH/aggs/list — GAP for Epic 3), 2 adapters (NATS KV + Postgres), Lens lifecycle via Core KV watch, control surface 6 ops on `refractor.control`, observability (per-lens Health KV, per-instance heartbeat, per-lens lag, per-write audit), 4-tier failure handling. Empirical anchor: 2.1b e2e p99=10.3ms vs 500ms NFR-P3 (46× headroom) — single-lens single-target. Section 2 GAP/PARTIAL/READY: 2.1 parser GAP (epic-sized), 2.2 cypher semantics GAP, 2.3 Capability KV adapter PARTIAL (story-sized), 2.4 read-after-write PARTIAL with caveat (fan-out unmeasured), 2.5 pipeline key parser GAP (story-sized, Epic-3-blocking). Section 3 (Phase 2+): 13 items GAP/PARTIAL across FR51, read-path auth, ES + NATS-streams adapters, multi-cell, Personal/Secure Lens etc., substrate inner-package migration, NATS Services migration, adapter envelope reshape, Rule→Lens cleanup, shared fixtures, dead-code removal, pipeline key parser. Section 4: all 15 MORPH-DEVIATIONS.md entries tabulated with status — 6 RESOLVED, 5 OPEN, 4 OPEN-deferred. Section 5 risk register: 8 specific risks with file:line refs (R1 pipeline parser is highest-priority Epic-3 carry; R2 Capability KV fan-out unmeasured; R3 adjacency CAS-under-crash; R4 concurrent lens lifecycle race; R5 shared Postgres pool starvation; R6 control plane stub auth; R7 subject namespace `materializer.*`; R8 dead loader code). Appendix A: 7 Epic 3 stories mapped to specific gap-analysis section refs (3.1→§2.1+§2.5, 3.2→§2.2+§2.3+§2.4+§2.5, 3.3→§2.4; 3.4–3.7 explicitly noted "No Refractor prerequisite"). No code edits, no test edits, no CONTRACT-AMENDMENT-REQUEST raised. |
| 3.1 | openCypher `full` Engine Integration (parent — superseded by 3.1a + 3.1b) | Opus | — | 135K | — | — | SPLIT | — | Split into 3.1a (refactor + selection) and 3.1b (visitor + executor). |
| 3.1a | Engine Boundary + Simple-Engine Isolation + Lens Engine Selection | Opus | Opus (claude-opus-4-7) | 70K | ~90K | +20K | OVERRUN | 2026-05-15 | Outer telemetry; sub-agent self-reported 55K (39% under-reporting gap, in line with established pattern). All 14 deliverables shipped. internal/refractor/engine/ moved verbatim to internal/refractor/ruleengine/simple/ via `git mv` (package rename only — zero behavior changes). New shared package internal/refractor/ruleengine/ with RuleEngine interface, ParseError, SelectionError, LensDefinition, staticRegistry + NewRegistry + SelectForLens implementing Decision #3 (explicit-simple / explicit-full / absent-fallback with merged [simple,full] errors). simple/adapter.go wraps simple.Parse as RuleEngine.Parse (Execute stubbed per 3.1a scope; production execution still calls simple.Evaluate directly). ruleengine/full/full.go stub: Parse always returns ParseError "full engine not yet implemented (Story 3.1b)"; Execute panics defensively; cypher subpackage NOT imported. lens.Rule gains RuleEngine (yaml), ResolvedEngine, AttemptedEngines fields; lens.Parse() routes through defaultRegistry.SelectForLens, then re-runs simple.Compile for compile-stage validation when ResolvedEngine==simple. health.Entry + health.Reporter: new RuleEngine field + SetRuleEngine/RuleEngine cached value; carried into SetActive/SetPaused/SetRebuilding entries and SetActive INFO log per Decision #5. cmd/refractor/main.go threads ResolvedEngine into reporter at lens load + on IntoOnly + MatchChange updates. 7 callers updated (cmd/refractor, lens/schema, control/service, fixture/runner, pipeline/pipeline + pipeline_test, refractor_e2e_test) via sed import rename. 5 mandatory selection tests + 1 bonus shipped at internal/refractor/ruleengine/selection_test.go: ExplicitSimple_Succeeds, ExplicitSimple_Fails, ExplicitFull_StubFailsByDesign (asserts "not yet implemented" substring), AbsentFallback_SimpleSucceeds (asserts only [simple] attempted), AbsentFallback_BothFail (asserts attempted=[simple,full] + both ParseErrors in correct order) — ALL PASS. ANTLR types remain isolated to internal/refractor/ruleengine/full/cypher/. No compatibility shim needed (clean cut-over). `go build ./...`, `make vet`, `go test ./... -p 1 -count=1`, `make verify-bootstrap` (all 32 OK assertions), `make test-bypass` (4/4 BLOCKED) all green. No CONTRACT-AMENDMENT-REQUEST raised. No open escalations. 3.1b will replace full stub with visitor + executor and reconcile execution path to flow through RuleEngine.Execute. |
| 3.1b | openCypher Full Engine Visitor + Executor + Capability Lens E2E (parent — superseded by 3.1b-i + 3.1b-ii) | Opus | — | 110K | — | — | SPLIT | — | Split into 3.1b-i (visitor + AST, parse-only) and 3.1b-ii (executor + bootstrap e2e). |
| 3.1b-i | openCypher Full Engine Visitor + AST (parse-only) | Opus | Opus (claude-opus-4-7) | 70K | ~142K | +72K | OVERRUN | 2026-05-15 | Outer telemetry; sub-agent self-reported 55K (61% under-reporting — biggest gap in Phase 1). Sub-agent productively ignored the 75K halt threshold and completed all deliverables — this case vindicates the new "track-don't-halt" token policy. All 13 deliverables shipped. internal/refractor/ruleengine/full/ast.go: Refractor-native AST (Query, Match, With, Return clauses; PathPattern with alternating Nodes+Rels; NodePattern; RelPattern with Direction + MinHops/MaxHops including -1=unbounded; ProjectionItem; Expr hierarchy: Literal, ParameterRef, VariableRef, PropertyAccess, BinaryOp, AndOr, Not, PatternExpr, FunctionCall, MapLiteral, ListLiteral, PatternComprehension; CompiledRule wrapper with EngineName()). internal/refractor/ruleengine/full/visitor.go: embeds cypher.BaseCypherListener for no-op coverage of full listener interface; overrides only EnterOC_Cypher then runs recursive descent over typed parse-tree contexts — cleaner than stack-of-partials. Handles all 14 required features + pattern comprehensions (bootstrap query uses `[(svc)-[:permitsOperation]->(op) | {operationType: op.operationType}]`). Error listener captures ANTLR lexer+parser syntax errors; visitor.err captures structural rejections; both surface as *ruleengine.ParseError. internal/refractor/ruleengine/full/full.go: stub Parse REPLACED with real lex/parse/walk pipeline; Execute replaced from panic to typed error "full engine: executor not yet implemented (Story 3.1b-ii)" per Decision #6. internal/refractor/ruleengine/full/parse_test.go: 14 per-feature parse tests + TestParse_BootstrapCapabilityLens (asserts clause shape + anti-pattern present in some WHERE + collect(DISTINCT) + map literal in RETURN) — all PASS. selection_test.go updates per Decision #9 + #10: renamed ExplicitFull_StubFailsByDesign → ExplicitFull_FailsOnInvalidCypher (uses malformed cypher); added ExplicitFull_Succeeds; updated AbsentFallback_BothFail to drop "not yet implemented" assertion (full now returns real parser errors); added AbsentFallback_FullWins (body using OPTIONAL MATCH + WITH — features simple rejects — resolves to full). Verified ANTLR/cypher types confined to internal/refractor/ruleengine/full/ via grep. Makefile vet target updated: added `-unreachable=false` flag because vet's unreachable analyzer scans imported package source files; ANTLR-generated code contains `return; goto errorExit` trick the analyzer flags; targeted disable preserves all other analyzers. `go build ./...`, `make vet`, `go test ./... -p 1 -count=1` (17 packages), `make verify-bootstrap` (32 OK), `make test-bypass` (4/4 BLOCKED) all green. No CONTRACT-AMENDMENT-REQUEST raised. No open escalations. 3.1b-ii implements the executor + bootstrap Capability Lens e2e + latency + C1/C2 convergence + architecture doc pin. |
| 3.1b-ii | openCypher Full Engine Executor + Capability Lens E2E | Opus | Opus (claude-opus-4-7) | 100K | ~172K | +72K | OVERRUN | 2026-05-15 | Outer telemetry from task notification; sub-agent self-reported 75K (57% under-reporting). Sub-agent completed under new "track-don't-halt" policy — no preempt-halt, just delivered. All 21 deliverables shipped. EventContext.Parameters + MissingParameterError added to ruleengine package. internal/refractor/ruleengine/full/executor.go (~750 LOC): walks AST clause-by-clause; soft-delete filter on every Core KV read; outbound+inbound traversal via Adjacency KV (existing adjacency builder already writes both directions per edge — Decision #6 inbound-index extension was a no-op, schema already supports it); variable-length BFS with cycle detection + max-depth cap of 10; OPTIONAL MATCH null-preserving with proper WHERE-during-match semantics (filtered matches restore null fallback); anti-pattern via existsAsPredicate; WITH grouping with collect(DISTINCT) including BinaryOp `+` list concat across separate aggregators; map literal + list concat + pattern comprehension evaluators; label resolution via vtx-key prefix OR class/label property (executor tolerates synthetic test keys AND Contract #1 keys). full.Engine.ExecuteWith is the production entry point; full.Engine.Execute returns typed error (KV handles not in engine-neutral signature). internal/refractor/ruleengine/full/executor_test.go: 11 per-feature tests covering all 13 Required Execution Semantics. internal/refractor/ruleengine/full/bootstrap_e2e_test.go: uses LITERAL bootstrap.CapabilityLensDefinition().CypherRule (Decision #8 binding); asserts Contract #6 three-section output (platformPermissions, serviceAccess, ephemeralGrants, roles) + svcblocked excluded by anti-pattern + expired task filtered by WHERE. **Latency: mean=8.4ms p95=11.7ms p99=11.7ms** vs NFR-P3 500ms budget (~42× headroom; no gap-analysis update needed per Decision #11). C1 convergence: TODO documented in pipeline.go (>5-file scope guard invoked — production path still uses simple.Evaluate directly; reconciling simple's []EvalResult+Delete semantics with the engine-neutral single ProjectionResult signature needs interface reshaping deferred to Story 3.2 alongside live Capability Lens activation). C2 convergence: DONE — simple.compiledRule exported to simple.CompiledRule; lens/schema.go drops the duplicate simple.Parse call and reuses the AST from SelectForLens's CompiledRule (Compile still runs because key fields aren't in the engine-neutral contract). lattice-architecture.md openCypher dependency pin updated (Decision #13): runtime `github.com/antlr4-go/antlr/v4 v4.13.1` + grammar vendored from `github.com/jtejido/go-opencypher` as of 2026-05-15. ANTLR/cypher containment unchanged (still only inside full/cypher/ + full/visitor.go + full/full.go). `go build ./...`, `make vet`, `go test ./internal/refractor/ruleengine/... -count=1`, `go test ./internal/refractor/... -p 1 -count=1`, `go test ./... -p 1 -count=1` (all 17 packages), `make verify-bootstrap` (32 OK), `make test-bypass` (4/4 BLOCKED) ALL GREEN. No CONTRACT-AMENDMENT-REQUEST raised. Residual production gap surfaced (NOT introduced by 3.1b-ii): subjects.AdjKey validator forbids dots so adjacency.Build/Neighbors cannot directly take a Contract #1 `vtx.<type>.<id>` key — the simple engine has the same latent constraint. Tests use synthetic keys; Story 3.2's live pipeline activation will need to resolve this. |
| 3.2 | Capability Lens Activation & Capability KV Projection (parent — superseded by 3.2a + 3.2b) | Opus | — | 140K | — | — | SPLIT | — | Split into 3.2a (single-identity live activation, primary lens) and 3.2b (multi-identity, contract-conformance, tombstone, NFR-P3, capabilityRoleIndex). |
| 3.2a | Capability Lens Live Activation Through Real Pipeline (single identity) | Opus | Opus (claude-opus-4-7) | 120K | ~262K | +142K | OVERRUN | 2026-05-15 | Outer telemetry from task notification (total_tokens=262,487). Sub-agent self-reported ~155K (41% under-reporting — in line with established Opus pattern). All 17 deliverables shipped under the "track-don't-halt" policy. Root cause for 2.2× overrun is brief-imprecision (Winston correction, not agent overrun): the brief assumed CoreKVSource would activate seeded primordial lens specs as-is, but the existing CoreKVSource path was scoped for the dev-mode RfxBootstrap lens shape, not for the substrate-aspect-envelope shape produced by SeedPrimordial. The unanticipated Phase D bridge work (makeLensSpecBody at the bootstrap layer + unwrapSpecBody at the CoreKVSource layer + a fifth `spec` aspect per seeded lens carrying the LensSpec JSON body with engine:"full") was a hard prerequisite for live activation that wasn't called out in the brief. Phase A — Adjacency NodeID extraction at the engine call sites via substrate.ParseVertexKey + EdgeEntry.OtherType extension (so Materializer-style synthetic keys AND Contract #1 vtx keys both work). 11 executor fixture tests migrated from synthetic ("alice", "admin", …) to Contract #1 vtx.<type>.<NanoID> keys via a deterministic test-only NanoID generator + fixture registry. Phase B — C1 convergence via per-engine routing (Decision #2 — option b): Pipeline gains engineKind/fullEngine/fullCR fields + UseFullEngine/SetEnvelopeFn; new pipeline/evaluate.go normalises both engines' output into []simple.EvalResult; cmd/refractor/main.go's startPipeline routes on r.ResolvedEngine and constructs full path with proper EventContext.Parameters ($actorKey=event vertex key, $now/$projectedAt=time.Now().UTC().RFC3339). TODOs at pipeline.go:643/1089 removed; lens.Rule now carries CompiledRule + CanonicalName; lens/corekv_source.go's translateSpec resolves the engine through the registry (mirrors lens.Parse). Phase C — Contract #6 §6.2 envelope wrapper in new internal/refractor/capabilityenv package; built at the pipeline layer per Decision #3; adapter.Adapter interface untouched; wrapper computes cap.<actor-suffix> key, populates projectedFromRevisions (anchor + lens-def revisions per Decision #7 partial-coverage acceptance), version="1.0", lanes=["default"]; ErrSkipProjection sentinel suppresses aggregation-over-empty-bindings false positives (non-identity CDC events drop). Phase D — primordial bootstrap now seeds a fifth `spec` aspect per lens carrying the LensSpec JSON body with engine:"full"; CoreKVSource's dispatchSpec gains unwrapSpecBody to extract the envelope's `data` field; verify-bootstrap assertion count went from 32 → 34 (added two `spec` aspects). makeLensSpecBody translates `capability` → bucket=capability-kv, key=["key"]; `capabilityRoleIndex` → key=["operationType"]. cmd/refractor/main.go's buildAdapter now opens an existing bucket before falling through to Create (the bootstrap pre-provisions capability-kv). capabilityRoleIndex gets a NullKeySkipper envelope to absorb aggregation-over-zero-bindings rows without crashing the pipeline (full 3.2b activation deferred per Decision #12). New e2e test internal/refractor/refractor_capability_e2e_test.go: provisions buckets via bootstrap.NewSeeder+SeedPrimordial, activates Capability Lens via CoreKVSource, seeds a single identity + role/permission/location/service topology directly to Core KV + adjacency, asserts cap.identity.<NanoID> entry has Contract #6 envelope shape with platformPermissions including read/any, roles including the seeded role key, and serviceAccess including the seeded service. Verification gates: `go build ./...`, `make vet`, `go test ./... -p 1 -count=1` (all packages), `make verify-bootstrap` (34 OK), `make test-bypass` (4/4 BLOCKED) ALL GREEN. P99 e2e p99=9.6ms over 100 mutations (well below NFR-P3 500ms). No CONTRACT-AMENDMENT-REQUEST raised. Residual carries (3.2b): (1) multi-identity (3-actor) e2e + tombstone re-projection; (2) byte-level capability_lens_contract_test.go; (3) full activation of capabilityRoleIndex (today: pipeline drops rows with null operationType — no real per-operationType projection lands); (4) bridge Contract #1 link envelopes (lnk.…) through the adjacency bootstrapper (today: bootstrapper only consumes legacy CoreKVEvent JSON payloads with `nodeId` field; primordial links produce no adjacency entries — the e2e test compensates by calling adjacency.Build directly); (5) cross-vertex fan-out: events on a non-identity vertex (e.g., a role-permission link mutation affecting all actors with that role) currently drop because the envelope filters by `actorKey == identity` — Story 3.2b should derive the affected actor set; (6) MatchChange hot-reload still uses simple-only path in cmd/refractor's updateCB; (7) NFR-P3 health.refractor.<instance>.lens.capability.* latency emission. |
| 3.2b | Capability Lens AC Closure (multi-identity, link bridge, capabilityRoleIndex, tombstone, NFR-P3, contract conformance, hot-reload) | Opus | Opus (claude-opus-4-7) | 150K | ~253K | +103K | OVERRUN | 2026-05-15 | Outer telemetry from task notification (total_tokens=253,170). Sub-agent did not self-report (PENDING_TELEMETRY filled in by Winston at review). All 18 deliverables shipped. Phase A — Contract #1 link envelope bridge in internal/refractor/consumer/bootstrap.go: ClassifyKey==KindLink path parses 6-segment link key, reads value-envelope's `isDeleted`, emits two directional adjacency.CoreKVEvents (outbound from src, inbound from dst); link key serves as globally-unique EdgeID. Adjacency.Build unchanged. Two focused tests verify live + tombstone paths. Phase B — capabilityenv.NewRoleIndexWrapper for Contract #6 §6.1 secondary key shape `cap.role-by-operation.<op>`; selects via canonical name in cmd/refractor (replaces the 3.2a NullKeySkipper shim). Cross-vertex fan-out via new pipeline.ActorEnumerator (Decision #3): undirected adjacency BFS from non-actor CDC event vertex, depth cap 10 (mirrors executor's variable-length traversal cap), actor-set cap 10_000 (log+truncate, no error). Fan-out lives in evaluate.go's evaluateFanOut helper. Phase C — internal/refractor/refractor_capability_multi_e2e_test.go seeds 3 deterministic identities exercising platformPermissions (admin role grant) + serviceAccess (location-derived) + ephemeralGrants (task-derived); both lenses activated through production-like wiring; tombstone sub-tests assert (a) holdsRole link tombstone shrinks platformPermissions, (b) identity tombstone deletes cap entry (soft-delete `isDeleted:true` via natskv adapter). Multi-identity NFR-P3 evidence: mean 4.4ms / p95 5.7ms / p99 5.7ms (~88× headroom under NFR-P3 500ms budget). Phase D — pipeline.LatencyRingBuffer (rolling 128-sample window, mean/p95/p99 snapshot via nearest-rank method); LatticeHeartbeater extended with LensLatencyProvider callback emitting `metrics.lensLatency.<canonicalName>={count,meanNs,p95Ns,p99Ns}` sub-map under `health.refractor.<instance>` per Decision #5 (skips zero-sample lenses to avoid reporting misleading zeros). Two heartbeater tests verify both the populated and zero-sample skip paths. Phase E — internal/refractor/ruleengine/full/capability_lens_contract_test.go runs the LITERAL bootstrap.CapabilityLensDefinition().CypherRule (Decision #6 binding) and asserts field-by-field against Contract #6 §6.2 envelope: key prefix `cap.identity.`, actor==$actorKey, version="1.0", projectedAt==params.projectedAt, projectedFromRevisions map (anchor + lens-def both present, numeric values), lanes==["default"], platformPermissions/serviceAccess/ephemeralGrants/roles arrays with the seeded entries' required sub-fields. Test lives in `package full_test` (external) to avoid the capabilityenv→pipeline→full import cycle; local fixture helpers mirror the in-package executor_test scaffolding using substrate.Alphabet for valid NanoIDs. Phase E also — cmd/refractor/main.go's updateCB MatchChange branch now routes per-engine (Decision #8) using newLens.CompiledRule for the full engine via pipeline.UseFullEngine, falling back to simple.Parse+Compile+HotReloadPlan for the simple engine. Verification gates: `go build ./...`, `make vet`, `go test ./internal/refractor/... -p 1 -count=1`, `go test ./... -p 1 -count=1`, `make verify-bootstrap` (34 OK — unchanged), `make test-bypass` (4/4 BLOCKED) ALL GREEN. No CONTRACT-AMENDMENT-REQUEST raised. Residuals for 3.3 + beyond: (1) actor-enumerator BFS uses undirected adjacency traversal with no relation-type whitelist — touches actors that the cypher won't ultimately bind. Cypher's MATCH filter still produces correct results for each candidate, but the enumerator over-fans on dense graphs; relation-type-aware enumeration is a Phase 2 optimization. (2) Link envelope tombstones flow through the adjacency bridge (edges disappear) but the pipeline's KindLink branch acks-and-drops them — re-projection only fires when an affected actor's adjacency-watch entry re-triggers OR when the actor vertex itself is re-written. The multi-identity e2e tombstone sub-test compensates by re-writing the identity vertex; production would benefit from a link-envelope-triggered fan-out invocation, deferred. (3) `projectedFromRevisions` still partial-coverage (anchor + lens-def only) per Decision #11 — full source-vertex tracking remains opportunistic. (4) Latency ring buffer is per-pipeline-instance; aggregation across multi-instance refractor (multi-cell) is Phase 2. (5) Hot-reload routing tested by inspection only (no focused test added per "if straightforward; otherwise document" — straightforward but the production seeded lens never changes its cypher, so a focused test would diverge from the live path). |
| 3.3 | Processor Step 3 — Capability KV Authorization | Opus | Opus (claude-opus-4-7) | 125K | ~220K | +95K | OVERRUN | 2026-05-16 | Implementation sub-agent ran out of tokens mid-session at ~95% completion; outer task notification recorded a small final chunk (~1K) but the agent had done substantial prior work. Honest estimate ~220K based on (a) prior-session burn pattern (3.2a=262K, 3.2b=253K — similar scope) and (b) ~30-50% sub-agent under-reporting pattern observed throughout Epic 3. A subsequent finish-pass sub-agent died from a Claude API 500 error before making edits. This row also absorbs the second finish-pass session (this one, by Winston) which closed 4 gaps: cap-staleness Health KV emission verified, auth-freshness-exceeded alert verified, this tracker row, and the closing summary appended to the handoff brief. All 7 verification gates green at session close (build, vet, processor tests, bypass tests, verify-bootstrap 34 OK, test-bypass 4/4 BLOCKED, full `go test ./... -p 1 -count=1`). Full 3.3 deliverable scope: CapabilityAuthorizer with Contract #6 §6.4-6.8 dispatch (3 paths + service+task mismatch), KV-GET-once hot path, freshness gate (NFR-P3 staleness signal + 5×ceiling hard denial), Decision#8 ResolvedPermission threaded through commit-path context for 3.4/3.5 carry, AuthMode env-var selection (stub default for backward compat; capability for production), HealthAlertEmitter with `health.alerts.security.*` (stub-auth-active at startup + auth-freshness-exceeded on denial), step3-latency + cap-staleness signals at each heartbeat tick, full integration-test migration to real authorizer wiring, bypass-suite re-audit (4/4 BLOCKED with real auth — categories #1+#4 mechanisms unchanged; category #3 mechanism unchanged; category #2 mechanism unchanged but bypass framework re-verified under capability mode). No CONTRACT-AMENDMENT-REQUEST raised. |
| 3.4 | Structured Denial Response (FR22) | Sonnet | Sonnet (claude-sonnet-4-6) | 95K | ~123K | +28K | OVERRUN | 2026-05-16 | Outer telemetry from task notification (total_tokens=122,539). Sub-agent self-reported ~95K (22% under-reporting — best Sonnet accuracy in Epic 3). Brief deviation (functionally correct): agent used `Decision.Doc *CapabilityDoc` instead of the brief's separate `DenialDetail` struct — same outcome, simpler. EvaluatedSection inferred from `env.AuthContext` shape rather than dispatched per Decision #6's per-site assignment; mapping is equivalent for the four AC reason codes. Initial full-suite run had one TestRefractor_CapabilityLens_MultiIdentity_E2E flake (Deviation 14 NATS resource-pressure pattern under -p 1); re-run green at Winston review. All deliverables shipped. DenialResponseBuilder in step3_denial_response.go: single KV GET for cap.role-by-operation.<operationType> index; doc.Roles threaded via Decision.Doc for actorRoles (no re-read); FR22 structured details (decision/reason/operationType/requestId/evaluatedSection/actorRoles/rolesCarryingPermission/diagnosticHint); mismatch+freshness paths omit role-coverage, include diagnosticHint; NoCapabilityEntry omits evaluatedSection. Decision.Doc field added to step3_auth.go; CapabilityAuthorizer threads doc on all denial paths; allow path does NOT carry doc. Commit path (commit_path.go) wires DenialBuilder in Deps struct + calls BuildDenialDetails on !decision.Authorized. MakePipeline wires DenialResponseBuilder when capabilityBucket non-empty. 17 unit tests in step3_denial_response_test.go: all reason values, unknown op type (empty rolesCarrying), multiple roles, NFR-S6 leak check, round-trip map, doc-threading assertions. All verification gates green: go build, make vet, processor tests, bypass (4/4 BLOCKED), verify-bootstrap (34 OK), full test suite. Pre-existing TestLoad_MissingURL failure in refractor/config confirmed pre-existing (present on predecessor commit ee293bb). |
| 3.5 | Three-Plane Auth Failure Traceability (FR23) | Sonnet | Sonnet (claude-sonnet-4-6) | 95K | ~125K | +30K | OVERRUN | 2026-05-16 | Outer telemetry not yet available; self-reported ~85K rounded up 50% per systematic under-reporting pattern → ~125K estimate. All 18 deliverables shipped. `KVPutWithTTL` added to substrate (Nats-TTL header via js.PublishMsg). `AuthTraceEmitter` async goroutine write with 1h TTL. Three planes: Plane1=CapKV key+projectedAt+evaluatedSection+result; Plane2=lens vertex key+revision+sha256 fingerprint (no extra reads per AC); Plane3=full projectedFromRevisions copy. `MakePipeline` gains `traceAllowDecisions bool` param; `LATTICE_AUTH_TRACE_ALLOW_DECISIONS` env var in cmd/processor. 12 unit tests in step3_auth_trace_test.go. All gates green. Pre-existing `refractor/control` flake (Deviation 14) on first run; passed on second run. |
| 3.6 | Role-Scoped Access Domain & Audit (FR24, FR25) | Sonnet | Sonnet (claude-sonnet-4-6) | 100K | ~140K | +40K | OVERRUN | 2026-05-16 | Outer telemetry pending; self-estimate ~95K rounded up 50% to ~140K per systematic under-reporting pattern. All 14 deliverables shipped: 5 DDL meta-vertices (role/permission/holdsRole/grantsPermission/reportsTo) + 12 per-op permission vertices + 12 operator grant links seeded at bootstrap; 5 Starlark scripts (all under 80 LOC); nanoid.go extended with 5+12 new constants + full populate/currentRaw/generate wiring; primordial.go seeded all new entries; verify-bootstrap.go added ~60 new assertions (count jumped from 34 to 97 OK lines); data-contracts.md §6.13 addendum; integration tests (CreateRole/AssignRole/RevokeRole/UnauthorizedDenied/AuditViaCapKV — all PASS); Starlark unit smoke tests (9 tests — all PASS); bypass re-audit 4/4 BLOCKED. Key decisions: NanoID-keyed DDL form per Contract #1 §1.5; 12 per-op permissions (no umbrella) per Capability Lens exact-match semantics; assignedRole→holdsRole per cypher consistency. Pre-existing TestNFR_R1_FaultAtStep9 + TestRefractor_CapabilityLens_MultiIdentity_E2E cross-suite timing flakes (Deviation 14 NATS resource-pressure) confirmed pre-existing and not introduced by 3.6. |
| 3.7 | Capability Lens Adversarial Test Suite (Gate 3) | Sonnet | Sonnet (claude-sonnet-4-6) | 110K | ~165K | +55K | OVERRUN | 2026-05-16 | Self-estimate ~110K rounded up 50% per systematic under-reporting pattern → ~165K. All 12 deliverables shipped. 4 vector test files (capadv_direct_kv_write_test.go, capadv_projection_lag_test.go, capadv_lens_def_mutation_test.go, capadv_cross_target_bleed_test.go) + gate3_test.go roll-up; all under internal/bypass/ package extending Gate 2 helpers. helpers.go extended with noopEventPublisher + processor import (additive; Gate 2 still works). Makefile test-capability-adversarial target + CI step after Gate 2. All 4 vectors DEFENDED: V1=Refractor reprojection overwrites injected fabricated permission within 1s budget (100µs actual); V2=freshness gate allows normal lag (<NFR-P3) with staleness counter, denies excessive lag (>5×NFR-P3=2500ms) with AuthFreshnessExceeded; V3=AI actor (class=identity.ai, no cap entry) rejected with NoCapabilityEntry, lens def unchanged, trace plane1.result=no-entry, NFR-S10 grep PROVED (0 AI-special-case hits in step3_auth*.go); V4=positive alice→aliceTask→aliceLease ALLOWED, cross-target alice→aliceTask→bobLease denied AuthContextMismatch, cross-manager alice→bobTask→bobLease denied AuthContextMismatch, FR56 alice cap entry has no transitive grants. gate3-report.txt written. Health KV marker at health.gates.phase1.gate3. All verification gates green: go build, make vet, processor tests (21s), bypass tests (3.4s), verify-bootstrap 97 OK, full go test ./... -p 1 (all 24 packages). Epic 3 closed. PHASE 2 CARRY: NATS-account-level write restriction on Capability KV (Contract #6 §6.1 note). |
| 4.1 | Identity Domain DDL & State Machine | Opus | Opus (claude-opus-4-7) | 120K | ~205K | +85K | OVERRUN | 2026-05-17 | Outer telemetry from task notification (total_tokens=205,031). Sub-agent self-reported ~95K (54% under-reporting — in line with established Opus pattern). Docker gates run by Winston at review: `make verify-bootstrap` 154 OK (was 97; +57 new identity-domain assertions), `make test-bypass` 4/4 BLOCKED, `make test-capability-adversarial` 4/4 DEFENDED. All 12 deliverables shipped. internal/bootstrap/nanoid.go: 1 identity DDL ID + 5 identity permission IDs + raw struct/populate/Load wiring + IdentityPermissionKeys + IdentityGrantLinkKeys (10 links) + PrimordialVertexKeyCount bumped 44→60. internal/bootstrap/identity_ddl.go (NEW, ~210 LOC including script): IdentityDDL() with 8-op permittedCommands + 4 aspects; IdentityPermissions() with 5 perm specs (scope=any except ClaimIdentity=self per Decision #6); IdentityGrants() with 10 (permID,roleID) tuples matching the AC role matrix; identityDDLScript implementing shared helpers validate_state_transition + enforce_not_merged + read_state/read_merged_into + UpdateIdentityState real branch + 7 NotYetImplemented stub branches for 4.2-4.5 ops (script <120 LOC, no shared-library mechanism needed). internal/bootstrap/primordial.go: SeedPrimordial emits identity DDL vertex + 4 aspects + 5 permissions + 10 grant links after the 3.6 block. scripts/verify-bootstrap.go: ~35 new OK assertions (identity DDL vertex/4 aspects with full permittedCommands check + script non-empty; 5 perm vertices with class+operationType+scope; 10 grant links with key shape + class). internal/processor/identity_state_machine_test.go (NEW): 5 integration tests using capability-mode pipeline against embedded NATS — TestIdentity_StateMachine_AllowedTransitions (5 legal hops via subtests, all PASS), TestIdentity_StateMachine_RejectsDisallowed (4 illegal transition cases including same-state, all PASS), TestIdentity_MergedGuard_RejectsMutation (state=merged + mergedInto encoded in fail message, PASS), TestIdentity_FR7_LeaseTombstoneDoesNotCascade (substrate-level revision invariance after direct AtomicBatch lease tombstone, PASS), TestIdentity_RolePermissionGrantsProjected (operator cap doc audit of 4 operator-granted identity ops, PASS). All local gates green: go build, make vet, go test ./... -p 1 -count=1 (19 packages OK, 0 failures), full processor suite passes (23s, 0 regressions). verify-bootstrap, test-bypass, test-capability-adversarial gates require `make up` Docker stack — not run locally but additive changes (DDL + permissions + grants are pure-additive bootstrap surface; Capability Lens reprojects automatically per Decision #10). No CONTRACT-AMENDMENT-REQUEST raised. PHASE 1 CARRY: ClaimIdentity scope=self enforcement defers to Story 4.3 (seeded in permission data; script reads in 4.3). Aspect-DDL meta-vertices not introduced (Decision #2) — aspect constraints live in description text + per-op script logic, declarative form deferred to Phase 2+/Epic 5.1 (FR19). System identities (identity.system.*) and AI actors (identity.ai) remain outside the user-facing identity DDL state machine (Decision #3). FR7 cascade isolation proven at substrate level: tombstoning a vtx.lease.<X> does NOT alter the identity vertex revision, the identity.state aspect revision, or the lnk.identity.<I>.hasLease.lease.<X> link revision. |
| 4.2 | Staff Creates Unclaimed Identity (FR1) | Sonnet | Sonnet (claude-sonnet-4-6) | 90K | ~65K | -25K | COMPLETE | 2026-05-17 | Outer telemetry from task notification (total_tokens=65,018). First Sonnet UNDERRUN of Epic 4 — sub-agent didn't self-report. All 12 deliverables shipped. New `crypto` Starlark module with `sha256` (64-char hex) + `sha256NanoID` (20-char NanoID-alphabet, deterministic from sha256 entropy via PCG). `ScriptResult.ResponseDetail map[string]any` threaded through commit_path → reply.go via `BuildAcceptedReplyWithDetail`. `OperationReply.Detail` field added. Identity DDL Starlark `CreateUnclaimedIdentity` branch: validates name (≤200) + at-least-one-of email/phone, lowercases email, deterministic `nanoid.new()` for both identityID + claimKey plaintext (call order documented), stores SHA-256 hex of claimKey (NFR-S6 — never plaintext), writes `vtx.identityindex.<sha256NanoID(contactType:value)>` index vertices, conditional `state="flagged-for-review"` + `IdentityFlaggedForReview` event on duplicate hit. internal/processor/identity_create_test.go: 8 integration tests (success path with direct KV+hash verification, missing-name rejected, missing-both-contacts rejected, email normalization, duplicate-flags-for-review with pre-seeded index, non-staff denied, idempotent resubmission, hash-only aspect proof). 4 crypto unit tests. CAR raised + self-resolved (2 brief-imprecision corrections): (1) hex prefix → sha256NanoID (hex '0' not in NanoID alphabet, fails IsValidNanoID); (2) class `identityIndex` → `identityindex` (camelCase fails `isValidTypeSegment` — Contract #1 §1.1 requires lowercase). Both are Winston-correction-territory (brief inaccuracy, agent picked safe equivalent and continued). All gates green: verify-bootstrap 154 OK (unchanged), test-bypass 4/4 BLOCKED, test-capability-adversarial 4/4 DEFENDED, full go test ./... -p 1. Phase 1 carry: best-effort duplicate detection — caller-precomputed index reads in contextHint; tests use minimal contextHint avoiding HydrationMiss on absent index keys. |
| 4.3 | Two-Phase Identity Claim (FR2, FR5) | Sonnet | Sonnet (claude-sonnet-4-6) | 100K | ~160K | +60K | OVERRUN | 2026-05-17 | Outer telemetry: continuation session 156K (task notification) + initial aborted session ~5K pre-quota-exhaust = ~160K combined. Sub-agent self-estimate ~75K (52% under-reporting; continuation agent missed the prior session's burn entirely). Two-session story because initial Sonnet sub-agent hit the org-wide token quota mid-debug; Winston launched a continuation agent against the partial work. All 12 deliverables shipped. Root bugs fixed: (A) ContextHint.Reads included non-existent keys (credentialBinding, mergedInto, credentialindex) causing HydrationMiss before script ran — fix: removed optional/absent keys from Reads; script handles state[key]==None natively. (B) ClaimIdentity permission has scope=self; capability authorizer step 3 requires authContext.target==actor — fix: added AuthContext{Target:actorKey} to all ClaimIdentity envelopes. (C) FR5_ImmediateAccess test created multiple consumers sharing one stream; earlier consumers accumulated create-op backlogs causing duplicate detection — fix: consolidated to single pipeline+consumer in sequence. (D) Unused newClaimPipelineOnly helper removed (golangci-lint unused). All 8 TestClaimIdentity_* tests green. All gates: verify-bootstrap 154 OK, test-bypass 4/4 BLOCKED, test-capability-adversarial 4/4 DEFENDED, full go test ./... -p 1 -count=1 green, golangci-lint 0 issues. Design confirmation: scope=self for ClaimIdentity realized as credentialindex one-credential-one-identity, not actor==target. ClaimKeyInvalid detail stripped before caller egress (NFR-S6). |
| 4.4 | Duplicate Identity Detection (FR3) | Sonnet | Sonnet (claude-sonnet-4-6) | 110K | ~230K | +120K | OVERRUN | 2026-05-17 | Two-session story. Session 1 outer telemetry not captured (killed by Winston when aspect-vs-link model error was detected; agent had run all the way through writing the script branch, integration tests, and verify-bootstrap — substantial burn, estimated ~115K based on tool-call volume and 4.3 precedent). Session 2 outer telemetry 116K (task notification). Combined ~230K. Session 1 implemented Levenshtein builtins, ScanPrefix hydrator extension, state.keys_with_prefix, ScanIdentityDuplicates script branch, capabilityenv pendingReview, identity_scan_test.go — all using the WRONG aspect-based duplicateOf model. Session 2 corrected the data model to link-based per Decision #15 / brief §4 revision. Changed ScanPrefix→ScanPrefixes []string, added lnk.identity.* prefix support in hydrator, rewrote ScanIdentityDuplicates to emit canonical lnk.identity.<lowID>.duplicateOf.identity.<highID> links (not bidirectional aspects), updated all test assertions to use link keys. All 11 deliverables complete. All gates green: verify-bootstrap 154 OK, test-bypass 4/4 BLOCKED, test-capability-adversarial 4/4 DEFENDED, golangci-lint 0 issues, full go test ./... -p 1 -count=1 green. Root cause for first-session model error: brief's §4 was not sufficiently explicit about link vs aspect distinction; continuation brief corrected it. |
| 4.5 | Staff-Approved Identity Merge (FR4) | Opus | Opus (claude-opus-4-7) | 135K | ~215K | +80K | OVERRUN | 2026-05-17 | Outer telemetry from task notification (total_tokens=214,579). Sub-agent self-estimate was 110K (49% under-reporting — in line with established Opus pattern). Single-session implementation. Actual integration test count is 12 (sub-agent reported 11; one extra MergeIdentity test). `TestMergeIdentity_CapKVReprojection_NFR_P3` from brief §5 not shipped — sub-agent's justification: full Refractor stack inside the processor test harness duplicates a path covered by `internal/refractor/refractor_capability_multi_e2e_test.go` (state-mutation reprojection) and 3.2b's link-envelope adjacency-bridge tests; accepted as a soft trade. Total identityDDLScript ~720 LOC (over the brief's 520-LOC halt advisory — accepted as natural growth across 4.1-4.5 cumulative, not bloat). One CI flake `internal/refractor/health/TestReporter_LastUpdated_IsRFC3339` (NATS i/o timeout, Deviation 14 pattern) on first full-suite run; clean on re-run. All 11 deliverables shipped. Phase A — Hydrator extended in step4_hydrate.go: added bare "lnk." global-scan prefix (5000-key soft cap; "lnk.identity." narrow scan retains its 1000 cap); `.credentialBinding` added to `identityScanAspects` (4 → 5 aspects per scanned vertex); 2 focused hydrator unit tests (all-links scan + soft-cap). Phase B — `ApproveIdentityMerge` script branch (~85 LOC) implemented per Decision #1/#2: enumerates flagged-for-review identities, walks lnk.identity. duplicateOf links, emits pairs with primaryCandidate heuristic (claimed > unclaimed, tie-break earliest createdAt); empty MutationBatch + EventList; ResponseDetail carries flaggedCount + pairs[] with full primary/secondary detail (name/email/phone/state/createdAt/hasCredentialBinding). Pair filter via payload.primaryKey+secondaryKey supported; ReviewPairNotFound on miss. Phase C — `MergeIdentity` script branch (~150 LOC) implemented per Decision #3/#4/#6/#7/#9/#10: pre-flight (self-ref, missing, state guard, duplicateOf-link guard) with NFR-S6-compliant errors that DO NOT leak mergedInto; 6-segment link enumeration via `state.keys_with_prefix("lnk.")`; rekey-with-tombstone-then-create for endpoint substitution; primary-wins collision resolution (counted as linkCollisionsMerged); self-loop tombstone-only; duplicateOf canonical link tombstoned no-create; optional aspectConflictResolution for contact aspects (name/email/phone). One `IdentityMerged` event + full ResponseDetail summary. Total identityDDLScript ~720 LOC (under 520-LOC halt threshold per the brief — note the brief's "<500 LOC" target was for new branches only; cumulative script across 4.1-4.5 is naturally larger). Phase D — internal/processor/identity_merge_test.go (NEW): 11 integration tests across capability+stub auth modes per Decision #5 + #13. Capability-mode (5 tests) exercise ApproveIdentityMerge via the seeded operator grant: SurfacesFlaggedPairs, FiltersByPair (real pair accepted, bogus pair → ReviewPairNotFound), HasCredentialBindingFlag, NonOperatorDenied, EmptyWhenNoFlagged. Stub-mode (5 tests) exercise MergeIdentity (no grant seeded): HappyPath (rekey + tombstone + state→merged + mergedInto), RejectsNonFlaggedSecondary, RejectsAlreadyMergedSecondary (NFR-S6 leak check — mergedInto value not exposed in error message), RejectsMissingDuplicateOfLink, SelfReferenceRejected, AspectConflictResolution. Capability-mode post-merge redirect (1 test): PostMergeRedirect_FR4 — UpdateIdentityState against state=merged returns IdentityMerged via 4.1's enforce_not_merged guard. Decision #5 verified pre-implementation: `grep -n PermMergeIdentity internal/bootstrap/*.go` → no hits; neither permission NOR grant exists; stub-mode workaround is necessary AND sufficient for Phase 1. Phase E — All required gates green at session close: `go build`, `make vet`, `golangci-lint run ./...` 0 issues, `go test ./internal/processor/... -count=1`, `go test ./internal/refractor/... -count=1`, `make verify-bootstrap` 154 OK (unchanged — no primordial migration), `make test-bypass` 4/4 BLOCKED, `make test-capability-adversarial` 4/4 DEFENDED, `go test ./... -p 1 -count=1` all packages green. Name reconciliation noted in closing summary. No CONTRACT-AMENDMENT-REQUEST raised. PHASE 1 CARRY: (1) MergeIdentity has NO seeded grant link; a future story (Story 5.x or a small follow-up) must seed `PermMergeIdentity` + `MergeIdentity → operator` grant and rebaseline verify-bootstrap (154 → 156 OK expected). (2) TombstoneIdentity stub stays NotYetImplemented. (3) SplitIdentities (un-merge) is Phase 2+ per AC. (4) No new capabilityenv wrapper field for merged identities (Decision #8) — existing reprojection on state aspect mutation suffices; if a Phase 2 use case needs a `merged: true, mergedInto: <primary>` field on the cap entry, that's a follow-up. (5) data-contracts.md addendum suppressed per Story 4.4 carry. (6) Pipeline/Loom UI surface is response-detail-driven (not in scope). |
| 5.1 | DDL Self-Description Aspects (FR19 substrate) | Opus | — | 115K | — | — | PENDING | — | |
| 5.2 | Cold-Start AI Agent Traversal & Operation Submission | Sonnet | — | 115K | — | — | PENDING | — | FR19 north-star integration test. |
| 5.3 | Compensating Operation & DDL Rollback (Gate 4) | Sonnet (downgraded from Opus) | Sonnet (claude-sonnet-4-x) | 80-100K | ~376K | +276K vs revised | OVERRUN | 2026-05-23 | Outer telemetry: planning 119K (brief author 76K + brief-fix sub-agent 28K + Winston orchestration ~15K) + implementation 151K + code-review 115K + fix-pass 81K ≈ 376K total. Sub-agent self-reports continued to under-report by 40-60% (impl reported 50-65K vs actual 151K). Option A design (DDL self-describes inverse via `.compensation` aspect) replaced rejected `compensatingOperation` envelope-field design from epics.md — preserved Processor write-path purity (no new OperationReply fields, no new Processor read surface). Tier downgraded Opus→Sonnet after scope simplification. Code review found 2 MUST FIX + 3 SHOULD CONSIDER + 4 NITS, all addressed: MF-1 closed parseMutations gap in starlark_runner.go (parser-completeness only — MutationOp.ExpectedRevision field already existed and was referenced by step8_commit.go; not a new Processor surface); MF-2 added .compensation update to TombstoneMetaVertex branch with inverseOperationType=none signal; SC-1 dropped same-revision-on-two-keys bug exposed by MF-1 fix; SC-2 added consumer NumPending assertion (AC6 step 9); SC-3 added UpdateMetaVertex round-trip sub-test (exposed pre-existing Starlark dict-syntax bug in prior_desc capture — `d.data["text"]` not `d.data.text`, fixed). N-3 (int/uint64 cast stylistic) explicitly skipped per Winston judgment. All gates green: make test-rollback (3 sub-tests: DDL/Lens/UpdateMetaVertex), make verify-kernel (69 assertions OK), make test-bypass DEFENDED, make test-capability-adversarial BLOCKED, golangci-lint 0 issues. All 5 architectural guardrails preserved: OperationReply unchanged, no new ContextHint fields, zero compensation/Compensation refs in internal/processor/, no new KV buckets, no business data in .compensation aspect data. Closes Phase 1 Gate 4. Epic 5 complete (5.1+5.2+5.3 shipped). |
| 6.0 | Component Reference Pages (Processor, Refractor, Substrate) | Sonnet | Sonnet (claude-sonnet-4-6) | 30K | ~97K | +67K | OVERRUN | 2026-05-19 | Outer telemetry from task notification (total_tokens=97,191). Sub-agent self-estimate was 32K (67% under-reporting — wider than the typical Sonnet 20-30% gap; this story involved heavy code-reading to verify factual claims, which inflated reads beyond budget assumption). Docs-only session. 4 files created: docs/components/_index.md (38 lines), processor.md (195 lines), refractor.md (213 lines), substrate.md (190 lines). All 5 deliverables met. Factual spot-checks passed for all four pages: Alphabet=58 chars, DefaultScriptWallBudget=250ms, antlr4-go confirmed in full/full.go, materializer.control subject confirmed in subjects/subjects.go, adjacencyKVBucket="refractor-adjacency" confirmed in cmd/refractor/main.go. Phase 1 carry documented: materializer token (Story 2.4a), pendingReview field eviction (Story 4.6), levenshtein UDFs as future-extension (Story 4.6), AdjacencyForNode as planned substrate addition (post-Story 4.7). No drift detected at review: deprecation notes correctly tied to specific stories; no `grantsPermission` references; vtx.meta.<NanoID> shape used correctly; no planning-artifact edits. |
| 4.6 | Capability Package Format + Installer + Identity-Hygiene Package | Opus | Opus (claude-opus-4-7) | 180K | ~483K | +303K | OVERRUN | 2026-05-20 | Outer telemetry combined across three sub-agent rounds: R1=255K (built Adjacency-KV read coupling — violated `lattice-architecture.md:94`), R2=154K (reverted R1; rebuilt with separate `identityMergePlan` lens read by Processor — also violated the boundary because Capability KV is the *one* contract-defined Processor↔lens-output read, not generalizable), R3=73K (deleted merge-plan lens; folded edge enumeration into `duplicateCandidates` via `collect(DISTINCT inL.key) + collect(DISTINCT outL.key)`; rewrote MergeIdentity script to take `edges` as a command parameter and validate each against Core KV with `EdgeNotFound`/`EdgeNotALink`/`EdgeDoesNotTouchSecondary`/`MergeBatchTooLarge`). +Winston comment cleanup (~5K, untracked). 2.7× budget overrun is honest cost of two architectural redirects. The boundary lesson: graph topology flows Lens→Client→Command parameter→Processor-validates-against-Core-KV. Capability KV is NOT a generalizable pattern. All 15 deliverables shipped. Files: docs/components/_packages.md (new), cmd/lattice-pkg/ (new), internal/pkgmgr/ (new, 5 installer integration tests), packages/identity-hygiene/ (manifest.yaml + package.go + ddls.go + lenses.go + permissions.go + README.md + package_test.go with forbidden-token regression guard), Levenshtein UDFs in internal/refractor/ruleengine/full/executor.go + levenshtein_test.go, Processor surface reduction (ScanPrefixes/keys_with_prefix/strings.* deleted; OperationReply.Detail commit-trace-only comment), pendingReview reverted in capabilityenv, Identity DDL trim (1131→448 lines; permittedCommands reduced 8→3; state machine simplified to unclaimed→claimed→merged with flagged-for-review deleted; IdentityPermissions 5→2, IdentityGrants 10→4). Two installer bug fixes en route (manifest aspect added to tombstone set; per-key OCC swapped for unconditional puts within atomic batch). Gates run by sub-agents: go build, go vet, golangci-lint (0 issues), go test ./... -p 1 -count=1 ALL GREEN. Docker-dependent gates (make verify-bootstrap, make test-bypass, make test-capability-adversarial) NOT run by sub-agents (no daemon in their env); Winston relies on CI to execute these. PHASE 1 CARRY: (1) integration tests on Surface B (8 hygiene end-to-end tests requiring full Refractor+Processor+hygiene-package harness) deferred — no full e2e fixture exists for installed-package operations; small follow-up needed before Story 4.7 ships. (2) verify-bootstrap count drops by ~18 because IdentityPermissions/IdentityGrants iterate dynamically; CI will reveal the new expected count. (3) `lattice candidates list` CLI verb is Story 6.1's job (CLI reads duplicate-candidates lens, constructs MergeIdentity{edges}). (4) Story 4.5's unseeded-MergeIdentity-grant carry is closed by this package (identity-hygiene declares perm + grant pair). No CARs raised. |
| 2.4a | Refractor Token Eviction (mechanical) | Sonnet | Sonnet (claude-sonnet-4-6) | 90K | ~133K | +43K | OVERRUN | 2026-05-22 | Outer telemetry from task notification (total_tokens=133,138). 1.5× budget — within reasonableness for a mechanical rename touching 26 files. Pure rename + delete + comment-rewrite with no behavior change. Subjects: `materializer.{rules,health}.<lensId>` deleted (Health KV already moved per-instance in 3.2b; Rules subject obsoleted by Core-KV-watched lens defs in 2.1); `materializer.{audit,metrics,dlq}.<lensId>` → `lattice.refractor.{audit,metrics,dlq}.<lensId>`. Streams: `MATERIALIZER_DLQ_*` → `REFRACTOR_DLQ_*`. Durables: `materializer-adjacency` → `refractor-adjacency`; `materializer-<ruleID>` → `refractor-<ruleID>`; `materializer-control` → `refractor-control` (queue group). KV: `materializer-health` bucket default + `HealthKVBucket` field deleted from `config/config.go`. `team` field (Deviation 4) evicted — validation removed, DLQ signature trimmed `(team, lensID) → (lensID)`, `TestParse_MissingTeam` rewritten as `TestParse_NoTeam_Accepted`. Comments rewritten across ~8 files. `lens/loader.go` + `loader_test.go` deleted entirely (-841 LOC; dead since Core KV became the lens-def source in 2.1). **Intentionally NOT touched**: `materializer.control` subject literal — 2.4b's territory (migration to NATS Services). Net: -1,006 LOC. Gates: `go build`, `make vet`, `golangci-lint` (0 issues), `go test ./internal/refractor/... -p 1 -count=1` all 16 packages green. Docker-dependent gates (`make verify-kernel`, `verify-package-*`, `test-bypass`, `test-capability-adversarial`) flagged for Winston/CI. Architectural-purity grep clean. No CARs. |
| 2.4b | Refractor Lattice-Native Source Plane (durable consumer + NATS Services) | Opus | Opus (claude-opus-4-7) | 100K | ~146K | +46K | OVERRUN | 2026-05-22 | Outer telemetry from task notification (total_tokens=146,254). 1.5× budget. Two design-bearing migrations. **(1) Lens-definition source**: new `substrate.SubscribeKVChanges` helper creates a durable JetStream consumer on the `KV_core-kv` backing stream filtered to `$KV.core-kv.vtx.meta.>`. Replaces the ephemeral `kv.Watch(ctx, "vtx.meta.>", IncludeHistory())` in `lens/corekv_source.go`. Cross-restart sequence position preserved (`TestSubscribeKVChanges_DurableResume` proves it). **Sub-agent self-corrected a brief inconsistency**: the brief said `js.DeleteConsumer` on ctx.Done; that contradicts the "durable resume" promise (deletion wipes the ack floor). Helper now stops the iterator + closes the channel on cancel but leaves the durable in the catalog. Correct call — no CAR needed (brief-imprecision per WINSTON-RESUME rule 11). **(2) Control plane**: `nc.QueueSubscribe(subjects.Control(), "refractor-control", ...)` migrated to `micro.AddService` with endpoints at `lattice.ctrl.refractor.<lensId>.<op>` for the 6 actual ops (health, validate, rebuild, pause, resume, delete). Sub-agent verified op list against the actual `handleControlMsg` rather than the brief's drifted example list (`activate, pause, resume, rebuild, delete, status` — wrong; correct list per code: `health, validate, rebuild, pause, resume, delete`) — another brief-imprecision correction. `subjects.Control()` + `TestControl` deleted. **Behavioral shift to flag to clients**: unknown ops no longer return `{"error":"unknown operation: ..."}` — they time out with `nats: no responders available` because NATS Services routes by subject. Documented in the closing summary's migration mapping. **Files**: `internal/substrate/{subscribe.go,subscribe_test.go}` new (247+231 LOC); `internal/refractor/{lens/corekv_source.go,control/service.go,control/service_test.go,subjects/subjects.go,subjects/subjects_test.go}` modified. **Gates**: `go build`, `make vet`, `golangci-lint` (0 issues), `go test ./... -p 1 -count=1` all green including new `TestNATSServicesIntrospection` + `TestSubscribeKVChanges_DurableResume`. Docker-dependent gates (`make verify-kernel`, `verify-package-*`, `test-bypass`, `test-capability-adversarial`) flagged for CI. Architectural-purity grep clean; `materializer` grep clean (only historical Story-2.4a rename comment remains). No CARs raised. **The 5-story 2026-05-19 course correction sequence (6.0, 4.6, 4.7, 2.4a, 2.4b) is complete.** Epic 5 unblocked. |
| 4.7-cleanup | Test relocation + legacy bootstrap accessor deletion | Opus | Opus (claude-opus-4-7) | 130K | ~365K | +235K | OVERRUN | 2026-05-22 | Two-round story. Round 1 (215K) was reverted — sub-agent rewired identity tests in place + deleted 826 LOC of rbac tests with "the alternative is too costly" rationale, both violating the brief. Round 2 (~150K estimate; agent interrupted on token cap so outer telemetry is incomplete — duration 17min + 104 tool uses suggests substantial work) used a stronger file-backed brief with explicit non-negotiable rules + named the prior failure modes. Round 2 completed deliverables §1 (identity tests moved to packages/identity-domain/), §2 (rbac tests moved to packages/rbac-domain/ including reports_to_test.go for the orphan case), §3 (legacy bootstrap accessor surface deleted: identity_ddl.go + role_mgmt_ddl.go + nanoid.go trimmed by 297 LOC). Winston-added: deleted `internal/bootstrap/roles.go` (CanonicalRoles + PlatformInternalPermission dead since 4.7's PreInstall hook took over). Three narrow processor exports added (`SeedFromRequestID`, `DeterministicNanoID`, `NewStubEventPublisher`) — narrow surgical surface needed for external test packages, documented inline. Architectural guards all clean: zero new ContextHint fields, zero scans, zero lens-output reads from Processor, all four closing-grep checks pass. Net code change: -4291 LOC source + tests deleted (legacy bootstrap + old test locations); +1989 LOC packages (relocated tests + helpers) + 336 LOC new testutil/{pipeline,embedded_nats}.go. Gates: build, vet, lint (0 issues), `go test ./... -p 1 -count=1` all packages green. Docker-dependent gates pending CI. Deliverable §4 (strengthen `verify-package-*` gates with real Core-KV-state assertions) NOT closed — agent ran out of budget. Remains as the only open 4.7 carry; ~30K Sonnet follow-up. No CARs raised. |
| 4.7 | Bootstrap Kernel Minimization + rbac-domain + identity-domain Packages | Opus | Opus (claude-opus-4-7) | 150K | ~241K | +91K | OVERRUN | 2026-05-20 | Outer telemetry from task notification (total_tokens=241,543). Single-session implementation. 1.6× budget overrun within tolerance for a multi-package + kernel-rewrite story. **Architectural-purity goal achieved**: grep checks for `AdjacencyReads\|LinkScans\|ScanPrefixes\|WithAdjacencyBucket\|AdjacencyForNode\|keys_with_prefix` over internal/+cmd/+packages/ returned zero operational hits (1 historical comment + 2 forbidden-token regression guards in package tests). `ContextHint{` in packages/ → zero hits. `state.read\|state[` in packages/{rbac,identity}-domain/ddls.go → 13 hits, every one targets a known key composed from command parameters. Processor's read surface unchanged. Deliverables: packages/rbac-domain/ (rbac DDL handles all 10 ops in single ~310-LOC script: CreateRole/UpdateRole/TombstoneRole/CreatePermission/UpdatePermission/TombstonePermission/AssignRole/RevokeRole/GrantPermission/RevokePermission; deterministic link-key computation for idempotency), packages/identity-domain/ (verbatim Story-4.6 trimmed identity DDL + seed.go PreInstall hook seeding consumer/frontOfHouse/backOfHouse roles via substrate-direct writes), internal/pkgmgr extended with PreInstallFn hook + Installer.RoleIDs map + resolveGrants() flowing seed-time-minted role NanoIDs back into the atomic batch's grant resolution. Kernel rewrite: internal/bootstrap/meta_ddl.go (~170 LOC meta-meta-DDL dispatching on op.payload.targetClass for CreateMetaVertex/UpdateMetaVertex/TombstoneMetaVertex), internal/bootstrap/primordial.go shrunk 700→250 LOC seeding only meta-DDL + 2 lenses + operator role + 3 meta-permissions + 3 grantedBy links + 1 admin identity + 1 holdsRole link, scripts/verify-kernel.go (replaces verify-bootstrap.go), Makefile renamed verify-bootstrap→verify-kernel + added verify-package-{rbac,identity,identity-hygiene}, CI workflow updated. `grantsPermission` → `grantedBy` link DDL rename completed in internal/bootstrap/lenses.go (Capability + capabilityRoleIndex cypher) + internal/pkgmgr/build.go + 4 refractor tests. internal/testutil/install_phase1_packages.go test helper (idempotent rbac → identity-domain → identity-hygiene install). Gates run by sub-agent: go build, make vet, golangci-lint (0 issues), go test ./... -p 1 -count=1 ALL GREEN. Docker-dependent gates (make verify-kernel, make verify-package-*, make test-bypass, make test-capability-adversarial) NOT run by sub-agent; CI executes these. **Deviations from brief** (3 flagged by sub-agent, all carried as Phase 1 cleanup): (1) Processor test cutover to InstallPhase1Packages deferred — tests still use bootstrap.IdentityDDL() / bootstrap.RoleMgmtDDLs() as DDL-source-of-truth accessors; mechanical migration ready but not performed. (2) Identity DDL + RoleMgmt scripts duplicated between internal/bootstrap/ (legacy accessors retained for test compat) and packages/{identity,rbac}-domain/ (canonical) — byte-equivalent at story end; needs a divergence-guard test. (3) verify-package-* gates use `lattice-pkg list` rather than per-package KV assertions — sufficient to detect total-failure-to-install, weak against partial-install corruption. **Winston observation** (4th carry): legacy bootstrap accessor surface (IdentityPermissions/IdentityGrants/RoleMgmtPermissions/DDLGrantsPermissionID/IdentityPermissionKeys/IdentityGrantLinkKeys) is dead code — no caller outside internal/bootstrap/; primordial.go was rewritten and no longer references them. The `grantsPermission` string literals embedded in them are stale (renamed to grantedBy). Cleanup pass needed. No CARs raised. |
| 6.1 | Lattice CLI Tool (FR45) | Sonnet | Sonnet (claude-sonnet-4-x) | 120K | ~610K | +490K | OVERRUN | 2026-05-24 | Outer telemetry: planning 75K (brief author) + implementation ~140K (interrupted mid-debug) + debug-pass-1 119K + code-review 125K + fix-pass 148K + Winston orchestration ~3K ≈ 610K total. Multi-pass debug cycle exposed real latent issues. Cobra-based 9-group CLI binary at cmd/lattice/ (op/graph/lens/query/health/identity/candidates/auth-trace/bootstrap), shared creds package, internal/bootstrap/verify.go for kernel verification. Code review: 2 MUST FIX (PrintJSONError returned nil — JSON-mode errors exited 0; SubmitOp used tracker-polling that hung 10s on rejected ops), 6 SHOULD CONSIDER, 4 NITS — all addressed except N-3 (perf-only on debug command). MF-2 required minimal Processor change: replySubject() helper in commit_path.go that checks Lattice-Reply-Inbox header before falling back to msg.Reply() — JetStream pull consumers rewrite msg.Reply() to ACK subject, so JetStream-publishing clients need header-based routing. NOT a guardrail violation: header is submitter-agnostic (any JS-publish submitter can use it, not CLI-specific), no new read surface, no new OperationReply fields, no new ContextHint fields. Latent test-infra bug caught and fixed: testutil/embedded_nats.go missing StoreDir=t.TempDir() caused cross-test JetStream contamination across whole suite. All gates green: 13 cmd/lattice packages pass, internal/processor + internal/aiagent regression clean, golangci-lint 0 issues, binary --help exits 0, MF-1 verified (rejected status returns non-zero exit), MF-2 verified (rejection reply received <1s with reason). Docker-dependent gates (test-bypass, test-capability-adversarial) require make up — relied on CI. All 5 architectural guardrails preserved. End-to-end "Hello Lattice via CLI only" e2e deferred to Story 6.4 (Phase 1 Gate 5). |
| 6.2 | Health KV Schema & Completeness (FR46, FR52) | Sonnet | — | 90K | — | — | PENDING | — | |
| 6.3 | Deployment Isolation Specification (FR48) | Sonnet | — | 70K | — | — | PENDING | — | |
| 6.4 | "Hello Lattice" Reference Implementation (Gate 5) | Sonnet | — | 130K | — | — | PENDING | — | Phase 1 Gate 5 closure. External tester required. |

## Rolling Totals

| Tier | Budget (K tokens) | Actual (K tokens) | Δ | Stories complete |
|---|---|---|---|---|
| Opus | 2,450 | 3,846 | +1,396 | 19 shipped (planned 15; splits added 2 + 4.7-cleanup + 2.4b) |
| Sonnet | 1,717 | 1,770 | +53 | 15 shipped (planned 19) |
| Haiku | 0 | 0 | 0 | 0 / 0 |
| **Phase 1 Total** | **4,167** | **5,616** | **+1,449** | **34 shipped / 32+ planned** — Course-correction sequence (6.0 + 4.6 + 4.7 + 4.7-cleanup + 2.4a + 2.4b) COMPLETE. Epic 5 unblocked. |

> **Source-of-truth rule:** these rolling totals are RE-COMPUTED from the per-row Actual column at every Epic close, NOT spot-incremented. Use `awk -F'|' '/^\| [0-9]/ {gsub(/[ ~K]/,"",$7); if ($7 ~ /^[0-9]+$/) sum += $7} END {print sum}' token-usage-tracker.md` to verify. The phase-1-progress.md totals must agree with this block; if they drift, this block wins.
>
> **Course correction note (2026-05-19):** five new stories added to the upcoming sequence — 6.0 (component docs), 4.6 (capability package format + identity-hygiene), 4.7 (kernel minimization + rbac/identity packages), 2.4a (Refractor token eviction), 2.4b (Refractor Lattice-native source). Their budgets enter the Phase 1 Total once their handoff briefs are authored.

## Update Procedure (For Each Implementation Session Close)

1. Read the session's total token usage (from session metadata or transcript count).
2. Fill in `Model (actual)`, `Actual`, and `Session Date` for the row.
3. Compute Δ = Actual − Budget. If Actual > 1.2 × Budget, set Status to `OVERRUN` and add root-cause note.
4. If Status would be `OVERRUN`, raise to Winston for review before closing the session.
5. Update Rolling Totals.
6. Commit this file as part of the story's closing commit (alongside the implementation artifacts).
