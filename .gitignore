/.claude
/_bmad
.DS_Store
/bin
CLAUDE.md

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/vendor/
go.work
go.work.sum
coverage.txt
coverage.html
# Compiled binaries left at repo root by `go build ./internal/spike/...`
/nats-batch
/starlark
/lattice
/lattice-refractor
/lattice-processor
# Stray top-level refractor binary (`go build ./cmd/refractor` without -o).
# Canonical build target is bin/refractor (see Makefile).
/refractor
refractor.log

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Local dev
.env
.env.local
*.local

# Story 1.3 — bootstrap runtime artifacts (gitignored; generated by make up)
lattice.bootstrap.json
/.agents
