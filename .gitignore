/.agents
/.claude
/_bmad
.DS_Store
/bin

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.log
/vendor/
go.work
go.work.sum
coverage.txt
coverage.html
# Compiled binaries left at repo root by `go build ./internal/spike/...`
/nats-batch
/starlark
/lattice
/lattice-refractor
/lattice-processor
# Stray top-level binaries from `go build ./cmd/<name>` without -o.
# Canonical build target is bin/<name> (see Makefile).
/bridge
/bootstrap
/clinic-app
/facet
facet.db
facet.db-*
/facet-store/
/loupe
/lattice-pkg
/loftspace-app
/processor
/refractor
/verify-package-rbac
/verify-package-identity
/verify-package-identity-hygiene

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Local dev
.env
.env.local
*.local

# bootstrap runtime artifacts 
lattice.bootstrap.json
loupe-operator.json

# Vault master KEK (Contract #3 §3.10) — generated locally by
# `make provision-vault-kek`. NEVER commit: it decrypts every sensitive
# aspect ever written.
/deploy/vault/

# Local state of the wrangler CLI (Cloudflare deploys run from ad-hoc shells);
# never repo content.
.wrangler/

# Hosted-demo box state written by deploy/demo scripts (per-world, per-box).
demo-loupe-host
loupe-demo-operator.json
loupe-demo-operator.pending.json
provision-demo-operator.lock
loupe-demo.pid
