/.claude
/_bmad
.DS_Store
/bin

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/vendor/
go.work
go.work.sum
coverage.txt
coverage.html
# Compiled binaries left at repo root by `go build ./internal/spike/...`
/nats-batch
/starlark
/lattice
/lattice-refractor
/lattice-processor
# Stray top-level binaries from `go build ./cmd/<name>` without -o.
# Canonical build target is bin/<name> (see Makefile).
/bridge
/refractor
/processor
/verify-package-rbac
/verify-package-identity
/verify-package-identity-hygiene
/bootstrap
*.log

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Local dev
.env
.env.local
*.local

# Story 1.3 — bootstrap runtime artifacts (gitignored; generated by make up)
lattice.bootstrap.json
loupe-operator.json
facet.db
facet.db-*
/facet-store/
/.agents
processor.log
/loupe
/lattice-pkg
/loftspace-app
/clinic-app

# Vault master KEK (Contract #3 §3.10) — generated locally by
# `make provision-vault-kek`. NEVER commit: it decrypts every sensitive
# aspect ever written.
/deploy/vault/
