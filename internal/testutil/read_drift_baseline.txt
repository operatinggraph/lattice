# MEASURED RESIDUE, NOT A PERMISSION LIST.
#
# Every row is a shape some script read or walked live in a test run while the
# submitting envelope declared nothing that covers it. The file exists so the
# drift guard (read_drift_guard.go) can be armed on every CapabilityPipeline
# TODAY: no NEW undeclared read or walk can land, whatever these rows say.
#
# A row is a shape the guard TOLERATES, never one it endorses. Reads already
# sanctioned as Contract #2 §2.5 class-(e) follow-ups on an enumeration — the
# key's vertex root was DISCOVERED by this execution's own kv.Links walk, hubs
# excluded — are admitted by the guard itself when the key is vertex-rooted
# (vtx.<type>.<id>…); a link-shaped follow-up read (lnk.…) has no vertex root
# for the guard to match, so it is listed here under category 3 below.
#
# WHAT THE READ ROWS ACTUALLY ARE — three different things, and only the first
# two are removable. Do not read the file as one undifferentiated debt pile; the
# classification was measured against the declaring sources, not assumed.
#
#  1. The envelope the TEST builds under-declares what the real dispatcher
#     already sends. The script is fine and production declares correctly; the
#     fixture omits it, which means the test drives the lazy live-read path
#     production never takes. Fix the fixture, drop the row.
#  2. A genuine declaration gap, payload-direct and therefore declarable —
#     the worksAt confinement reads reachable from the submitted payload. Fix
#     the descriptor, drop the row.
#  3. Sanctioned and undeclarable BY DESIGN — class-(c) config reads
#     (deliberately outside OCC) and reads chained off a value only a prior read
#     resolves. Nothing should ever declare these; the rows record where the
#     guard must stay quiet.
#
# WHAT THE WALK ROWS ACTUALLY ARE. A walk is declarable through
# ContextHint.Enumerations, and five channels populate it: Loom StepSpec, Weaver
# GapActionSpec, pkgmgr.OpDispatchSpec (an ordinary descriptor-dispatched op's
# own Enumerations field, internal/pkgmgr/definition.go), a hand-built envelope
# (cmd/lattice/candidates/candidates.go does this for MergeIdentity), and a
# client passing enumerations through the gateway or Loupe
# (internal/gateway/gateway.go, cmd/loupe/op.go). The residue that remains is
# the LINK-DISCOVERED hub: a walk whose hub vertex only exists after a prior
# read resolves it — e.g. `vtx.building.<id> containedIn out`, reached from a
# unit's own containedIn target — which no surface above can declare, because
# every one of them resolves its hub at dispatch time, before the walk that
# would produce this hub has run. Those rows are a structural gap, not script
# debt.
#
# The two are not the same fix and the split is measurable: a walk row whose op
# is ALSO seen declaring that exact shape in some other execution is a fixture
# under-declaration (class 1 applied to walks), because a declaring channel
# demonstrably exists for it. The rest have no observed declaring channel. The
# generator prints both counts each time it runs — read them off that output
# rather than trusting a number frozen into this comment.
#
# Rows are REMOVED when a dispatcher declares the key or walk. A row may be ADDED
# BY HAND, under a comment line stating why, when a read or walk is sanctioned and
# undeclarable — that is the path the guard failure itself points at.
# REGENERATING is not that path: it re-records everything the run observed,
# including drift that has just landed, so its DIFF must be reviewed row by row
# and it must never be run to make a failing test pass.
#
# Format: <kind> TAB <operationType> TAB <normalized shape>, sorted. Fields are
# whitespace-trimmed and an indented # comment is prose, so hand annotation is
# safe.
# Regenerate with the census + command in scripts/gen-read-drift-baseline.go.

# read rows: per operationType, the live Core KV reads no declaration covers.
#
read	CancelBooking	lnk.identity.<id>.worksAt.building.<id>
read	CancelRenewal	lnk.identity.<id>.manages.unit.<id>
# Charge's own staff-standing confinement (leaseapp_unit -> require_workplace)
# resolves the tab's own lease into a unit only the walk itself discovers, so
# the worksAt key it checks at each containedIn ancestor cannot be named by any
# dispatcher up front -- the same two undeclarable follow-ups OpenTab and
# VoidCharge already carry for the same tab-confinement guard.
read	Charge	lnk.identity.<id>.worksAt.building.<id>
read	Charge	lnk.identity.<id>.worksAt.unit.<id>
read	Charge	vtx.leaseapp.<id>
read	ClinicCreditAccount	lnk.patient.<id>.identifiedBy.identity.<id>
# ClinicDebitAccount's visitRef patient check: the forPatient link it reads is
# keyed by the patient id its own heldFor walk resolves off the account (the
# walk ClinicCreditAccount's ownership proof already carries), so it is a
# class-(e) follow-up no dispatcher can name up front — link-shaped, hence
# listed rather than admitted by the guard's vertex-root rule.
read	ClinicDebitAccount	lnk.appointment.<id>.forPatient.patient.<id>
# CorrectAppointmentStatus reuses SetAppointmentStatus's staff-standing
# confinement verbatim, so it inherits the same two undeclarable follow-ups
# (the `SetAppointmentStatus` rows below): each hangs off a vertex only a prior
# walk resolves — the building from the appointment's site walk, the provider
# from its withProvider walk — so no dispatcher can name either key up front.
read	CorrectAppointmentStatus	lnk.identity.<id>.worksAt.building.<id>
read	CorrectAppointmentStatus	lnk.provider.<id>.identifiedBy.identity.<id>
read	CreateAppointment	lnk.identity.<id>.worksAt.building.<id>
read	CreateAppointment	lnk.patient.<id>.identifiedBy.identity.<id>
read	CreateAppointment	vtx.provider.<id>.hours
read	CreateAppointment	vtx.provider.<id>.timeOff
read	CreateBooking	lnk.identity.<id>.worksAt.building.<id>
read	CreateLeaseDocInstance	vtx.leaseapp.<id>.signature
read	CreateLeaseDocInstance	vtx.leaseapp.<id>.terms
read	CreateMenuItem	lnk.identity.<id>.worksAt.building.<id>
read	CreateSession	lnk.identity.<id>.worksAt.building.<id>
# CreateSessionSeries runs the SAME staff-standing confinement CreateSession
# does (ddls.go: "checked ONCE — the whole series shares one studio"), same
# fixture under-declaration the row above records — unexercised by any
# non-operator test until now.
read	CreateSessionSeries	lnk.identity.<id>.worksAt.building.<id>
read	CreateStudio	lnk.identity.<id>.worksAt.building.<id>
read	CreateStudio	lnk.identity.<id>.worksAt.unit.<id>
read	CreateUnclaimedIdentity	vtx.identity.<id>.erasureRequested
read	CreateUnclaimedIdentity	vtx.identity.<id>.piiKey
# loftspace-ledger's CreditAccount / LoftspaceRecordCharge self-scope proofs
# (category 3): the account's OWN heldFor walk resolves the lease, whose
# applicationFor (resident) and appliesToUnit -> manages (landlord) links are
# then read off it -- link-shaped follow-ups chained off a value only the walk
# resolves, so no dispatcher can name them up front. The unit vertex read
# between them is vertex-rooted and admitted by the guard itself.
read	CreditAccount	lnk.identity.<id>.manages.unit.<id>
read	CreditAccount	lnk.leaseapp.<id>.applicationFor.identity.<id>
read	CreditCafeAccount	lnk.identity.<id>.worksAt.building.<id>
read	CreditCafeAccount	lnk.identity.<id>.worksAt.floor.<id>
read	CreditCafeAccount	lnk.identity.<id>.worksAt.unit.<id>
read	CreditCafeAccount	lnk.leaseapp.<id>.applicationFor.identity.<id>
# DecideLeaseApplication / EndTenancy / RecordApplicationLoss: the terminal
# states' guard release (free_applied_to_unit_guard) — the guard key is built
# from the applicant a walk resolves and the unit another resolves; link-shaped,
# category 3 (the ReassignLeaseUnit row's shape).
read	DecideLeaseApplication	lnk.identity.<id>.appliedToUnit.unit.<id>
read	DecideLeaseApplication	lnk.identity.<id>.manages.unit.<id>
read	DecideLeaseApplication	lnk.identity.<id>.worksAt.building.<id>
read	DecideLeaseApplication	lnk.identity.<id>.worksAt.unit.<id>
read	EndTenancy	lnk.identity.<id>.appliedToUnit.unit.<id>
read	EndVisitSeries	lnk.identity.<id>.worksAt.building.<id>
# GiveNotice's landlord probe (require_manages, packages/lease-signing/
# scripts.go): the manages link is keyed by the unit the application's own
# appliesToUnit walk resolves -- the same link-shaped follow-up
# DecideLeaseApplication's row above carries. Reached only on the
# platform-validated self path when the tenant link is absent.
read	GiveNotice	lnk.identity.<id>.manages.unit.<id>
# JoinWaitlist runs CreateBooking's staff-standing confinement verbatim
# (prepare_booking_common -> require_workplace, packages/wellness-domain/
# ddls.go), so it inherits the same undeclarable worksAt follow-up: the
# building is discovered by the session's own atStudio -> locatedAt walk, so
# no dispatcher can name the key up front; only a non-operator JoinWaitlist
# vector (the credit-hold staff leg) reaches it.
read	JoinWaitlist	lnk.identity.<id>.worksAt.building.<id>
# LoftspaceCreateAccount's landlord probe (require_manages): the manages link
# is keyed by the unit the lease's own appliesToUnit walk resolves -- the
# same link-shaped follow-up DecideLeaseApplication's row above carries.
read	LoftspaceCreateAccount	lnk.identity.<id>.manages.unit.<id>
read	LoftspaceCreateAccount	lnk.identity.<id>.worksAt.building.<id>
read	LoftspaceCreateAccount	lnk.identity.<id>.worksAt.unit.<id>
# LoftspaceRecordCharge's self-scope proofs: the same two link-shaped
# follow-ups CreditAccount's rows above carry, off the same heldFor walk.
read	LoftspaceRecordCharge	lnk.identity.<id>.manages.unit.<id>
read	LoftspaceRecordCharge	lnk.leaseapp.<id>.applicationFor.identity.<id>
read	MergeIdentity	lnk.identityindex.<id>.indexes.identity.<id>
read	MergeIdentity	vtx.credentialindex.<id>
read	OpenTab	lnk.identity.<id>.worksAt.building.<id>
read	OpenTab	lnk.identity.<id>.worksAt.unit.<id>
read	PauseVisitSeries	lnk.identity.<id>.worksAt.building.<id>
# PayOutBalance's self-scope proof (self_scope_standing, loftspace-ledger):
# the same two link-shaped follow-ups CreditAccount's rows below carry, off
# the same account-owned heldFor walk RecordDepositDeduction's proof below
# also shares.
read	PayOutBalance	lnk.identity.<id>.manages.unit.<id>
read	PayOutBalance	lnk.leaseapp.<id>.applicationFor.identity.<id>
# PayoutCafeCredit runs CreditCafeAccount's workplace confinement verbatim (the
# same post_entry require_workplace site RefundCafeCharge shares), so it
# inherits the same two undeclarable follow-ups: each worksAt key is built from
# a location the walk itself discovered off the account's own heldFor /
# appliesToUnit / containedIn chain, so no dispatcher can name it up front.
read	PayoutCafeCredit	lnk.identity.<id>.worksAt.building.<id>
read	PayoutCafeCredit	lnk.identity.<id>.worksAt.unit.<id>
# PayoutCafeCredit's self-clearing refusal (post_entry's require_not_own_account)
# reads the applicationFor link off the lease the account's own heldFor walk
# resolved — the same link-shaped class-(e) follow-up CreditCafeAccount's row
# above records, the key data-derived and unknowable to any dispatcher.
read	PayoutCafeCredit	lnk.leaseapp.<id>.applicationFor.identity.<id>
# internal/refractor's own E2E fixture (real-op multiactor race) under-declares
# the same shape identity-domain's fixtures already fix; internal/refractor is
# off-limits for this sweep.
# PromoteWaitlistedBookings walks the session seat dimension to find the cells
# a waitlist can be seated into (claim_free_seats, wellness-domain/ddls.go). A
# seat index is derived from the session's own .schedule.capacity, not from any
# column of the lens row that dispatches the gap, so the weaver target
# (wellness-domain/targets.go) has nothing to template a declaration off — the
# only declarable form would enumerate all MAX_SESSION_CAPACITY=200 keys on
# every dispatch, which is one hydration KVGet per listed key whatever the
# class's real capacity. The read is bounded by that same ceiling and stops as
# soon as it has one free cell per waiting candidate. It cannot be dropped in
# favour of a blind create either: a cell a cancellation left TOMBSTONED needs
# its revision to OCC-revive, which only the read supplies. One row per index
# the suite actually exercises.
read	PromoteWaitlistedBookings	vtx.session.<id>.seat1
read	PromoteWaitlistedBookings	vtx.session.<id>.seat2
read	PromoteWaitlistedBookings	vtx.session.<id>.seat3
# ReassignLeaseUnit: guard key built from the applicant a walk resolves; link-shaped, category 3.
read	ReassignLeaseUnit	lnk.identity.<id>.appliedToUnit.unit.<id>
# ReassignSessionSeries makes TombstoneSessionSeries's per-occurrence confirmation
# (its row below), off the same walk, for the same reason (an occurrence moved
# elsewhere holds cells on a hub this call never confirmed) — the occurrence id
# is walk-resolved, undeclarable.
# ReassignSession and ReassignSessionSeries carry every live booker's slot cells with
# a moved class (bookerSlotClaim, wellness-domain/ddls.go): the booker hub is the
# .status.booker value of a booking the forSession walk discovered — two hops off
# the enumeration, so not the walk's own far endpoint the guard admits — and a
# dispatcher that knows the roster (cmd/wellness-app) declares the cells as
# optionalReads; one that does not (the CLI, this harness) reads them live.
read	ReassignSession	vtx.identity.<id>.slot<t>
read	ReassignSessionSeries	lnk.session.<id>.atStudio.studio.<id>
read	ReassignSessionSeries	vtx.identity.<id>.slot<t>
# ReassignSessionSeries' front-of-house binder is CreateSessionSeries' workplace
# walk verbatim (worksAt_covers off the caller-supplied studio) — the same
# category-2 confinement read the other staff-widened wellness ops carry.
read	ReassignSessionSeries	lnk.identity.<id>.worksAt.building.<id>
# ReassignSessionSeries claims, on the studio the caller confirmed, every cell
# the moved run does not already hold (claim_cell, wellness-domain/ddls.go). A
# dispatcher COULD compute these cells — the roster it loaded carries every
# occurrence's span, and the shift is its own payload — but that declaration
# would be the roster's copy of the schedules, not the schedules: an occurrence
# ReassignSession moved after the roster loaded would leave the real cells
# undeclared and read live anyway, silently. The script derives the set from
# the .schedule each walked occurrence holds NOW, so the read is left live by
# design and recorded here rather than declared from a snapshot. The
# instructor-hub twin of this read needs no row: the instructor vertex is the
# ledBy walk's own far endpoint, admitted as class (e).
read	ReassignSessionSeries	vtx.studio.<id>.slot<t>
read	RecordApplicationLoss	lnk.identity.<id>.appliedToUnit.unit.<id>
read	RecordCapabilityProposal	vtx.capabilityproposal.<id>.request
# RecordDepositDeduction's self-scope proof (self_scope_standing,
# loftspace-ledger): the same two link-shaped follow-ups CreditAccount's rows
# above carry, off the same heldFor walk.
read	RecordDepositDeduction	lnk.identity.<id>.manages.unit.<id>
read	RecordDepositDeduction	lnk.leaseapp.<id>.applicationFor.identity.<id>
# RecordEncounter's workplace fallback (its ONLY non-operator grant is
# `provider`; frontOfHouse is not among its grantees) reuses SetAppointmentStatus's
# staff-standing confinement and bound-provider binder, same fixture under-
# declaration the SetAppointmentStatus/CorrectAppointmentStatus rows record —
# unexercised by any non-operator test until now.
read	RecordEncounter	lnk.identity.<id>.worksAt.building.<id>
read	RecordEncounter	lnk.provider.<id>.identifiedBy.identity.<id>
read	RecordServiceOutcome	lnk.service.<id>.providedBy.serviceprovider.<id>
read	ReleaseOrphanedBooking	vtx.session.<id>.schedule
# RefundCafeCharge runs CreditCafeAccount's workplace confinement verbatim (the
# same post_entry require_workplace site), so it inherits the same two
# undeclarable follow-ups: each worksAt key is built from a location the walk
# itself discovered off the account's own heldFor / appliesToUnit / containedIn
# chain, so no dispatcher can name it up front.
read	RefundCafeCharge	lnk.identity.<id>.worksAt.building.<id>
read	RefundCafeCharge	lnk.identity.<id>.worksAt.unit.<id>
# RefundCafeCharge's self-clearing refusal (post_entry's require_not_own_account)
# reads the applicationFor link off the lease the account's own heldFor walk
# resolved — the same link-shaped class-(e) follow-up CreditCafeAccount's row
# above records, the key data-derived and unknowable to any dispatcher.
read	RefundCafeCharge	lnk.leaseapp.<id>.applicationFor.identity.<id>
read	ReportIssue	lnk.identity.<id>.worksAt.building.<id>
read	ReportIssue	lnk.identity.<id>.worksAt.unit.<id>
# RescheduleAppointment reuses SetAppointmentStatus's staff-standing
# confinement (appointment_sites off the appointment's own withProvider
# walk) and its bound-provider binder (identifiedBy), same fixture under-
# declaration the SetAppointmentStatus/CorrectAppointmentStatus rows record —
# unexercised by any non-operator test until now.
read	RescheduleAppointment	lnk.identity.<id>.worksAt.building.<id>
read	RescheduleAppointment	lnk.provider.<id>.identifiedBy.identity.<id>
read	RescheduleAppointment	vtx.provider.<id>.hours
read	RescheduleAppointment	vtx.provider.<id>.timeOff
read	ResolveWorkOrder	lnk.identity.<id>.worksAt.building.<id>
read	ResolveWorkOrder	lnk.identity.<id>.worksAt.unit.<id>
read	ResumeVisitSeries	lnk.identity.<id>.worksAt.building.<id>
read	RetireMenuItem	lnk.identity.<id>.worksAt.building.<id>
# RevokeIdentityClaim reads each swept credential's credentialindex vertex as
# the (e) follow-up on its boundTo walk (identity-domain/ddls.go): the index
# key is a sha256NanoID of the credential the walk discovered, so its vertex
# root is not the walked endpoint and the guard's discovered-root arm cannot
# admit it, and no dispatcher can name it up front. Category 3; the same shape
# MergeIdentity's row records.
read	RevokeIdentityClaim	vtx.credentialindex.<id>
read	SetAppointmentSite	lnk.identity.<id>.worksAt.building.<id>
read	SetAppointmentSite	lnk.provider.<id>.identifiedBy.identity.<id>
read	SetAppointmentStatus	lnk.identity.<id>.worksAt.building.<id>
read	SetAppointmentStatus	lnk.provider.<id>.identifiedBy.identity.<id>
read	SetBookingAttendance	lnk.identity.<id>.worksAt.building.<id>
# SetCafePolicy's staff-standing confinement (require_workplace on the
# payload's locationKey) reads the caller's worksAt link live — the same
# fixture under-declaration every sibling house-configuration op's row above
# records (CreateMenuItem/RetireMenuItem/SetMenuItemAvailability/UpdateMenuItem).
read	SetCafePolicy	lnk.identity.<id>.worksAt.building.<id>
read	SetMenuItemAvailability	lnk.identity.<id>.worksAt.building.<id>
# SetMenuItemLocation's own staff-standing confinement (require_workplace on
# the payload's newLocation) reads the caller's worksAt link live, same
# fixture under-declaration every sibling menu-catalog op's row above records
# (CreateMenuItem/RetireMenuItem/SetMenuItemAvailability/UpdateMenuItem) —
# this op simply had no non-operator test exercising it until now.
read	SetMenuItemLocation	lnk.identity.<id>.worksAt.building.<id>
# menu_item_served_at's release-old-link follow-up (Contract #2 class (e),
# the same shape the row below already records for a unit-typed prior
# location) reads live whichever type the item's CURRENT servedAt happens to
# be — building here.
read	SetMenuItemLocation	lnk.menuitem.<id>.servedAt.building.<id>
read	SetMenuItemLocation	lnk.menuitem.<id>.servedAt.unit.<id>
read	SetRenewalTerms	lnk.identity.<id>.manages.unit.<id>
# SetStudioProfile's front-of-house binder is TombstoneStudio's verbatim: the
# worksAt key is tested at the building the studio's own declared locatedAt
# walk resolves, a link-shaped class-(e) follow-up (category 3) no dispatcher
# can name up front.
read	SetStudioProfile	lnk.identity.<id>.worksAt.building.<id>
read	SetVisitSeriesSite	lnk.identity.<id>.worksAt.building.<id>
# Settle's staff-standing confinement (leaseapp_unit -> require_workplace) is
# the walk Charge/OpenTab/VoidCharge/MarkLineServed carry verbatim: the tab's
# own lease resolves into a unit only the walk itself discovers, so the worksAt
# key tested at each containedIn ancestor cannot be named by any dispatcher up
# front. The op always had the walk; the first non-operator Settle vector (a
# frontOfHouse Settle{paidCents}, workplace_confinement_test.go) is what first
# exercised it -- an operator short-circuits before it runs.
read	Settle	lnk.identity.<id>.worksAt.building.<id>
read	Settle	lnk.identity.<id>.worksAt.unit.<id>
read	Settle	vtx.leaseapp.<id>
read	StartVisitSeries	lnk.identity.<id>.worksAt.building.<id>
read	StartVisitSeries	vtx.visitseries.<id>.paused
read	StartVisitSeries	vtx.visitseries.<id>.series
# StopSessionSeries' front-of-house binder is CreateSessionSeries' workplace
# walk verbatim (worksAt_covers off the caller-supplied studio) — the same
# category-2 confinement read TombstoneSessionSeries and the other
# staff-widened wellness ops carry.
read	StopSessionSeries	lnk.identity.<id>.worksAt.building.<id>
read	TombstoneAppointment	lnk.appointment.<id>.forPatient.patient.<id>
read	TombstoneAppointment	lnk.appointment.<id>.withProvider.provider.<id>
read	TombstoneAppointment	vtx.appointment.<id>.schedule
read	TombstoneSession	lnk.identity.<id>.worksAt.building.<id>
# TombstoneSessionSeries' front-of-house binder is CreateSessionSeries' workplace
# walk verbatim (worksAt_covers off the caller-supplied studio) — the same
# category-2 confinement read the other staff-widened wellness ops carry.
read	TombstoneSessionSeries	lnk.identity.<id>.worksAt.building.<id>
# TombstoneSessionSeries confirms, per occurrence the partOf walk resolves, that
# the occurrence is still held at the studio the caller confirmed — the same
# deterministic atStudio key require_matching_studio reads, so an occurrence
# ReassignSession has since moved elsewhere is passed over rather than having a
# stranger's slot cells released through it. The occurrence id comes out of the
# walk, so no dispatcher can name this key: the caller holds only a series key.
read	TombstoneSessionSeries	lnk.session.<id>.atStudio.studio.<id>
# TombstoneStudio's front-of-house binder is CreateStudio's workplace walk
# (worksAt_covers), but off the studio's OWN building, which the script
# resolves from the studio's locatedAt link rather than from the payload — so
# the worksAt key tested at that building is a class-(e) follow-up off the
# declared locatedAt walk (link-shaped, hence listed; category 3), not a
# payload-direct declaration gap.
read	TombstoneStudio	lnk.identity.<id>.worksAt.building.<id>
read	UpdateMenuItem	lnk.identity.<id>.worksAt.building.<id>
read	VerifyGuarantor	lnk.identity.<id>.manages.unit.<id>
# MarkLineServed runs VoidCharge's confinement walk verbatim (the tab's own
# lease -> unit -> containedIn ancestors, then the worksAt key at each), so it
# carries the same three undeclarable follow-ups.
read	MarkLineServed	lnk.identity.<id>.worksAt.building.<id>
read	MarkLineServed	lnk.identity.<id>.worksAt.unit.<id>
read	MarkLineServed	vtx.leaseapp.<id>
read	VoidCharge	lnk.identity.<id>.worksAt.building.<id>
read	VoidCharge	lnk.identity.<id>.worksAt.unit.<id>
read	VoidCharge	vtx.leaseapp.<id>

# walk rows: per operationType, the kv.Links enumerations no contextHint declares.
#
walk	BackfillAppointmentSite	vtx.appointment.<id> atSite out
walk	BackfillAppointmentSite	vtx.appointment.<id> withProvider out
walk	BackfillAppointmentSite	vtx.provider.<id> practicesAt out
# BackfillLeaseTerms resolves its unit the same way SignLease/DecideLeaseApplication
# do (leaseapp_unit(), scripts.go); an operator-only manual repair op with no
# OpMeta/Dispatch descriptor to declare the walk in.
walk	BackfillLeaseTerms	vtx.leaseapp.<id> appliesToUnit out
walk	BackfillVisitSeriesSite	vtx.provider.<id> practicesAt out
walk	BackfillVisitSeriesSite	vtx.visitseries.<id> atSite out
walk	BackfillVisitSeriesSite	vtx.visitseries.<id> withProvider out
walk	CancelBooking	vtx.booking.<id> settlesClassPrice in
walk	CancelBooking	vtx.building.<id> containedIn out
walk	CancelBooking	vtx.session.<id> atStudio out
walk	CancelBooking	vtx.session.<id> forSession in
walk	CancelBooking	vtx.studio.<id> locatedAt out
walk	CancelBooking	vtx.wellnesstransaction.<id> postedTo out
walk	CancelRenewal	vtx.leaseapp.<id> appliesToUnit out
walk	CancelRenewal	vtx.renewal.<id> renews out
# The link-discovered second hop of Charge's own worksAt_covers walk: reached
# only when the tab's unit itself does not match, off the containedIn ancestor
# that hop resolves -- the same shape VoidCharge's confinement walk carries.
walk	Charge	vtx.building.<id> containedIn out
walk	Charge	vtx.leaseapp.<id> appliesToUnit out
walk	Charge	vtx.menuitem.<id> servedAt out
# house_tab_limit's walk up the tab's unit chain (the resident-self leg's
# TabLimitExceeded guard) reaches the property level a building is contained
# in — the same link-discovered hop as the building row above, one level up.
walk	Charge	vtx.property.<id> containedIn out
walk	Charge	vtx.unit.<id> containedIn out
walk	ClinicCreditAccount	vtx.clinicaccount.<id> heldFor out
walk	ClinicCreditAccount	vtx.clinicaccount.<id> postedTo in
# The link-discovered hubs of that same confinement guard: the appointment's
# provider, that provider's sites, and each site's containedIn ancestry are all
# reached from a hub a prior walk produced, which no declaring channel can name
# at dispatch time (see this file's walk-row note above).
walk	CorrectAppointmentStatus	vtx.appointment.<id> atSite out
walk	CorrectAppointmentStatus	vtx.appointment.<id> withProvider out
walk	CorrectAppointmentStatus	vtx.building.<id> containedIn out
walk	CorrectAppointmentStatus	vtx.provider.<id> practicesAt out
walk	CreateAppointment	vtx.building.<id> containedIn out
walk	CreateAppointment	vtx.leaseapp.<id> applicationFor out
walk	CreateAppointment	vtx.provider.<id> practicesAt out
walk	CreateBooking	vtx.building.<id> containedIn out
walk	CreateBooking	vtx.session.<id> atLocation out
walk	CreateBooking	vtx.session.<id> atStudio out
walk	CreateBooking	vtx.studio.<id> locatedAt out
walk	CreateLeaseDocInstance	vtx.leaseapp.<id> applicationFor out
walk	CreateLeaseDocInstance	vtx.leaseapp.<id> appliesToUnit out
walk	CreateLeaseDocInstance	vtx.unit.<id> manages in
walk	CreateMenuItem	vtx.building.<id> containedIn out
walk	CreateSession	vtx.building.<id> containedIn out
walk	CreateSession	vtx.studio.<id> locatedAt out
# CreateSessionSeries' front-of-house binder walks the studio's rooms and
# their containment the same way CreateSession's row above does (the shape
# ReassignSessionSeries' and TombstoneSessionSeries' own rows already name).
walk	CreateSessionSeries	vtx.building.<id> containedIn out
walk	CreateSessionSeries	vtx.studio.<id> locatedAt out
walk	CreateStudio	vtx.building.<id> containedIn out
walk	CreateStudio	vtx.unit.<id> containedIn out
walk	CreditAccount	vtx.account.<id> heldFor out
walk	CreditAccount	vtx.account.<id> postedTo in
# The lease hub of the landlord self-scope proof (CreditAccount /
# LoftspaceRecordCharge, loftspace-ledger) is resolved by the heldFor walk
# above, so its appliesToUnit enumeration is link-discovered and undeclarable
# by any dispatcher.
walk	CreditAccount	vtx.leaseapp.<id> appliesToUnit out
walk	CreditCafeAccount	vtx.building.<id> containedIn out
walk	CreditCafeAccount	vtx.cafeaccount.<id> heldFor out
walk	CreditCafeAccount	vtx.cafeaccount.<id> postedTo in
# backfill_balance (packages/cafe-ledger/scripts.go) probes reverses off each
# reason-less CREDIT its bounded postedTo replay discovers, to tell a
# pre-0.6.0 payment (cash paid in) from a pre-0.6.0 refund (not cash) when it
# computes .balance.cashCents. The transaction key only exists once that
# postedTo walk resolves it, so no dispatcher can name it up front — the same
# shape EvaluateCafeArrears' arrears_entries carries below. All three legs
# that run the replay (payment, refund, payout) inherit it.
walk	CreditCafeAccount	vtx.cafetransaction.<id> reverses out
walk	CreditCafeAccount	vtx.floor.<id> containedIn out
walk	CreditCafeAccount	vtx.leaseapp.<id> appliesToUnit out
walk	CreditCafeAccount	vtx.unit.<id> containedIn out
walk	DecideLeaseApplication	vtx.building.<id> containedIn out
walk	DecideLeaseApplication	vtx.unit.<id> containedIn out
walk	EndVisitSeries	vtx.building.<id> containedIn out
walk	EndVisitSeries	vtx.provider.<id> practicesAt out
walk	EndVisitSeries	vtx.visitseries.<id> withProvider out
# arrears_entries (packages/cafe-ledger/scripts.go) follows reverses off each
# CREDIT transaction its own bounded postedTo replay discovers, to net a
# refund against the specific charge it names (arrears_head's rule) instead
# of aging by plain FIFO order. The transaction key only exists once that
# postedTo walk resolves it, so — like the postedTo walk's own per-candidate
# .entry read this target's own doc comment (targets.go) already carries —
# no dispatcher can name it up front.
walk	EvaluateCafeArrears	vtx.cafetransaction.<id> reverses out
# arrears_entries (packages/clinic-ledger/scripts.go) follows reverses off each
# CREDIT transaction its own bounded postedTo replay discovers, to net a
# waiver against the specific charge it names (arrears_head's rule) instead
# of aging by plain FIFO order — the café shape exactly, one hop. The
# transaction key only exists once that postedTo walk resolves it, so no
# dispatcher can name it up front.
walk	EvaluateClinicArrears	vtx.clinictransaction.<id> reverses out
# tenant_for_account (packages/loftspace-ledger/scripts.go) resolves the
# tenant an arrears reminder addresses TWO hops from the account: the heldFor
# walk (declared by the loftspaceArrearsReminders playbook) names the lease,
# and the lease's own applicationFor link names the identity. The lease hub
# only exists once the heldFor walk has resolved it, so no dispatcher can
# name this second walk up front — the same shape CreditAccount's landlord
# proof carries above for the lease's appliesToUnit hop.
walk	EvaluateLoftspaceArrears	vtx.leaseapp.<id> applicationFor out
# arrears_entries (packages/wellness-ledger/scripts.go) follows the same
# netting hops off each CREDIT its bounded postedTo replay discovers, and in
# this ledger the charge a refund gives back is TWO hops away: the refund
# credit's settlesRefund link names wellness-domain's wellnessrefund marker,
# and the marker's reverses link names the charge (the booking is tombstoned
# by the time the refund posts, which is why the marker exists). Both hubs
# only exist once the postedTo walk — and then the settlesRefund hop — has
# resolved them, so no dispatcher can name either up front.
walk	EvaluateWellnessArrears	vtx.wellnessrefund.<id> reverses out
walk	EvaluateWellnessArrears	vtx.wellnesstransaction.<id> settlesRefund out
# JoinWaitlist's share of CreateBooking's confinement walk (see the read row
# above): session_locations resolves the studio off the session's atStudio
# link and the building off the studio's locatedAt link, worksAt_covers then
# climbs containedIn from that building, and the tombstoned-studio fallback
# reads the session's own atLocation snapshot — every hub past the session is
# link-discovered, and the four are one chain, left undeclared as one piece.
walk	JoinWaitlist	vtx.building.<id> containedIn out
walk	JoinWaitlist	vtx.session.<id> atLocation out
walk	JoinWaitlist	vtx.session.<id> atStudio out
walk	JoinWaitlist	vtx.studio.<id> locatedAt out
walk	LoftspaceCreateAccount	vtx.building.<id> containedIn out
walk	LoftspaceCreateAccount	vtx.leaseapp.<id> appliesToUnit out
walk	LoftspaceCreateAccount	vtx.unit.<id> containedIn out
walk	LoftspaceRecordCharge	vtx.account.<id> heldFor out
walk	LoftspaceRecordCharge	vtx.leaseapp.<id> appliesToUnit out
walk	MarkPastDueNoShow	vtx.appointment.<id> forPatient out
walk	MarkPastDueNoShow	vtx.appointment.<id> withProvider out
walk	OpenTab	vtx.building.<id> containedIn out
walk	OpenTab	vtx.leaseapp.<id> appliesToUnit out
# house_tab_limit's walk up the lease's unit chain on the resident-self leg
# (the closed-house TabLimitExceeded guard) reaches the property level a
# building is contained in — the same link-discovered hop as Charge's row.
walk	OpenTab	vtx.property.<id> containedIn out
walk	OpenTab	vtx.unit.<id> containedIn out
walk	PauseVisitSeries	vtx.building.<id> containedIn out
# PayOutBalance's self-scope proof + its own account_balance_cents replay
# (self_scope_standing / account_balance_cents, loftspace-ledger): the
# account's own heldFor walk resolves the lease (appliesToUnit resolves the
# unit for the landlord's manages proof), and the postedTo walk is the SAME
# bounded, read-posture-(e)-annotated budget CreditAccount's own resident
# self-credit cap runs (see its rows above) -- here it runs on every accepted
# submission, not only a resident's.
walk	PayOutBalance	vtx.account.<id> heldFor out
walk	PayOutBalance	vtx.account.<id> postedTo in
walk	PayOutBalance	vtx.leaseapp.<id> appliesToUnit out
# PayoutCafeCredit's share of CreditCafeAccount's confinement walk (see the read
# rows above), on RefundCafeCharge's terms: the heldFor and appliesToUnit hubs
# are payload-reachable and the descriptor could name them, but the containedIn
# hubs are link-discovered — a unit's own containedIn target is not known until
# the walk that produced it has run — so the chain is left undeclared as one
# piece rather than split across two postures.
walk	PayoutCafeCredit	vtx.building.<id> containedIn out
walk	PayoutCafeCredit	vtx.cafeaccount.<id> heldFor out
walk	PayoutCafeCredit	vtx.cafetransaction.<id> reverses out
walk	PayoutCafeCredit	vtx.leaseapp.<id> appliesToUnit out
walk	PayoutCafeCredit	vtx.unit.<id> containedIn out
walk	PauseVisitSeries	vtx.provider.<id> practicesAt out
walk	PauseVisitSeries	vtx.visitseries.<id> withProvider out
walk	ReassignSession	vtx.session.<id> atStudio out
walk	ReassignSession	vtx.session.<id> ledBy out
# ReassignSessionSeries' front-of-house binder walks the studio's rooms and
# their containment the way CreateSession's / CreateSessionSeries' rows do.
walk	ReassignSessionSeries	vtx.building.<id> containedIn out
# ReassignSessionSeries reads each moved occurrence's CURRENT instructor to move
# that instructor's own slot cells with it — the same link-discovered hub as
# TombstoneSessionSeries's row below, off the same declared partOf walk.
walk	ReassignSessionSeries	vtx.session.<id> ledBy out
# ReassignSessionSeries walks each moved occurrence's bookings to carry their
# bookers' cells with it — the same link-discovered hub as the ledBy row above.
walk	ReassignSessionSeries	vtx.session.<id> forSession in
walk	ReassignSessionSeries	vtx.studio.<id> locatedAt out
# RecordDepositDeduction's self-scope proof (self_scope_standing,
# loftspace-ledger): the same two walks CreditAccount's/LoftspaceRecordCharge's
# landlord self-scope proofs share, off the same account-owned heldFor walk.
walk	RecordDepositDeduction	vtx.account.<id> heldFor out
walk	RecordDepositDeduction	vtx.leaseapp.<id> appliesToUnit out
# RecordEncounter's staff-standing confinement resolves the appointment's
# bound provider's site(s) the same way SetAppointmentStatus's rows do
# (appointment_sites: withProvider -> practicesAt -> containedIn).
walk	RecordEncounter	vtx.appointment.<id> withProvider out
walk	RecordEncounter	vtx.building.<id> containedIn out
walk	RecordEncounter	vtx.provider.<id> practicesAt out
walk	RecordServiceOutcome	vtx.service.<id> instanceOf out
walk	ReleaseOrphanedBooking	vtx.wellnesstransaction.<id> postedTo out
# Idempotency guard on the no-show-fee charge this op's own `settles in`
# walk discovers, mirroring SetBookingAttendance's identical probe below: an
# attendance correction can already have reversed this same charge before
# TombstoneSession ever ran (fee charged on noShow -> corrected to attended,
# reversing it -> marked noShow again, no second charge posts -> the class
# is then called off), so this release re-walks a settles link a DIFFERENT
# op already resolved. The hub is link-discovered off that walk, so no
# dispatcher can name it up front.
walk	ReleaseOrphanedBooking	vtx.wellnesstransaction.<id> reverses in
# RefundCafeCharge's half of CreditCafeAccount's confinement walk (see the read
# rows above). The heldFor and appliesToUnit hubs are payload-reachable and the
# descriptor could name them, but the containedIn hubs are link-discovered — a
# unit's own containedIn target is not known until the walk that produced it has
# run — so the chain is left undeclared as one piece rather than split across
# two postures.
walk	RefundCafeCharge	vtx.building.<id> containedIn out
walk	RefundCafeCharge	vtx.cafeaccount.<id> heldFor out
walk	RefundCafeCharge	vtx.cafetransaction.<id> reverses out
walk	RefundCafeCharge	vtx.leaseapp.<id> appliesToUnit out
walk	RefundCafeCharge	vtx.unit.<id> containedIn out
walk	ReportIssue	vtx.building.<id> containedIn out
walk	ReportIssue	vtx.unit.<id> containedIn out
# RescheduleAppointment's staff-standing confinement resolves the appointment's
# bound provider's site(s) the same way SetAppointmentStatus's rows below do
# (appointment_sites: withProvider -> practicesAt -> containedIn).
walk	RescheduleAppointment	vtx.appointment.<id> withProvider out
walk	RescheduleAppointment	vtx.building.<id> containedIn out
walk	RescheduleAppointment	vtx.provider.<id> practicesAt out
walk	ResolveWorkOrder	vtx.building.<id> containedIn out
walk	ResolveWorkOrder	vtx.unit.<id> containedIn out
walk	ResolveWorkOrder	vtx.workorder.<id> locatedAt out
walk	ResumeVisitSeries	vtx.provider.<id> practicesAt out
walk	ResumeVisitSeries	vtx.visitseries.<id> withProvider out
walk	RetireMenuItem	vtx.building.<id> containedIn out
walk	RetireMenuItem	vtx.menuitem.<id> servedAt out
walk	SetAppointmentSite	vtx.appointment.<id> atSite out
walk	SetAppointmentSite	vtx.appointment.<id> withProvider out
walk	SetAppointmentSite	vtx.building.<id> containedIn out
walk	SetAppointmentSite	vtx.provider.<id> practicesAt out
walk	SetAppointmentStatus	vtx.appointment.<id> atSite out
walk	SetAppointmentStatus	vtx.appointment.<id> withProvider out
walk	SetAppointmentStatus	vtx.provider.<id> practicesAt out
# A re-mark from noShow to attended reverses the same already-posted
# no-show-fee charge ReleaseOrphanedBooking does, because the mark itself was
# wrong rather than because the class was called off. Its `settles in` lookup
# has no declaring channel, unlike ReleaseOrphanedBooking's identical one: that
# op is a Weaver gap whose GapActionSpec declares the walk on every envelope
# (wellness-domain/targets.go), while this one reaches the Processor from a
# front-desk caller under a descriptor that names no enumeration. `reverses in`
# has none either: CancelBooking is exempt (it tombstones the booking in the
# same batch, so it dispatches at most once and nothing re-walks its settles
# link afterward), but SetBookingAttendance is re-markable, so this walk is
# an idempotency check guarding against walking into the same settles-linked
# transaction twice and double-crediting it — and ReleaseOrphanedBooking
# carries the identical guard for the identical reason (its own row above):
# an attendance cycle can reverse this charge before the session is ever
# tombstoned, so release re-walks the same settles link with no memory of
# that.
walk	SetBookingAttendance	vtx.booking.<id> settles in
walk	SetBookingAttendance	vtx.building.<id> containedIn out
walk	SetBookingAttendance	vtx.session.<id> atStudio out
walk	SetBookingAttendance	vtx.studio.<id> locatedAt out
walk	SetBookingAttendance	vtx.wellnesstransaction.<id> postedTo out
walk	SetBookingAttendance	vtx.wellnesstransaction.<id> reverses in
# worksAt_covers' multi-parent containment walk over the payload's
# locationKey, same shape every sibling house-configuration op's row above
# records (CreateMenuItem/RetireMenuItem/SetMenuItemAvailability/UpdateMenuItem).
walk	SetCafePolicy	vtx.building.<id> containedIn out
walk	SetMenuItemAvailability	vtx.building.<id> containedIn out
walk	SetMenuItemAvailability	vtx.menuitem.<id> servedAt out
# worksAt_covers' multi-parent containment walk over the payload's
# newLocation, same shape every sibling menu-catalog op's row above records
# (CreateMenuItem/RetireMenuItem/SetMenuItemAvailability/UpdateMenuItem) —
# unexercised by any non-operator test until now.
walk	SetMenuItemLocation	vtx.building.<id> containedIn out
walk	SetMenuItemLocation	vtx.menuitem.<id> servedAt out
walk	SetRenewalTerms	vtx.leaseapp.<id> appliesToUnit out
walk	SetRenewalTerms	vtx.renewal.<id> renews out
# SetStudioProfile's front-of-house binder walks the studio's building's
# containedIn ancestors (worksAt_covers) when the direct worksAt link at that
# building is absent — TombstoneStudio's row below, for the same
# link-discovered hub.
walk	SetStudioProfile	vtx.building.<id> containedIn out
walk	SetVisitSeriesSite	vtx.building.<id> containedIn out
walk	SetVisitSeriesSite	vtx.provider.<id> practicesAt out
walk	SetVisitSeriesSite	vtx.visitseries.<id> atSite out
walk	SetVisitSeriesSite	vtx.visitseries.<id> withProvider out
# The link-discovered hops of Settle's confinement walk (see its read rows):
# the unit off the lease's appliesToUnit target and each containedIn ancestor
# off that -- the same shape OpenTab/VoidCharge/MarkLineServed carry.
walk	Settle	vtx.building.<id> containedIn out
walk	Settle	vtx.leaseapp.<id> appliesToUnit out
walk	Settle	vtx.unit.<id> containedIn out
walk	SettleStaleTab	vtx.tab.<id> chargedTo out
walk	StartVisitSeries	vtx.building.<id> containedIn out
walk	StartVisitSeries	vtx.provider.<id> practicesAt out
# StopSessionSeries' front-of-house binder walks the studio's rooms and their
# containment the way TombstoneSessionSeries' rows do — its only walks: the
# stop is the one series verb that never enumerates the run's occurrences.
walk	StopSessionSeries	vtx.building.<id> containedIn out
walk	StopSessionSeries	vtx.studio.<id> locatedAt out
walk	TombstoneSession	vtx.building.<id> containedIn out
walk	TombstoneSession	vtx.session.<id> atStudio out
walk	TombstoneSession	vtx.session.<id> ledBy out
walk	TombstoneSession	vtx.studio.<id> locatedAt out
# TombstoneSessionSeries' front-of-house binder walks the studio's rooms and
# their containment the way CreateSession's / CreateSessionSeries' rows do.
walk	TombstoneSessionSeries	vtx.building.<id> containedIn out
# TombstoneSessionSeries reads each cancelled occurrence's CURRENT instructor to
# release that instructor's own slot cells, exactly as TombstoneSession does for
# the one class it cancels (session_ledby_link, wellness-domain/ddls.go). The hub
# is link-discovered — the occurrence surfaces from the series' partOf walk, which
# the op-meta DOES declare (opmetas.go) — so this second-order walk has no
# dispatcher that could name its hub up front.
walk	TombstoneSessionSeries	vtx.session.<id> ledBy out
walk	TombstoneSessionSeries	vtx.studio.<id> locatedAt out
# TombstoneStudio's front-of-house binder walks the studio's building's
# containedIn ancestors (worksAt_covers) when the direct worksAt link at that
# building is absent — the building is discovered by the declared locatedAt
# walk, so no dispatcher can name this hub up front.
walk	TombstoneStudio	vtx.building.<id> containedIn out
walk	UpdateMenuItem	vtx.building.<id> containedIn out
walk	UpdateMenuItem	vtx.menuitem.<id> servedAt out
walk	VerifyGuarantor	vtx.leaseapp.<id> appliesToUnit out
walk	VerifyGuarantor	vtx.renewal.<id> renews out
walk	MarkLineServed	vtx.building.<id> containedIn out
walk	MarkLineServed	vtx.leaseapp.<id> appliesToUnit out
walk	MarkLineServed	vtx.unit.<id> containedIn out
walk	VoidCharge	vtx.building.<id> containedIn out
walk	VoidCharge	vtx.leaseapp.<id> appliesToUnit out
walk	VoidCharge	vtx.unit.<id> containedIn out
walk	WellnessCreditAccount	vtx.wellnessaccount.<id> heldFor out
walk	WellnessCreditAccount	vtx.wellnessaccount.<id> postedTo in

