// Package healthkv is the shared Health-KV-plane library: primitives that
// read and write the health-kv bucket, used by the engine packages (Loom,
// Weaver, Bridge) rather than duplicated inside each.
package healthkv
