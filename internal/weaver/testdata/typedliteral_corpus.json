{
  "note": "Shared corpus for the json:<literal> param-value rule. internal/weaver's decodeTypedLiteral and internal/pkgmgr's typedLiteralValueError are two independent implementations of one rule (the installer must not import an engine), so each package runs THIS file and asserts its own verdict against `accept`. A rule change lands in one file and reds the other package until it follows.",
  "cases": [
    {"value": "leased", "accept": true, "why": "plain string literal, not a typed literal at all"},
    {"value": "row.entityKey", "accept": true, "why": "row template; the typed-literal rule never inspects it"},
    {"value": " json:5", "accept": true, "why": "token matched on the leading bytes only, so this is a plain string"},
    {"value": "yaml:{oops", "accept": true, "why": "unrecognised prefix falls through to the plain-string arm (whitelist, not blacklist)"},
    {"value": "json:5", "accept": true, "why": "integer"},
    {"value": "json:0", "accept": true, "why": "zero is a value, not an absence"},
    {"value": "json:1.5", "accept": true, "why": "float"},
    {"value": "json:1e3", "accept": true, "why": "exponent notation is float notation; the author asked for float semantics"},
    {"value": "json:true", "accept": true, "why": "bool"},
    {"value": "json:false", "accept": true, "why": "false is a value, not an absence"},
    {"value": "json:\"leased\"", "accept": true, "why": "string"},
    {"value": "json:\"json:foo\"", "accept": true, "why": "the escape: a string that itself begins with the token"},
    {"value": "json:[1,2]", "accept": true, "why": "array"},
    {"value": "json:{\"k\":1}", "accept": true, "why": "object"},
    {"value": "json:9007199254740992", "accept": true, "why": "2^53 exactly — the boundary is representable, so it is accepted"},
    {"value": "json:{oops", "accept": false, "why": "not valid JSON"},
    {"value": "json:[1,2", "accept": false, "why": "not valid JSON"},
    {"value": "json:\"unterminated", "accept": false, "why": "not valid JSON"},
    {"value": "json:", "accept": false, "why": "the token with no literal after it"},
    {"value": "json:nope", "accept": false, "why": "a bare word is not JSON"},
    {"value": "json:5 6", "accept": false, "why": "trailing data after the top-level value"},
    {"value": "json:{}{}", "accept": false, "why": "trailing data after the top-level value"},
    {"value": "json:null", "accept": false, "why": "indistinguishable from an omitted param"},
    {"value": "json: null", "accept": false, "why": "whitespace does not change what null means"},
    {"value": "json:\"\"", "accept": false, "why": "the empty string is indistinguishable from an omitted param"},
    {"value": "json:9007199254740993", "accept": false, "why": "2^53+1: float64 would dispatch 9007199254740992"},
    {"value": "json:-9007199254740993", "accept": false, "why": "same loss with the sign"},
    {"value": "json:1700000000000000123", "accept": false, "why": "a nanosecond timestamp rounds"},
    {"value": "json:12345678901234567890", "accept": false, "why": "beyond int64 and not exactly representable"},
    {"value": "json:[1,9007199254740993]", "accept": false, "why": "the loss is refused at any depth, not just top level"},
    {"value": "json:{\"a\":9007199254740993}", "accept": false, "why": "the loss is refused inside an object too"}
  ]
}
